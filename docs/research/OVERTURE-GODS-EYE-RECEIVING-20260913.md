# Overture / God's Eye — received build handoff

Received: 13 September 2026. Owner: Codex primary implementation.
Order: [472](../../handoff/orders/472-overture-gods-eye-market-discovery-integration.md).
This is the single receiving record; other project documents link here.

## Latest receiving addendum —13 September2026

The discovery/map/table/confirmed-set/planner source described by the historical
checkpoints below is accepted and published in dd38a974, not yet delivered locally.
Use PROJECT-STATUS for current release gates; earlier statements of missing
composition/UI are dated checkpoints, not the current implementation inventory.

Root directly read the newest Yellow technology research task
6aa66f9c-4154-83e8-a5bf-c2a1fb8bda13 and acknowledged this additional candidate:
Dubai Real Estate Transactions by waelr1985, reported Kaggle version1 uploaded
4March2026, Transactions.csv,89.2MB compressed/1.025GB expanded and1,665,112 rows.
The research task reports1,274,696 sales,328,970 mortgages,61,446 gifts and a
latest transaction date of17February2026 after a full scan. These are
research-reported findings, not receiver-verified file bytes or a Yellow import.
It is transaction history, not the separate Ejari rental-contract ledger or
hotel occupancy/demand truth. The claimed MIT label does not resolve upstream
DLD reuse rights; archive hash, exact version/source and upstream terms remain
required before ingestion.

Revision1.1 handoff is now reported preserved by the research task in
[DLD Drive folder](https://drive.google.com/drive/folders/1pm06Pk0qnHMu-lMSFjn2IIxDRrykQZwo),
with [handoff copy](https://drive.google.com/file/d/1lgLIWgbEqbuCMN_T_JhJN8BxqPlZftpd/view).
Receiver directly read the task reply, not the archive or this Drive file's bytes.
Reported transaction archive:
DLD_Transactions_Kaggle_waelr1985_v1_2026-03-04.zip,89,196,096 bytes,
SHA256 f6e8ab888dad894c4dbcf32b0d5bdb28674d699ba9ff38e8a5c02b5afe0cd89d.
Contained CSV reported1,025,416,653bytes/full scan1,665,112rows; counts above retained.
[Exact versioned source](https://www.kaggle.com/api/v1/datasets/download/waelr1985/dubai-real-estate-transaction?datasetVersionNumber=1).
instance_date reports DD-MM-YYYY/latest17-02-2026; anomalous values
02-07-1416,04-02-1417,30-01-1420,23-11-1422 have unknown calendar provenance.
Upstream rights, completeness and transaction-ID uniqueness remain unverified.

Separate rental/history dataset zainabhejairi/dubai-real-estates-1969-2023 v1 is
reported archived in3parts/216,834,183bytes; original ZIP SHA256
29a2c22e4eaac85864fd5b2918eae98aa64aff892bdf04a4a8afcfd2ebd1310f.
[Versioned source](https://www.kaggle.com/api/v1/datasets/download/zainabhejairi/dubai-real-estates-1969-2023?datasetVersionNumber=1).
ZIP integrity is research-reported; rental counts/calendar coverage and commercial
grant remain unknown (labelOther). Root requested the literal3part manifest/URLs/
hashes before receiver admission. These are not OTA rates, hotel occupancy or
automatically usable RMS training data.

The two original Jarvis documents remain retained by the research task, not
regenerated; it reports two502 retrieval failures and pending Drive copies.
The founder asked that task to build in parallel. Root replied with explicit
non-overlapping ownership: research performs existing-DLD provenance/uniqueness/
calendar quality and compact manifests/scripts; Codex owns Yellow code, DB,
release and CI. No duplicate repository implementation or product-scope change.

The founder suggests the anomalous1416 year could be Hijri. Retain that as a
hypothesis, preserve raw date/calendar fields and seek source provenance before
conversion; never infer a calendar solely from the year. No transform was run.

The founder also asks to keep research in the existing5TB Google Drive. Root
requested durable archive/deliverable locations and integrity receipts directly
from the research task, without a duplicate laptop download or new upload.
Research now reports the DLD backup above; receiver has not independently verified
its file bytes. Stable source/evidence and permitted datasets
must remain distinct from live database/runtime disks, caches, Git internals and
secrets. These research inputs do not interrupt the current bounded release.

## Priority and authority

The founder explicitly prioritized Overture/God's Eye integration before other
feature work in **Yellow technology research** (task
`6aa132ed-07f0-83e8-bfb0-02da6c460a6a`) and the
[PR92 priority comment](https://github.com/dcpnode-maker/yellow/pull/92#issuecomment-5649226705).
The [complete PR86 handoff](https://github.com/dcpnode-maker/yellow/pull/86#issuecomment-5648628002)
contains the recovered archive provenance and acceptance requirements.

Root read the actual task and both comments, sent a receiving message to that
existing task, and assigned an independent read-only audit to
`/root/astra_ultra_handoff` using **gpt-6-astra / ultra** at source63338ed3.
The audit is source inspection, not executed integration acceptance. No reply to
the receiving message was visible at the last task read; the verified founder
messages and GitHub handoffs are sufficient to continue without waiting.

This is a bounded **Phase14 RMS discovery** dependency delivered early, not a
renumbering of the roadmap: Phase13 is voice, Phase11 groups and Phase17 outlets/
interfaces. After this delivery, resume the preserved fiscal work and existing
dependency-gated priorities, including11 ->13 ->17. No feature scope is removed.

## Received assets and what has actually been checked

| Asset | Pinned identity | Evidence boundary |
|---|---|---|
| Overture Places | Release2026-08-19.0, schema v1.18.0;16 original Parquet files,10,480,684,059 bytes,73,631,092 rows | Research reports319 original-byte chunks; all local hashes,36/319 remote redownload/hash checks. Root independently enumerated319 chunks plus7 support files and the same aggregate byte size. Root has not imported or rehashed the data. |
| God's Eye View | Upstream commit759652207fd1279ece97f0f19af566feb9a82146, v0.1.1-8-g7596522 | Research reports all15 chunks/five reconstructed archives remotely hash-verified. Root independently enumerated15 chunks plus6 support files,381,188,971 chunk bytes, and read provenance, restore and archive-verification Markdown. This is not a Yellow runtime or licence acceptance. |

Authoritative Drive locations:

- [Overture v3 folder](https://drive.google.com/drive/folders/1xUx8mnW2RKmQG1AQzzEwEBbSeV0TemvW).
  Never mix incomplete v1/v2 attempts into v3.
- [God's Eye exact archive](https://drive.google.com/drive/folders/1p7o2ssWfFltbNzMzqQmo2aBM_HzwG-iP).
- [Recovery handoff ZIP](https://drive.google.com/file/d/1oMICefkwi5oIK4PCipWYqRkKw57gjsWp/view),
 150,578 bytes. A handoff archive is not evidence that genuine client PriceLabs
 data has been imported.

Support identities (not credentials):

| File | Drive ID | Bytes |
|---|---|---:|
| TRANSPORT_MANIFEST_V3.json | 148en8lsn6FjrG_D5gLq9rggnZrzI-pdX | 97,240 |
| OVERTURE_DRIVE_COMPLETION.json | 1tDlct7yqwSKKwfBwQTYpM8jSJIjN-k-Q | 122,144 |
| OVERTURE_V3_README.md | 18rs9xudv3uhCnwJzuD2Lwh96PSEafeGa | 2,184 |
| restore_overture.py | 1EQda06a28jeN4kvBhizmCQpLMIhj1Rxd | 3,020 |
| EXACT_PROVENANCE.md | 1H0-4b-S6t7Xgeow_xH0dIugaAHJuj012 | 1,700 |
| EXACT_RESTORE.md | 13NUVleBEOTdrgmNZ8YJOhvpFsmCS2aA4 | 1,117 |
| DRIVE_ARCHIVE_VERIFICATION.md | 12jh3gr8XeouV1PhU-PSZlNbD0NF1Pif7 | 4,181 |

The readable Overture README, complete JSON manifest/completion receipt and Python
restorer have now been inspected. The raw streaming connector first returned a
non-materializable sediment reference; bounded readable fetch supplied an
authenticated HTTPS file reference, which the receiver read in memory without
using inline binary or exposing its URL. No file was made public.

Receiver-executed checks: the97,240-byte manifest SHA256 is
7c7541c8af085a10d5d4bfabe95a9b52b04701747cf20f0f2799d7dd0ac89d34,
matching the completion receipt. All16 originals have contiguous unique chunks,
valid declared SHA256 fields and matching byte/row totals. All319 manifest chunk
names/sizes match the Drive inventory; all319 completion-receipt Drive IDs, names,
sizes and declared hashes match that inventory/manifest. The completion receipt
explicitly reports36 remote content checks, not319. These are receiver-executed
metadata/manifest checks, not rehashes of the10.48GB payload or a Yellow import.

No restoration script was executed or global archive copied to the laptop. Before
local use, the supplied script needs Windows path/reparse and concurrent ownership
review: its name check alone is not a full Windows path policy; its exception
cleanup can unlink a partial file when exclusive creation failed, and replace()
can overwrite a target created after its initial existence check. Prefer a bounded
regional extraction path with verified source provenance to duplicating chunks
and originals. The original research archive/script remain unchanged.

God's Eye source is MIT; bundled layers are not uniformly MIT. The handoff identifies
TeleGeography CC BY-NC-SA3.0 and OSM/OpenInfrastructureMap ODbL1.0. Inspect actual
pinned source, notices and selected layers before reuse; disable or replace
ineligible commercial layers. Archived Linux runtimes/npm caches/dist are recovery
material, not permission to add a second stack or install all upstream dependencies.

## Product contract

Property URL/ID -> explainable own-property match -> regional candidate search ->
explicit client-confirmed compset -> selectable2D map/table/attributes. Preserve
source IDs/GERS, names, addresses, coordinates, websites, categories, release,
attribution and available confidence. An ambiguous name/proximity match must not
silently become an operational property or confirmed competitor.

Keep the global snapshot server-side; send bounded regional/viewport results.
Optional3D loads only on demand, and its failure must leave the table and
confirmation journey usable. Respect the existing approved interface direction.

Overture discovery is not a live quote, availability, rooms sold, occupancy or
unconstrained-demand measure. Unknown operating status is not proof a property is
open; category counts are not unique verified hotels or complete STR coverage.
Keep permitted market observations separate with source, capture/as-of time,
original search conditions and explicit dated fallback.

The existing founder cadence remains: days0–7 hourly;8–20 every2hours;21–30
every3hours;31–45 every4hours;46–90 every5hours;91–180 every6hours. Boundaries45/90
use the nearer band. Normal shopping uses the selected3/4-calendar-month horizon;
beyond180 requires an expanded horizon and configured slower cadence. Shared
queries must preserve entitlements and all comparison conditions. Supplier
budgets, incremental refresh and stale-data labelling are required.
For agreed30% gross margin, quote cost/0.70, not cost plus30%.

## Reuse and remaining gaps — Astra Ultra audit

Reuse the distribution context, not a new PMS or pricing engine:

- `market-shopping.ts`: exact comparison keys, cadence, bounded runner and integer
  gross-margin quotation already exist. Cache/budgets are process-local, not a
  persistent autonomous scheduler.
- `market-batches.ts`: calendar horizon, timezone, shared destination queries,
  request budgets and urgency already exist.
- `market-source-adapters.ts`: rate/query normalization is comparison-only.
  Overture needs a separate discovery type; never invent a zero price to fit the
  live-rate candidate type.
- `distribution/index.ts`: retain the public context boundary.
- `src/http/operator.ts`: reuse authenticated property grants and the existing
  shell. No mounted market/compset screen exists yet. D1489 now proves persisted
  confirmed compsets through the new managed service/API on a synthetic database.

Do not expand the PriceLabs staging loader: it enforces exactly two tables and a
100MiB archive bound. Its integrity/provenance principles are reusable, not its
schema as a global discovery warehouse. See
[PriceLabs receiving](PRICELABS-RECEIVING-INTEGRATION-20260909.md).

Built source at the current checkpoint:

- Normalized identity/provenance contract, bounded v2 source-point artifact
  adapter, immutable byte-admission checker and dated refresh fallback.
  Non-implementing root personally passes66 tests/470 assertions, strict types
  and201 import boundaries; actual pinned artifacts pass byte admission too.
- Native range extractor independently reviewed and executed by Astra Ultra:
  13 tests pass, including the actual pinned DuckDB GEOMETRY query. Root produced
  and independently hashed/adapted two real bounded outputs: Riyadh33 records/
  41,226 bytes; Dubai35 records/43,688 bytes. Both are all-places queries, not hotel
  completeness or identity-accuracy proof. Source operating status remains unknown.
- Old bbox-derived v1 artifacts and the genuine GEOMETRY-to-BLOB failure remain
  evidence. Current v2 uses strict source WKB points and rejects legacy v1.

Exact hashes, commands, reviewer ownership, artifacts and limitations live in
[Review472](../../handoff/reviews/472-market-discovery-intake.md), not duplicated here.

Concrete remaining gaps, not completed capabilities:

1. Runtime composition and useful candidate lookup. Native byte/hash/source loading
   is independently proved, but startup does not yet compose the new market API.
   Small extracts do not establish real UAE/KSA match accuracy.
2. UI use of the now-proved authenticated property confirmation/versioned compset
   storage. D1489 uses managed `market_compset`, not unit/rate `channel_map`.
3. One mounted end-to-end map/table/confirmation workspace.
4. Connect the now-built dated fallback to the eventual operator journey. Cached
   evidence survives failed/non-actionable refreshes with its original source
   dates/reason; it does not become fresh or actionable, and the cache is not durable.
5. Windows market-batch intake still rejects `unsupported_windows_acl`; the
   PriceLabs Windows writer does not fix this separate path.
6. Persistent supplier quota/schedule/lease/cost accounting and permitted live
   shopping remain later explicit work, not claims inferred from a pure planner.

## Acceptance and speed

Astra Ultra's completed storage audit selects a new kind of versioned configuration,
`market_compset`, using the existing extension primitive rather than a new table.
The server must derive tenant/property/actor, resolve immutable artifact references,
lock before expected-current-version comparison, atomically retire/activate with
facts and outbox, and validate bounded content on reads as well as writes. Generic
extension write/list endpoints must not bypass property permissions for this type.
That service/API is now implemented and independently root-executed (D1489).
The canonical seed catalogue adds the schema type only:11 types,41 unchanged
instances, no default compset and no automatic permission grants. Migration0092
provides a narrow owner-fixed authorization/row-lock function, not broad identity
table UPDATE access. Actual concurrency, replay revocation and atomic rollback
proof passed on the one guarded synthetic target; no live registration occurred.

The initial server catalog will use a private two-entry deployment manifest for
the accepted Riyadh/Dubai snapshots and load it once into immutable memory. The
pure byte-admission checker binds expected length/hash/region to exact source/tool
pins; expected metadata must come from the server, never a browser request.
The filesystem half is now independently accepted at source (D1488): a hidden,
deadline/output-bounded PowerShell7 child retains native directory/file handles,
checks every ancestor and leaf for reparse flags, verifies opened final paths and
identity, and returns exact read-only bytes for the existing admission checker.
Missing or changed files fail the entire configured catalog, with no directory
scan, legacy fallback or permission to confirm property identity. Root personally
loaded both pinned files and executed path/junction/hard-link/writer/size/tamper
rejection; combined81pass/0fail. Exact evidence is in Review472. No dependency was
installed for this loader. Non-Windows native loading explicitly fails closed;
Q266 now mounts this catalog explicitly at startup before any resources and adds
the lazy manual source-point map/table/review/confirmation workspace. Separate
read-only readiness verifies exact authority metadata without granting or repair.
Root independently executed136pass/0fail/1311 across19 related files, including15
actual PostgreSQL cases,9 native filesystem cases and the38-assertion real browser
journey with synthetic responses. The test-client settlement stall and UI fixes
are retained in Review472; no guard/deadline was weakened. Property confirmation
and database persistence were previously root-proved in D1489. This is source
acceptance, not live delivery. URL/ID suggestions, market-only property discovery
and the existing planner connection remain required, not replaced by manual selection.

### Next bounded integration design — Astra Ultra, after Q266

Reuse the existing identity helper per one exact admitted snapshot for ID, safe
canonical public URL, name and coordinate-based suggestions. A suggestion is
never automatically confirmed and no URL is fetched.

Add a read-only preview from the validated persisted competitor set, with exact
expected extension ID/version and explicit comparator indexes. The existing
planner accepts1–200 competitors versus the stored set's0–500 limit: reject an
empty/oversized preview selection, never silently truncate. Derive tenant,
property/timezone/currency server-side; reuse current authorization/version locks
and the existing planner unchanged. Use a non-executable preview namespace and
bounded result summary/sample, not supplier authorization, scheduling or live
collection. Saved historical evidence must remain visible independently of the
editable current-catalog selection.

Market-scoped property discovery must remain separate from operational grants,
allowing market-only users to enter this workspace without giving availability
access. This navigation list does not replace per-operation locked authorization.
The proposed source changes need their exact next admitted order scope before
implementation; no new table, permission, migration or external query is proposed.

### Q267 receiving completion — D1491,13 September2026

The preceding design was admitted before code in Q267 and is now independently
accepted at source. The current read-authorized APIs return explainable identity
suggestions, exact saved-version planner previews and market-only property pages.
The workspace preserves historical evidence, deliberate identity/subset selection,
guest/search conditions, preview-only status and current-property/session isolation.
Root final153pass/0fail/1641 across19 files includes20 actual PostgreSQL cases,
9 native filesystem cases and2 real-shell/synthetic-HTTP browser journeys with82
assertions. Types/202 boundaries pass; exact proof and limitations are in Review472.

Remaining operator integration: expose the already returned bounded planner
window/sample details, the real2D map and selectable attributes. Do not confuse
the current source-point plot with a street map or planner counts with collected
market rates. Real shopping/observation lifetime and provider activation remain
separate admitted work; no PriceLabs client database is claimed populated.
This checkpoint introduces no new table, migration, service, dependency or stack.
It does not publish or promote the source; current live truth remains in status.

Pinned God's Eye source selection at759652207fd1279ece97f0f19af566feb9a82146:
reuse patterns from retryableLoad, bounded marker-cohort selection and request-render
governor only after an exact scoped adaptation. Do not import the full OSINT app,
commercially restricted cable layers, automatic imagery providers, caches or fonts.
The lockfile resolves Cesium1.138.0; a package range is not its installed identity.
The Google3D configuration asserts Google-geocoder-only use and has GiB-scale cache
defaults, so it is not an appropriate default for Overture lookup. No upstream
source has been copied or viewer dependency installed.

The regional source tranche used one hash-pinned, isolated small DuckDB tool on E:
and publisher range reads, not a10GB Drive restore. Full-download fallback and
automatic extension installation are disabled; memory/temp/threads, wall time,
output and disk reserve are bounded. SQL LIMIT is not a hard network-byte cap;
actual network bytes were not measured. Exact extraction provenance and regional
incompleteness are explicit. This is not global production hosting or a map launch.

Measure representative UAE/KSA match accuracy, duplicate rate, attribute
completeness, time to first validated dashboard, browser bytes, map latency and
actual operating cost. Financial/tenant/state changes require a non-implementer to
execute relevant proof. Discovery alone never activates RMS price publication.

Use Astra Ultra for foundational decisions, faster workers for disjoint bounded
implementation, targeted tests during iteration and full exact-source gates per
coherent delivery. Preserve one checkout and one stable local; avoid duplicate
data, repeated research, per-control orders and repeated unchanged CI retries.

No new founder product decision is required to start this integration. Resolve
authenticated raw-file access and an existing server/index location during
implementation. Ask only if access cannot be completed, representative acceptance
identities are genuinely missing, or live-provider credentials/spending become
necessary. None prevents independent source work now.

Current development, CI, local runtime and frozen fiscal checkpoint are maintained
in [PROJECT-STATUS](../PROJECT-STATUS.md), not duplicated as live claims here.

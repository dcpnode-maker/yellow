# Order 317 — Management-demo navigation local refresh

**Status:** BUILT-LOCALLY-PENDING-FRESH-TIER3-REVIEW-D879
**Phase:** 7 — founder-visible presentation of already-built Phase 4–6 journeys
**Branch:** `phase-7/management-demo-navigation-local-refresh`
**Base:** `d2522e2` (independently approved Order316 governance head)
**Runtime source:** `d81de9ce4abf820c4aa529fe7ba8407bb990cc2c`
**Risk tier:** 3 — replacement of the sole founder-visible local app; fresh independent non-operating verification mandatory

## Outcome

Build the exact independently approved Order316 candidate and replace only the sole
loopback port-3000 app so the founder sees Today as the working command centre, the
truthful seven-journey index and unobscured Simple destinations. Preserve every
database, property, companion, credential, status and business identity.

## Exact scope

- this order plus append-only governance and fresh review evidence;
- build one exact image from clean runtime source `d81de9c`;
- record pre-change topology, health, app identity and read-only database digest
  without exposing secrets;
- stop and retain the current approved Order315 app as rollback, then start one
  replacement app on the same network, exact loopback bind and inherited environment;
- verify protected one-click login, exactly two properties, canonical root Today with
  all three lanes settled, exact seven journey controls, navigation/overlay/focus,
  24/24 workspace routes, exact310/311/91/P7 status, asset identity, closed obsolete
  ports and unchanged database/companions;
- fresh non-operating Tier3 read-only review.

## Forbidden

No second/public UI, database/provider/Valkey/network/volume restart or replacement;
no schema/migration/seed/data/credential/environment-value/status/review/phase/business
authority change; no post310 statutory implementation; no merge, push, production
deploy or rollback deletion.

## Rollback

If health or acceptance fails, remove only the failed new app, restore the retained
Order315 app name and exact loopback bind, then verify its prior approved health.

## Definition of done

- [x] Exact approved Order316 runtime image built.
- [x] Guarded app-only cutover and live acceptance pass.
- [x] Prior app retained; companions and data unchanged.
- [ ] Fresh non-operating Tier3 reviewer approves.

## Builder evidence — D879

- Exact approved source `d81de9ce4abf820c4aa529fe7ba8407bb990cc2c` built image
  `sha256:56f4caafd3ad54102e14fd4490818b60813acfa4545433b00717dc47a4958477`.
- The first guarded attempt correctly restored Order315 because its explicit health
  metadata had not been copied. The corrected attempt preserves that exact health
  command and now runs `yellow-order317-app` healthy with zero restarts on only
  `127.0.0.1:3000`; `yellow-order315-app-rollback-d873` is retained stopped.
- Protected local login succeeds, exactly two properties are granted, all 24 property
  workspace routes return 200/no-store, root contains Today plus the exact journey
  index, and one-click fields remain prefilled. Served/runtime CSS and JS hashes exactly
  match the approved source.
- Status remains truthful at latest-built310/current311/review91/active-phase7/referee11.
  Ports3002/3123/3188 remain closed.
- PostgreSQL/provider/Valkey remain healthy with zero restarts. Read-only database truth
  is unchanged at59 migrations/110 base tables/2 views/100 policies/2 properties and
  party8/contact0/party-role8/fact75/outbox22.
- Fresh non-operating Tier3 live-browser review remains mandatory.

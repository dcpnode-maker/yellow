# Order 246 — Current project status through Order 245

**Status:** BUILT-UNREVIEWED-D642
**Phase:** 7 — Tax engine and India IRP
**Branch:** `phase-7/current-project-status-order245`
**Base:** `33f29e0` (built-unreviewed Orders244/245)
**Risk tier:** 1 — founder-visible recorded-status truth only
**Owner:** Codex implementation; independent review deferred by founder build-first direction

## Outcome

Refresh the authenticated Project Status snapshot from built Order240/current Order242
to exact built Order245/current Order246 truth. Preserve generated independent review
coverage at 91 and distinguish the new governed tax-attribution persistence plus
parking-security prerequisite from approved or Phase-complete work.

## Exact scope

- `src/project-status.ts`;
- `tests/founder-status.integration.test.ts`;
- this order, `DECISIONS.log` and `handoff/LEDGER.md`.

No HTML, CSS, client JavaScript, endpoint shape, schema, migration, seed, credential,
Compose, database, runtime, local promotion, permission, dependency, merge or public/
production change is admitted.

## Required truth

1. `recordedAt` remains `2026-08-28`; `latestBuiltOrder` becomes 245,
   `currentOrder` becomes 246, `activePhase` remains 7 and `phaseCount` remains 13.
2. Independent contiguous review coverage remains exactly generated Order91.
3. Preserve every independently approved milestone through Order195 and the compact
   builder-only Phase5/6 milestones at Orders199 and 236.
4. Replace the stale Order240 builder milestone with compact Order245 truth for
   Orders237–245: tax evaluation, jurisdiction resolution, attributable quote preview,
   canonical snapshot, governed append-only persistence and the narrow inherited
   occupancy definer-path repair.
5. Explicitly retain posting, fiscal documents/IRP, independent review and Phase7
   completion as remaining work. Phase states remain reviewed0–3,
   built-unverified4, active5–7 and planned8–12.
6. The existing authenticated endpoint remains the sole rendering source and returns
   the same deeply immutable, credential-free snapshot shape.

## Proof

- exact stale-snapshot red before implementation;
- focused founder-status proof;
- standing suite, typecheck, boundaries, licence, audit, four JavaScript syntax and
  diff hygiene.

## Forbidden

- review coverage above 91;
- calling Orders237–245 approved;
- Phase5, Phase6, Phase7 or application-complete claim;
- local rebuild/promotion or database/credential mutation in this order.

This order closes only built-unreviewed. A separately scoped Order247 may promote the
exact committed snapshot and built migrations to the sole loopback-local runtime.

## Built evidence

The authenticated recorded snapshot now reports exact built Order245/current
Order246/active Phase7 truth while generated contiguous independent review coverage
remains 91. The compact builder-only Phase7 milestone replaces Order240 with
Orders237–245 and explicitly retains posting, fiscal documents/IRP, independent
review and Phase7 completion as pending. Phase states and endpoint shape are unchanged.

Focused founder-status proof passes 5/5 plus 2 expected database skips with 84
assertions. The standing suite passes 822/822 plus 717 expected environment skips
with 8,375 assertions across 1,539 tests/278 files. Typecheck, 91 import boundaries,
23-package licence policy, zero-vulnerability audit, all four JavaScript syntax checks
and diff hygiene are green. No database, credential or local runtime changed in this
order.

# Order 271 — Promote approved Orders266–270 to the retained sole local

**Status:** READY-D706
**Phase:** 7 — Tax engine and India IRP
**Branch:** `phase-7/promote-orders266-270-local`
**Base:** `070528a` (independently approved Order270 descendant)
**Risk tier:** 3 — live governed-financial migrations and reversible app replacement
**Owner:** Codex operations; independent non-operating verification mandatory

## Outcome

Advance only retained `yellow_dev` from exact historical migration44 to forward
migrations45/46, then replace only the app from the same clean approved descendant.
Final authenticated status is date2026-08-29/latest266/current269/review91/active7.
Preserve both hotels, every product-table count, credentials, PostgreSQL, Valkey,
network, retained volume and sole loopback3000 topology.

## Exact scope

- verify clean approved ancestry and exact pre-change app/PG/Valkey/image/network/
  volume/topology/database/status identities;
- build exact app and database-tools images before the mutation window;
- create an owner+SYSTEM-only custom-format `yellow_dev` backup under
  `D:\Yellow\backups`, with SHA-256/readable catalogue plus protected97-table count
  and exact property-identity manifests;
- retain current app image as `yellow-order271-rollback:pre-orders266-270`;
- stop only the app, run only the committed production runner, validate binary ledger
  rows1–44, apply exactly migration45 `aec7f04e…10dba` and migration46
  `bd7fb83f…b574`, then rerun zero-PID no-op;
- prove final46/98/88/two hotels, unchanged97 non-ledger counts/property identity and
  exact governed function owner/ACL/search-path truth;
- replace only the app from clean approved HEAD while preserving the complete current
  environment in memory;
- prove health/root/assets, populated masked no-store one-click login, operator
  authentication, exactly two properties, both exact266/269/91/7 statuses and sole3000;
- this order, decision, ledger and independent review evidence.

## Forbidden

No provision, seed, credential/role/password rotation, cache flush, product cleanup,
migration-ledger edit, PostgreSQL/Valkey/network/volume replacement, second app,
public bind, Compose-wide recreate/down, merge, production deployment, review-coverage
advance, Phase7 completion or application-complete claim.

## Rollback

Build/backup/preflight failure causes no runtime mutation. If stopped before migration,
restart the exact retained app. Migration failure keeps the app closed and permits only
complete restoration of the verified backup to literal `yellow_dev` before any app
reopens. App failure after migration restores only the retained prior app image;
migrations45/46 remain forward-compatible. After app reopen, database restoration is
forbidden without a separate emergency order.

## Required proof

1. Restricted readable backup, exact rollback image and pre-change manifests exist.
2. Historical ledger1–44 bytes and all97 product counts remain exact; only45/46 append
   and the runner reruns `applied=0 status=no-op` with no transaction PIDs.
3. PostgreSQL/Valkey/network/volume identities and restart0 remain exact; database is
   exactly46/98/88 with two properties and contained final functions.
4. Sole healthy loopback3000 serves exact approved product/status source and populated
   masked protected login works by pressing only the sign-in button.
5. Fresh independent non-operating Tier-3 review records approval or findings.

## Definition of done

- [ ] Preflight, backup and rollback evidence is verified.
- [ ] Only migrations45/46 and the app container change.
- [ ] Login, two properties, exact status and sole-local topology are green.
- [ ] Independent non-operating review is recorded.

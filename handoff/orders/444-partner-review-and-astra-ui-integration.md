# Order444 — Partner review app and Astra workflow integration

**Status:** ACTIVE — first three-layout shell implemented and locally verified;
complete journey/identity integration and native review promotion remain.
**Date:** 2026-09-07. **Owner:** Codex coordinator. **Phase:** cross-phase UI and
review delivery; preserves Phase7 completion and the 18-phase plan.

## Founder direction

Expose genuinely built functionality in the single review app without waiting for
all of Phase7. Follow Astra's redesigned workflows, not a recolour of the old UI.
The latest instruction defers neomorphism; an earlier request to add it is
superseded for this delivery. Creative/tooling freedom does not waive tenant,
financial, permission, licensing or spending boundaries. The separately prepared
voice-agent document will be supplied for final review and implementation; voice
must be able to invoke governed actions, not merely answer data questions.

## Authoritative source and precedence

The coordinator personally read the complete PR86 handoff comment5556411974 and,
at exact commit397251e0b1756c746657e3fc85b746ab0f238686, the UIUX direction,
staff-workbench specification, profile/skin order and independent review.
[Implementation direction](../../docs/design/ASTRA-IMPLEMENTATION-HANDOFF.md)
records these sources and the newer founder clarification.

PR86 is open/draft and conflicting with current main. Its original five CI jobs
and CodeQL are now successful, verified through GitHub on2026-09-07. That does not
prove a newly integrated fiscal/UI tree or close its rendered acceptance. Do not
merge it wholesale: it still retains the explicitly superseded global expertise
selector. Preserve its source and provenance while composing with current Q208.

Its `442-profile-cards-and-workspace-skins.md` is a different historical order
from local `442-host-recovery-and-merged-local-review.md`. Retain both identities;
never overwrite or silently renumber either order or its evidence.

## Exact initial scope

```text
handoff/orders/444-partner-review-and-astra-ui-integration.md
docs/design/ASTRA-IMPLEMENTATION-HANDOFF.md
docs/design/UIUX-DIRECTION.md
docs/PROJECT-STATUS.md
DECISIONS.log
handoff/LEDGER.md
```

Read-only source/diff/route/runtime inventories are allowed. Before product edits,
append the exact source and test ownership plus route/command mapping to this
order; no silent widening. Before runtime mutation, append exact immutable
candidate, source/control paths, new synthetic targets, privileges, commands,
rollback and verification. These are technical preparations under standing founder
authority, not requests to wait for a model or ask for routine approval.

## Delivery sequence and acceptance

Question209, `handoff/questions/209-current-review-fixtures-and-preserved-upgrade.md`,
admits the exact next native tooling and synthetic fiscal fixture files/owners.
Question210, `handoff/questions/210-partner-preview-status-snapshot.md`, admits
the exact source/test alignment of the in-app recorded status before publication.
It does not authorize runtime mutation before an exact frozen target/rollback
appendix here. PR92/3a0a013 is the prior independently publication-approved
development checkpoint, with its own currently running CI.

### Scoped first product slice — three-layout shell

Admitted 2026-09-07 after the coordinator personally read Astra's definitive
comment5564886892. This is an incremental integration, not completion of the full
new journey/identity/profile/STR design. Q208 browser synchronization now passes
the full run; that run still has one intermittent provider configuration boundary
failure (1832 pass, 1310 explicit environment skips, 1 fail) being investigated
independently. Do not release on a red gate. UI work may proceed in disjoint files.

Exact additional scope and ownership:

```text
native_resume_builder:
  src/http/operator/index.html
  src/http/operator/operator.js
  tests/operator-workspace-skins.test.ts (new, current-source behavioral proof)
coordinator:
  src/http/operator/operator.css
  tests/operator-workspace-layout.browser.test.ts (new, actual rendered proof)
  docs/design/BUILT-CAPABILITY-MANIFEST.md (new, truthful route/evidence inventory)
assigned test integration worker, no production edits:
  tests/material-theme-skins.test.ts
  tests/operator-adaptive-experience.test.ts
  tests/operator-app-bar-responsive-containment.intentional-red.test.ts
  tests/operator-appearance-geometry.test.ts
  tests/operator-folio-loaded-responsive-geometry.intentional-red.test.ts
  tests/operator-invoices.browser.test.ts
  tests/operator-material-themes.test.ts
  tests/operator-management-demo-navigation-finetune.intentional-red.test.ts
  tests/operator-reservation-workspace.integration.test.ts
  tests/operator-workbench.integration.test.ts
  tests/operator-flagship-motion.test.ts (retired global transition coordinator
    replaced by actual pure layout switching and reduced-motion/forced-colours proof)
provider_transport_builder, after the test integration lane is frozen:
  src/app.ts (only missing Owner trust shell deep-GET registration)
  tests/operator-trust-navigation.test.ts (new bounded shell route proof)
```

Replace public Appearance and global Simple/Advanced/Expert controls with exactly
Calm Workbench / Precision Desk / Service Timeline. Retain internal `data-theme`
compatibility temporarily to avoid destabilizing hundreds of existing qualified
styles, but expose no historical skin picker or Neo option. Remove global detail
state and its hiding rules. Contextual disclosure keeps all existing destinations
reachable; role authorization remains the existing server boundary.

Routes/commands are unchanged: `/p/:property/today` feeds existing arrival queues;
reservation board/detail routes feed existing reservation/readiness/check-in
commands; folio routes retain posting/correction/window/settlement commands;
`/p/:property/invoices` and existing new/document subroutes retain readiness,
immutable issue, original-key recovery, receipt and print paths. Housekeeping,
cashier, day-close, owner-trust, vehicle, inventory, restriction, rates, operations
and project-status routes remain reachable through the same view dispatcher.
The manifest binds these to exact existing endpoints/permissions before release.
No endpoint, DTO, SQL, domain command, synthetic fixture or permission change is
admitted in this first slice. Never manufacture receiving acknowledgements.

Narrow read-only route correction: inventory found the already-built Owner trust
client route `/p/:property/trust` lacks its server shell GET, so reload is broken.
Admit only that deep-GET registration alongside the existing shell routes and a
test proving shell response, no domain command and unchanged property/API
authorization. This is not a trust financial API change or a new permission.

Layout switching is a validated page-session-only dataset change, not navigation,
business submission, refetch, rerender/remount or storage write. Preserve in-flight
work, values and request identity. Update obsolete tests to assert the new contract
and retain actual safety/overflow/focus/command coverage; never merely delete the
old tests or broaden expected results until they pass. Root owns shared scope and
governance; each worker edits only its assigned paths.

1. Freeze and finish Q208's current-source tests and independent high-risk proof.
   Keep full-suite failures and explicit environment skips in the record.
2. Integrate the three workspace designs and remove global expertise selection
   through contextual task/role/property disclosure. Preserve mounted subjects,
   drafts, focus, Back context and unknown-command identity. Existing appearance
   options are not the new workflow. No neomorphism development in this pass.
3. Inventory every implemented user action against its actual route, command,
   current permission and review fixture. Label planned/unsupported capability;
   never present the fictional study as an operational module or invent counts.
4. Prepare a reversible native review update. Existing loopback3000 app is
   b5ef708/frontier77; keep its source, database, credentials and receipts for
   rollback. The current resume helper is intentionally fixed to that release;
   never point it at incompatible source or silently mutate its old database.
   No WSL/Docker/new cluster/worktree is required. Current85 needs its own tested
   source, catalogue, governed synthetic permissions and meaningful fixtures.
5. Verify candidate readiness, prefilled sign-in and real authorized journeys
   before replacing only the identified old app on3000. Staging, if needed,
   is temporary, and final topology has one founder app. Hosted fiscal providers
   stay absent/default-off; do not simulate an authentic acceptance receipt.
6. Render actual desktop/intermediate/phone views, keyboard, zoom, reduced motion,
   forced colours and failed/empty/denied/stale/unknown states. Capture real
   screenshots and inspect them. Screen and voice use the same governed commands.

Completed actions should be reachable and demonstrable before the entire roadmap
is finished. This order does not claim that every Phase7 or future hotel/STR
journey already exists. Publish current source/status with exact evidence; never
claim the founder app has updated before its actual runtime/login/journey proof.

## First shell implementation receipt — 2026-09-07

### Scoped follow-on presentation slice — invoice evidence disclosure

Coordinator owns `src/http/operator/invoices.js`,
`src/http/operator/operator.css`, and
`tests/operator-workspace-layout.browser.test.ts` for the next presentation-only
slice. Show issued document number/date and actual registration state ahead of
print actions; place complete existing technical identifiers/hashes in a native
keyboard-operable disclosure. Preserve source values, print freshness checks,
current route/subject and request identities. Do not parse a second financial
truth, change API/DTO/issuance logic, infer a provider outcome, or hide legal
confirmation facts before issuance. Test real rendered disclosure, keyboard,
layout switching, narrow screens and no additional network calls. The prior
PR92 frozen-source receipt below does not cover this later slice.

Coordinator execution, not a substitute for the separate independent fiscal
database proofs recorded under Order440. Final frozen production files:

```text
src/http/operator/index.html 87626e559e5092666094b8caa5ee1ac2ca48450436f11bd83c08f1b37d020ff
src/http/operator/operator.js 07fee442fd5a1ebE9452587037995cd1a6454698c22c88f4bdc09d785f558208
src/http/operator/operator.css 88e0781f67fb6167f009247c5abfbc94eeb14711f3770c7778d4edb764b92525
tests/operator-workspace-layout.browser.test.ts 7eca1888cc4b550847d1e04dfdcfa936ca5fb028faf4a64afb68426e59ae580f
```

Root `bun test`: **1839 pass,1310 explicit environment skips,0 fail,
24731 assertions,3149 tests/522 files,136.38s**. `bun run typecheck`,
`bun run boundaries`(185), `bun run license-check`(23 installed packages) and
`git diff --check` pass. The immediately preceding1837/2 full run retained two
older status/long-stay timing failures. Their unchanged focused suites pass6/0
plus1 Unix skip and12/0 respectively. The final full rerun used unchanged source,
assertions and deadlines, without concurrent static checks; this does not prove
the earlier failures' cause. Prior1832/1 credential-bound failure is retained inQ208.

The new exact-CDP viewport test deliberately reproduced the old window-size
runner's1440-request/1422-rendered mismatch, then verified actual CSS dimensions.
Screenshot checks subsequently reproduced mobile navigation height338.4375px
against an184px maximum. Root corrected that and the320px picker clipping.
Final capture proof:1/0,124 assertions,5.59s; actual1440x900 allthree concepts,
1024x768,768x1024,390x844,320x844, reduced motion and forced colours. Captures
remain local under `.yellow/evidence/order444-workspace-cdp/`; root personally
inspected the corrected320, Precision1440 and Timeline1440 images. Do not commit
the private `.yellow` directory. Browser fixtures are explicitly synthetic HTTP,
not the real fiscal database or the retained founder app.200% browser zoom and
reference-matched identity/full-journey visual acceptance are still outstanding.

The shell preserves all15 existing destinations and fixes only the missing trust
HTML route; no financial API, permission or SQL change belongs to this UI slice.
The public selector offers exactly3 concepts and no Neo/expertise option.
System fonts and historical internal CSS compatibility remain technical debt;
licensed Urbanist/Phosphor and Astra's real people-only identity treatment are next
integration work, not falsely included in this receipt. Stablelocal77 is unchanged.

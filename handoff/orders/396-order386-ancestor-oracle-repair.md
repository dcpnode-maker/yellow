# Order 396 — Order386 ancestor-oracle repair

**Status:** ACTIVE-D1161
**Phase:** 5 — Financials operator delivery proof repair
**Base:** exact Order386 candidate `696f472`
**Risk tier:** 3 — mandatory standing-proof repair for trust-money delivery

Close only the three deterministic stale ancestor-test expectations found by the
fresh non-implementing Order386 reviewer. Production behavior is frozen.

## Exact scope

- update `tests/operator-business-day-seal.integration.test.ts` only so its exact
  `OperatorHttpApi` constructor-tail assertion includes the legitimate appended
  `ownerTrustExpenses` dependency while retaining the seal dependency and every
  existing route, authorization and mutation-sensitive assertion;
- update `tests/operator-reservation-travel.integration.test.ts` only for the same
  exact appended constructor dependency while retaining all travel assertions;
- update `tests/operator-flagship-motion.test.ts` only so its exact non-front-desk
  workspace count includes the added Trust workspace while retaining every identity,
  motion and accessibility assertion;
- append Order386/396 review and governance evidence only.

No production, migration, schema, seed, permission, service, route, UI, dependency,
local, Docker, deployment, merge or push change is admitted.

## Required gates

Run the three repaired tests, the complete operator sweep and the complete standing
suite. Rerun typecheck, import boundaries, licence, audit and diff checks. Then send
the exact repaired candidate to a different fresh non-implementing Tier-3 reviewer,
who must personally rerun the complete Order386 database/security/referee/browser
proof and the repaired standing gates before Orders386/396 may close.

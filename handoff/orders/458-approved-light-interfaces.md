# Order458 — Approved light interfaces and spatial presentation

Status: source implementation and scoped browser proof complete; not published or
promoted to the local review runtime. No phase-completion claim.
Owner: Codex. Active source: phase-7/operator-invoice-workflow in the retained
yellow-order175-folio-responsive-containment worktree. No canonical edits.

## Intent

Exclude Order456 concepts01 Atelier and02 Meridian. Integrate03 Ledger,04 Aura,
07 Orbit,08 Atlas,09 Focus and10 Index as selectable light interfaces. Reinterpret
05 Dispatch as Relay and06 Canvas as Journey: retain useful workflow ideas, not
their literal skins. Existing authorized data, commands, forms, deep links and
permission decisions remain the sole implementation underneath every interface.

Provide meaningful spatial depth, material layering and interruptible transitions,
including an interactive interface gallery. No simulated processing, invented hotel
facts, automatic business actions or claims of a game engine/4D renderer. Respect
reduced motion, keyboard access, visible status and mobile task usability.

## Scope

- src/http/operator/index.html — interface choices, gallery/motion controls only.
- src/http/operator/operator.js — presentation allowlist/default/setter only.
- src/http/operator/operator-interfaces.js — isolated presentation controller only.
- src/http/operator/operator-interfaces.css — eight structural/material treatments.
- src/http/operator.ts and src/app.ts — exact new static asset registration only.
- tests/operator-workspace-skins.test.ts, tests/operator-flagship-motion.test.ts,
  tests/operator-workspace-layout.browser.test.ts — successor interface expectations
  and preservation/geometry proofs, retaining authority and data safeguards.
- tests/operator-interfaces.test.ts and tests/operator-interfaces.browser.test.ts —
  presentation-only behavior, asset delivery, reduced motion, no mutation proof.
  Implementation record: the planned separate browser file was consolidated into
  the already scoped tests/operator-workspace-layout.browser.test.ts; no standalone
  operator-interfaces.browser.test.ts exists or is claimed as executed evidence.
- Q244 successor: tests/operator-material-themes.test.ts,
  tests/operator-adaptive-experience.test.ts,
  tests/operator-management-demo-navigation-finetune.intentional-red.test.ts,
  tests/operator-workbench.integration.test.ts, and
  handoff/questions/244-selected-interface-regression-scope.md. Update only current
  catalogue/default assertions; keep historical fixtures/evidence unchanged.
- docs/design/SELECTABLE-INTERFACES.md and docs/research/OTA-VARIABLE-MAPPING.md —
  approved direction, source-linked inspiration, canonical unique-field catalogue
  and separate provider-depth/mapping requirements. Documentation only, not schema.
- docs/design/prototypes/light-explorations/README.md — selection status pointer.
- this order, DECISIONS.log, handoff/LEDGER.md, docs/PROJECT-STATUS.md.
- .yellow/evidence/order458/ — bounded test captures/receipts, synthetic fixtures only.

## Exclusions and coordination

No database/migration, API business route, authorization, credential, invoice,
reservation/folio command, external OTA action, new dependency, WSL/Docker,
new worktree or local runtime promotion. Order457 remains separately scoped retained
release recovery; its prepared helper is not permission to deploy Order458.
The OTA catalogue distinguishes shared semantics from provider-specific units,
age bands, enum versions, cardinality and unsupported/lossy mappings. PriceLabs is
an RMS reference, not an OTA. Runtime mapping tables are future distribution scope.

Root owns JS/HTML/asset integration and governance. One worker owns the new CSS.
A second owns browser/static proof files after the UI contract is handed over.
A third owns the two design/research documents. No overlapping edits.

## Acceptance

Eight selectable interfaces, no01/02;05/06 reinterpreted. Selection only updates
presentation, never rebuilds forms, requests, draft identities or routes. Every
mounted control retains identity, values, hidden/disabled state and semantics.
Verify actual Chromium at desktop, tablet and phone widths, reduced motion,
keyboard gallery escape/focus restoration, all eight modes and synthetic data
unchanged. Capture representative live-source/synthetic-fixture screenshots.
Run focused source/asset/behavior tests, types and boundaries. Retain failures.
Build evidence is not native-app certification, live deployment or phase completion.

## Execution receipt — 2026-09-08

Root personally executed the final Chromium proof: 1 pass, 0 fail, 627 assertions,
11.74s; all eight modes at desktop/tablet/phone, draft and request preservation,
native gallery keyboard/focus, real 3D projection, explicit/OS reduced motion and
forced-colour/font fallback. Focused source suite: 25 pass, 8 explicit DB-gated
skips, 0 fail, 532 assertions. Types and 194-file import boundaries pass.

Real depth flattening/occlusion and Relay contrast were repaired and recaptured.
The screenshot timing and test-authoring failures are retained in
`.yellow/evidence/order458/ACCEPTANCE.md`, with exact final file hashes and logs.
The comparison is `.yellow/evidence/order458/index.html`. All captured hotel data
is synthetic; no actual database login or business command was used.

The 53-key OTA dictionary is non-exhaustive documentation, not runtime tables.
Current source remains uncommitted/unpublished. Port3000 remains safety-stopped;
Order457 recovery and backend Orders454/455 are separate, unfinished work.

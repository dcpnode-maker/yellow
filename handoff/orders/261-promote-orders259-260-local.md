# Order 261 — Promote approved Order259 and status Order260 to the sole local app

**Status:** READY-D676
**Phase:** 7 — Tax engine and India IRP
**Branch:** `phase-7/promote-orders259-260-local`
**Base:** `2ae5f2a` (approved Order259 plus built Order260 status)
**Risk tier:** 3 — guarded additive migration plus reversible app replacement
**Owner:** Codex operations; independent non-operating verification required

## Outcome

Advance the sole founder-local database from migration42 to the approved empty
migration43 semantic-route root and replace only the app from clean committed
`2ae5f2a`, so `http://127.0.0.1:3000` displays exact status
date2026-08-29/latest259/current260/review91/active7. Preserve both hotels, all data,
protected populated masked one-click sign-in, PostgreSQL, Valkey, retained volume and
one-local topology.

## Exact scope

- sole Compose project `yellow-order175-folio-responsive-containment`;
- exact read-only preflight of current Order258 baseline;
- new owner/SYSTEM-restricted verified custom-format backup on `D:\Yellow\backups`;
- retained rollback tag for current app image;
- production runner applies only migration0043 checksum
  `a5036df30f07c4c8add08c46cdb805c71b87597efa542e368e64aa35d572bf40`
  and then proves no-op;
- database becomes migration43/97 tables/87 policies with empty
  `tax_semantic_route`; all prior rows and two properties remain exact;
- app-only image rebuild/replacement from clean `2ae5f2a` with protected environment
  unchanged;
- this order, decisions, ledger and independent operational review evidence.

## Required proof

1. Preflight exact app/PostgreSQL/Valkey ids, health, retained mount, sole3000,
   closed3002/3188, migration42/96/86, two properties, current256/257 status and
   protected populated masked login/login200 without exposing values.
2. Nonempty restricted backup with SHA-256, readable restore catalogue and exact
   pre-change table digest; retain exact rollback app image.
3. Production runner applies exactly0043 then no-op; migration rows1–42 and every
   pre-existing table count are unchanged; final43/97/87, semantic routes0.
4. Replace only app; PostgreSQL/Valkey ids, retained volume, protected environment,
   two properties and data remain exact.
5. Prove health/root200, no-store three populated fields with masked password,
   login200, both exact status259/260/review91/active7, served project-status,
   semantic-route and migration hashes exact to clean committed candidate, sole3000
   with3002/3188 closed.
6. Independent non-operating reviewer records final state.

## Forbidden

No seed/provision/credential/password/role/permission/data cleanup/cache/volume
mutation; no second lasting local, PostgreSQL/Valkey recreation, public bind,
product-review advance, merge, production deploy, Phase7 or application-complete
claim. No credentials may enter logs, Git or user-facing output.

## Rollback

Build failure leaves the current app untouched. Replacement failure restores the
retained prior app image with exact protected environment. Empty additive migration43
remains backward compatible; any database restore requires a separate emergency
irreversible authorization.

## Definition of done

- [ ] Exact preflight, backup and rollback image are verified.
- [ ] Only migration0043 and the app container change.
- [ ] Login, two hotels, exact status and one-local topology are green.
- [ ] Independent non-operating verification records approval or findings.

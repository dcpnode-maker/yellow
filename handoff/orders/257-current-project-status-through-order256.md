# Order 257 — Current project status through Order256

**Status:** BUILT-UNREVIEWED-D668
**Phase:** 7 — Tax engine and India IRP
**Branch:** `phase-7/current-project-status-order256`
**Base:** `a593745` (independently approved Order256)
**Risk tier:** 1 — authenticated founder-status truth only
**Owner:** Codex implementation

## Outcome

Refresh the existing authenticated project-status snapshot to exact independently
approved Order256 and current Order257 truth so the sole local can visibly report the
latest pushed build. Preserve generated independent review coverage through Order91
and keep unfinished Phases5–7 active rather than claiming completion.

## Exact scope

- `src/project-status.ts`;
- `tests/founder-status.integration.test.ts`;
- this order, `BUILD-PLAN.md`, `handoff/PHASE-7-PLAN.md`, `handoff/ROADMAP.md`,
  `DECISIONS.log` and `handoff/LEDGER.md`.

## Fixed truth

- `recordedAt=2026-08-29`, `latestBuiltOrder=256`, `currentOrder=257`,
  `activePhase=7`, `phaseCount=13`;
- independent review coverage remains generated through Order91;
- phase states remain reviewed0–3, built-unverified4, active5–7, planned8–12;
- the compact Phase7 milestone becomes Orders237–256 and adds the canonical
  positive posting topology, quoted-tax reservation lineage and independently
  approved primary-folio eligibility read/lock/recheck prerequisite;
- configured semantic routing, governed positive posting and reversal/correction,
  fiscal documents/IRP, independent product review and Phase7 completion remain
  explicitly pending.

## Forbidden

No endpoint/HTML/CSS/client/schema/migration/seed/database/credential/runtime/local,
permission, dependency, merge, public/production deployment, product, Phase or
application-complete change. Exact committed status may reach the sole local only
through a separate reversible promotion order.

## Definition of done

- [x] Intentional stale-snapshot proof precedes implementation.
- [x] Exact founder-status and standing gates are green.
- [x] Status closes built-unreviewed without changing runtime or local state.

## Built evidence

The intentional stale snapshot first failed `4/1` plus two environment skips because
committed truth still reported latest Order252. Focused status proof is `5/0` plus two
environment skips with 98 assertions. Standing proof is `835/0` plus 743 expected
environment skips and 8,496 assertions across 1,578 tests/285 files. Typecheck, 94
import boundaries, 23-package licence policy, zero-vulnerability audit and diff hygiene
are green. No runtime or local state changed.

# Order 180 — Single current founder local

**Status:** READY — D-461
**Phase:** 5 · founder human testing
**Branch:** `phase-5/single-current-local`
**Base:** `ad0c895` (independently approved Order179)
**Risk tier:** 2 — reversible loopback-only local operation
**Owner:** Codex operations; independent post-start verification

## Outcome

Restore one and only one current founder-facing Yellow application at
`http://127.0.0.1:3000`, using the exact approved Order179 executable and truthful
Project status. Create a fresh persistent local database and owner-only login handoff,
run governed seeds, and prove login plus core CRUD journeys without exposing secrets.

## Scope

- local Compose project `yellow-local-current` with PostgreSQL, Valkey and exactly one
  app container bound only to `127.0.0.1:3000`;
- fresh named local volumes and exact approved Order179 source/image;
- ignored owner-only `.yellow/current-founder-login.env` containing only distinct
  operator and approver review passwords;
- ignored runtime database authority generated by the approved setup tooling;
- this order, additive D-461, `handoff/LEDGER.md`, and one operational review.

No product, test, schema, migration, seed-code, fixture-code, dependency, public bind,
port3002 app, second local, Order178 database import, payment, tax, fiscal, merge, push
or production deployment is in scope.

## Required operation and proof

1. Preflight proves Docker has no Yellow app and ports 3000/3002 are unbound.
2. Generate two distinct strong passwords once in an ignored owner-only handoff file;
   never print, chat, log or place either in a container environment after seeding.
3. Run all approved migrations, fresh referee 11/11, canonical seed and governed review
   seed. Exact reruns are no-ops and no Order178 scenario output is imported.
4. Build/start exactly one app from approved Order179 on loopback3000. No app listens
   on 3002 and no staging app remains.
5. Prove health, operator login, property grant, Project status latest178/current179,
   Today exact bounded reads, Party/reservation/folio/charge CRUD and restart persistence.
6. Docker inspection proves one app, loopback-only bind, healthy dependencies, no
   secret value in image/container environment/logs and persistent named volumes.
7. Founder handoff gives hotel `yellow-demo`, email `operator@yellow.local`, URL and a
   no-output clipboard command reading the protected file. The password value never
   appears in chat.

## Definition of done

- [ ] Exactly one healthy current app listens only on 127.0.0.1:3000.
- [ ] Port3002 is unbound.
- [ ] Login, Project status and core CRUD proof pass against persistent data.
- [ ] Private credentials are recoverable by the founder without chat/log exposure.
- [ ] Independent operational verification approves the local.

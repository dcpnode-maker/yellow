# Order 274 — Promote approved Order272 and status Order273 to the retained sole local

**Status:** READY-D714
**Phase:** 7 — Tax engine and India IRP
**Branch:** `phase-7/promote-orders272-273-local`
**Base:** `2cc2622` (approved Order272 product plus built Order273 status)
**Risk tier:** 3 — live expand-only migration and reversible app replacement
**Owner:** Codex operations; independent non-operating verification mandatory

## Outcome

Advance only retained `yellow_dev` from exact migration46 to migration47, then replace
only the app from the same clean descendant. Final authenticated status is
date2026-08-29/latest272/current273/review91/active7. Preserve both hotels, every
pre-existing product-table count, credentials, PostgreSQL, Valkey, network, retained
volume and sole loopback3000 topology.

## Exact scope

- verify clean exact source and pre-change app/PG/Valkey/image/network/volume/
  topology/database/status identities;
- build exact app and database-tools images before the mutation window;
- create an owner+SYSTEM-only custom-format `yellow_dev` backup under
  `D:\Yellow\backups`, with SHA-256/readable catalogue plus protected97-table count,
  exact property identity and binary ledger1–46 manifests;
- retain current app image as `yellow-order274-rollback:pre-orders272-273`;
- stop only the app, run only the committed production runner, validate binary ledger
  rows1–46, apply only migration47 `7e5b8a91…c95bf0`, then rerun zero-PID no-op;
- prove final47/99/89/two hotels, unchanged97 prior non-ledger counts, empty new
  registration root and exact owner/RLS/ACL/FK/index truth;
- replace only the app while preserving the complete current environment in memory;
- prove health/root/assets, populated masked no-store one-click login, operator
  authentication, exactly two properties, both exact272/273/91/7 statuses and sole3000;
- this order, decision, ledger and independent review evidence.

## Forbidden

No provision, seed, credential/role/password rotation, cache flush, product cleanup,
migration-ledger edit, PostgreSQL/Valkey/network/volume replacement, second app,
public bind, Compose-wide recreate/down, merge, production deployment, review-coverage
advance, Phase7 completion or application-complete claim.

## Rollback

Build/backup/preflight failure causes no runtime mutation. If stopped before migration,
restart the exact retained app. Migration failure keeps the app closed and permits only
complete restoration of the verified backup to literal `yellow_dev` before any app
reopens. App failure after migration restores only the retained prior app image;
migration47 remains forward-compatible. After app reopen, database restoration is
forbidden without a separate emergency order.

## Required proof

1. Restricted readable backup, exact rollback image and pre-change manifests exist.
2. Historical ledger1–46 bytes and all97 prior product counts remain exact; only47
   appends and the runner reruns `applied=0 status=no-op` with no transaction PIDs.
3. PostgreSQL/Valkey/network/volume identities and restart0 remain exact; database is
   exactly47/99/89 with two properties and contained registration authority.
4. Sole healthy loopback3000 serves exact source/status and populated masked protected
   login works by pressing only the sign-in button.
5. Fresh independent non-operating Tier-3 review records approval or findings.

## Definition of done

- [ ] Preflight, backup and rollback evidence is verified.
- [ ] Only migration47 and the app container change.
- [ ] Login, two properties, exact status and sole-local topology are green.
- [ ] Independent non-operating review is recorded.

# Order 154 — Integrate reviewed runtime-DML follow-ups

**Status:** READY — exact reviewed-union integration
**Phase:** 5 · Cyber remediation
**Branch:** `phase-5/runtime-dml-reviewed-integration`
**Base:** `aff09155d68ad3f69cd0a119e24b79e7f876fc56`
**Risk tier:** 3 — cumulative financial authority integration
**Owner:** Codex integration; non-implementing Tier-3 reviewer executes the final union

## Inputs

- Order 151 executable `8dee5e8c489098f17b59f4f7145e051e06f3c3db`;
- independently approved Order 152 evidence `d7ab0a3b890ae665ee3a098cc606d519d2a25c17`
  for executable `62772c1d7df5119081c3f405677bc177d806ea58`;
- governance-only Order 153 `8683a567d1b0fb85a3105cc9ed859f4e6f89ca60`.

Order 151's isolated cumulative P4 stopped only because its declared Base correctly
excluded Order 152: the matrix reproduced the pre-registered runtime `42501` in the
stale reservation proof. This order composes the authorized sibling corrections before
the final cumulative review; it does not reinterpret or weaken that stop.

## Outcome

Create one exact union containing the bounded financial row-lock capability, all eight
reviewed proof-maintenance corrections, and canonical resolved markers for Questions
147, 148 and 162–164. Change no implementation or assertion while composing the inputs.

## Scope

- this order;
- merge commits for the exact three Inputs above;
- additive `handoff/reviews/154-runtime-dml-reviewed-integration.md` and
  `handoff/LEDGER.md` evidence only.

No hand resolution may change an Input blob. If Git cannot form the exact union without
content edits, stop and record a new bounded order.

## Required proof

1. Base-to-candidate paths equal the set union of the three Inputs plus this order and
   final review evidence; every inherited blob matches its named Input byte-for-byte.
2. Order 151 P0 remains exact `42501`; candidate row-lock authority is 4/4, folios 12/12,
   postings 10/10, runtime-DML 5/5 and SECURITY DEFINER containment 3/3.
3. Order 152's eight fresh isolated suites remain 64/64 with the corrected rollback
   oracle, and forbidden runtime app_user/task/sellable-unit/table-level idempotency DML
   remains `42501`.
4. The complete 22-suite isolated phase matrix passes from zero with distinct deploy and
   runtime credentials; migrations pass 22/22 on Linux/WSL; acceptance and exact live
   schema pass.
5. Standing tests, typecheck, 64-file boundaries, licenses, audit, protected hashes and
   a fresh app-never-started referee pass exactly `11 passed, 0 failed of 11`.
6. A non-implementing Tier-3 reviewer personally executes the union proof and records
   commands, results and findings before any integration approval.

## Forbidden

- Any source, migration, test, schema snapshot, script, documentation or prior
  governance content change beyond the exact Input union.
- Stale proof reuse after the stopped isolated Order 151 matrix.
- New authority, decision, product behavior, dependency, merge to main, push,
  deployment, self-review or Cyber-wide closure claim.

## Definition of done

- [ ] Exact reviewed union exists with no hand-edited Input blob.
- [ ] Full cumulative P0–P4 and fresh referee are green.
- [ ] Independent Tier-3 review approves the immutable union.

# Order 264 — Promote approved Orders262–263 to the sole local app

**Status:** READY-D684
**Phase:** 7 — Tax engine and India IRP
**Branch:** `phase-7/promote-orders262-263-local`
**Base:** `10f78fa` (independently approved Orders262 and 263)
**Risk tier:** 3 — credential-preserving reversible app-only replacement
**Owner:** Codex operations; independent non-operating verification required

## Outcome

Replace only the sole founder-local app from clean committed `10f78fa`, so
`http://127.0.0.1:3000` displays exact status
date2026-08-29/latest262/current263/review91/active7 and its loopback sign-in fields
remain populated after sign-out or failed sign-in. Preserve both hotels, every data
row, the exact protected credentials/environment, PostgreSQL, Valkey, retained
volume and one-local topology.

## Exact scope

- sole Compose project `yellow-order175-folio-responsive-containment`;
- read-only preflight of app/PostgreSQL/Valkey identities, health, ports and exact
  live migration44/98-table/88-policy state;
- new owner/SYSTEM-restricted verified custom-format backup on `D:\Yellow\backups`;
- retained rollback tag for the current app image;
- app-only image rebuild/replacement from clean committed `10f78fa` with protected
  environment byte-identical;
- this order, decisions, ledger and independent operational review evidence.

## Required proof

1. Preflight exact container ids, health, retained mount, sole3000, closed3002/3188,
   migration44/98/88, two properties, old259/260 status and protected populated
   masked login/login200 without exposing values.
2. Nonempty restricted backup with SHA-256 and readable restore catalogue; retain
   the exact current app image under a rollback tag.
3. Do not run migrations or write the database. Prove migration44/98/88, every
   table count, both properties, PostgreSQL/Valkey ids and retained volume unchanged.
4. Replace only app; protected environment names and values remain byte-identical.
5. Prove health/root/login HTTP200, no-store three populated fields with masked
   password, served source exact to the candidate, both property snapshots exact
   262/263/review91/active7 and sole3000 with3002/3188 closed.
6. Independently prove the loopback helper can restore all three closure-held
   defaults through the exact cancelable event and that ordinary/non-loopback
   documents retain password clearing.
7. Independent non-operating reviewer records final state.

## Forbidden

No migration runner, seed, provision, credential/password/role/permission/data
mutation, database SQL write, cleanup, cache or volume mutation; no second lasting
local, PostgreSQL/Valkey recreation, public bind, product-review advance, merge,
public/production deploy, Phase7 or application-complete claim. No credential value
may enter logs, Git, review evidence or user-facing output.

## Rollback

Build failure leaves the current app untouched. Replacement failure restores the
retained prior image with the exact protected environment. Database restore is not
authorized by this order and requires separate emergency authority.

## Definition of done

- [ ] Exact preflight, restricted backup and rollback image are verified.
- [ ] Only the app container changes; database/cache/data remain byte-equivalent.
- [ ] Login, two hotels, exact status and one-local topology are green.
- [ ] Independent non-operating verification records approval or findings.


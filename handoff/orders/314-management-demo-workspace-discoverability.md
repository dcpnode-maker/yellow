# Order 314 — Management-demo workspace discoverability

**Status:** COMPLETE-D870
**Phase:** 7 — Tax engine and India IRP (presentation-only local demo refinement)
**Branch:** `phase-7/management-demo-workspace-discoverability`
**Base:** `9ca6d38` (independently approved Order311 governance head)
**Risk tier:** 1 — static presentation and accessibility copy only

## Outcome

Keep the safe Simple workspace default while making every already-built management
workspace discoverable before the founder opens the secondary-workspace menu. Name the
seven additional workspaces in compact noninteractive preview copy and bind that copy to
the existing disclosure for assistive technology.

## Exact scope

- this order plus append-only decision, ledger, Phase-7 plan and roadmap evidence;
- `src/http/operator/index.html` and `src/http/operator/operator.css` only;
- the exact adaptive-experience regression proof and bounded UI specification copy;
- read-only authenticated browser verification against the sole approved local after a
  separately admitted guarded app-only refresh.

## Required behavior

- Simple remains the initial detail level and retains one disclosure for the seven
  secondary workspaces;
- before opening it, visible compact copy names Operations, Housekeeping, Vehicle
  register, Inventory setup, Restrictions, Rates and Project status;
- the disclosure references that preview through `aria-describedby`;
- Advanced and Expert continue to expose the real controls directly and do not repeat
  the preview;
- there is still exactly one semantic control and route for each of the twelve existing
  workspaces.

## Forbidden

No new workspace, route, command, API, permission, business behavior, status/review/phase
claim, dependency, asset, animation, database/schema/migration/seed/data/credential,
second local, public bind, merge, push or production deployment. Do not change the
Order176 safe Simple default or the Order195 fixed-menu/focus behavior.

## Pre-registered proof

1. Intentional red requires the absent preview, exact seven names and accessibility bind.
2. Focused adaptive/geometry/assets tests stay green with one shell and twelve controls.
3. Standing/static gates show no product authority or dependency drift.
4. Guarded app-only local refresh, if separately admitted, preserves the approved
   Order311 database, credentials, topology and data counts.

## Definition of done

- [x] Intentional red precedes implementation.
- [x] Exact discoverability and accessibility proof passes.
- [x] Focused and standing/static preservation gates pass.
- [ ] Sole-local guarded refresh is separately evidenced.

## Completion evidence — D870

- Intentional red failed `0/1` before product source changed.
- Exact adaptive, geometry and asset proof passes `22/0` with 293 assertions;
  the twelve semantic workspace controls remain singular.
- Standing proof passes `1126/0` with 890 expected database skips, 17,114
  assertions and 2,016 tests across 366 files.
- Typecheck, 6/0 import boundaries (27 assertions), 23-package licence policy,
  zero audit findings and diff checks pass.
- Product delta is limited to static preview/accessibility copy, styling, one
  regression test and bounded UI specification text. No JavaScript, API, domain,
  database, dependency, credential or runtime source changed.

# Order 311 — Recover and refresh the sole management-demo local

**Status:** READY-D861
**Phase:** 7 — Tax engine and India IRP
**Branch:** `phase-7/current-management-demo-local`
**Base:** `3f50f7e` (independently approved Order310 governance head)
**Risk tier:** 3 — recovery of retained hotel data, forward migration and local app replacement
**Owner:** Codex operations; fresh independent non-operating verification mandatory

## Outcome

Restore exactly one loopback-local Yellow stack from the verified Order274 backup after
the laptop/Docker reset removed its containers, advance it only through the committed
forward migrations, run the current approved-descendant app, and show truthful status
through approved Order310. Expose existing integrated user journeys for the founder's
management demonstration without claiming unfinished phases complete.

## Exact scope

- verify the retained Order274 dump hash/catalogue and existing approved app/rollback images;
- update only the founder-visible recorded snapshot and its exact tests from Order272/273
  to latest approved Order310/current local-refresh Order311;
- retain reviewed-through Order91 and truthful phase states:0–3 reviewed,4 built-unverified,
  5–7 active,8–12 planned;
- create exactly one loopback-only Compose project using the protected existing runtime
  authority and founder-login files without printing secrets;
- restore the verified dump into a fresh named volume, apply only committed forward
  migrations47–59 with the production runner, then prove a zero-apply rerun;
- build/replace only this one local app from the exact clean candidate and enable the
  existing operator workbench, one-click protected login and existing safe workers;
- verify health/root/assets/auth/two-property discovery, status, Today, availability,
  reservations, folios, operations, inventory, restrictions, rates and project-status
  journeys using read-only or already governed requests;
- record exact backup, image, container, volume, migration and executable evidence.

## Forbidden

No second local; no public bind; no production deployment; no reseed or synthetic overwrite;
no credential rotation or secret disclosure; no historical migration edit; no migration-
ledger edit; no feature invention, phase-complete claim, review-coverage advance, merge or
push. No financial/statutory mutation is performed merely for the demonstration.

## Rollback

Before app exposure, any restore/migration/build failure removes only the newly created
Order311 disposable local resources and leaves the verified backup untouched. After app
exposure, app failure may replace only the app from the retained Order274 image; database
restoration requires closing the local and replaying the verified dump into a new volume.

## Required proof

1. Intentional status red precedes the snapshot update; focused/standing/static gates pass.
2. Backup hash/catalogue and protected credential-file presence pass without secret output.
3. Exactly one loopback local reaches migration59 and a second runner pass applies zero.
4. Login, both hotels, truthful310/311/91/7 status and the existing management journeys pass.
5. Fresh independent non-operating Tier-3 verification records approval or findings.

## Definition of done

- [ ] Status implementation and proof are green.
- [ ] Verified hotel data is restored and migrated forward exactly once.
- [ ] Sole local login and management-demo journeys are executable.
- [ ] Fresh independent verification is recorded.

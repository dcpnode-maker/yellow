# Q279 — isolated real PostgreSQL registry proof preparation

Order460, 13 September2026. Prepare proof for accepted Q276 metadata registration.
The existing synthetic Q265 public catalogue already contains market metadata.
Never delete it to manufacture an added-path test.

Builder /root/q258_runtime_cutover owns only these new ignored files:

- .yellow/evidence/order460/market92-registration-native.test.ts
- .yellow/evidence/order460/market92-registration-isolation.ts
- .yellow/evidence/order460/market92-registration-isolation.test.ts

Keep market92-registration.ts and every production source unchanged. A test-only
tagged-SQL adapter may substitute exactly the two literal public relation names
with an exact validated UUID-named proof schema, leaving all parameter values,
SQL clauses and the advisory-lock key unchanged. Reject every unexpected query,
schema, identifier or operation. This is a transparent isolation seam, not proof
that the fixed-public deployment call was executed.

Prepare opt-in native proof using only the existing Q265 environment parser and
exact yellow_order472_compset_20260913 target on127.0.0.1:55503. Before any DDL,
verify native16.15/cluster/deployer identity. One exclusive UUID-owned schema may
later contain only empty LIKE copies of extension_type and permission, preserving
actual constraints/defaults; no public DML, seeded fixtures, role/grant change or
new database. Prove rollback, one added/one existing result under actual lock
contention, exact conflict rollback and unchanged public metadata/instance/grant
fingerprints. Verify schema ownership before cleanup; drop only that schema with
exact known objects, never an arbitrary CASCADE or reused target.

Use bounded connection/statement/lock/overall deadlines and transaction cleanup.
Do not log private authority or record contents. Failed proof must not report
success or hide cleanup failure. Pure seam tests and strict types accompany code.
Root independently inspects and executes pure proof first. Actual connections,
DDL/DML, metadata registration or cleanup require a separate root action admission
after source review; this question currently admits source preparation only.

Root owns this question, Order460 and existing review/checklist/coordination paths.
No public product change, Git/CI action, live migration/recovery, app restart or
provider operation. Existing Q276 pins need no change for this isolated proof.

## Independent root source acceptance and exact native action

Root fully read the final native/isolation sources and personally executes the
three pure Q276/Q279 files:10pass/0fail/91 assertions. Explicit ignored-file strict
TypeScript passes with --ignoreConfig (initial invocation lacked that required
flag and failed before compilation). Independent Astra also executed isolation
2/0/27 and strict types; its three settlement/cleanup findings were repaired.
Final native hash db6d8beaccde0899d97bfc0be796753c7b1f69aba430b6df4085dacaf1154f1c;
adapter c5b0c57bb5c2ec4cf6b619bcf9327c6da45d59ff4efc2f5c7baa3c7fdddd04c5;
pure test dfba20288830473ffa76d28bb701ae23308dabb4f6f02c59cf6c75db5f6ce782.
Production registration remains6fe1df20032cdcac4f07c23439f2047ad926e9e0c321eb9bc42e1d782dad0e20.

Root, who did not implement these files, now admits one opt-in execution of only
market92-registration-native.test.ts against the already-existing Q265 target.
Use the previously reviewed protected authority wrapper, validate its exact source
URL/role/host/port before deriving the dedicated target; secrets stay in the child
environment. Require the above file hashes, current native binary/cluster identity
and adequate reserve (fresh C5.20/D21.81/E7.72GiB; listeners3000=17936/55503=13580).
Test preflight must verify160015/data directory/actor/target and absent exact UUID
schema before one atomic schema/empty-copy transaction. Only that owned schema
may receive synthetic rows and exact RESTRICT cleanup. No public DML, new database,
roles/grants, provisioning, hotel fixtures, app restart or provider action.

Use a bounded100s exact-child wrapper around the existing30s setup/test/cleanup
limits, capture only bounded proof output, and restore every opt-in environment
value afterward. A failure stops execution; inspect exact residual identity
read-only before any further separately admitted action. No automatic retry.
Successful output must include real rollback/conflict/observed advisory contention,
exact added/existing rows and post-cleanup public/global fingerprint equality.
This proves the documented isolation seam, not fixed-public live registration.

## Executed result

Root personally executed the admitted frozen native source through the protected
wrapper:1pass/0fail/16 assertions,520ms. Independent Astra accepted the targeted
final corrections at the same hash. The real server observed the blocking advisory
lock before release; intentional rollback, conflict rollback, one added/one existing
and exact clone contents passed. afterAll exact schema cleanup and final fingerprint
passed. Separate bounded read-only check reports remaining_proof_schemas=0.
No public metadata, hotel rows, role/grant, database inventory or serving process
was changed. Only test-owned schema/empty tables/synthetic metadata were removed;
they are disposable proof data, not recoverable hotel records.

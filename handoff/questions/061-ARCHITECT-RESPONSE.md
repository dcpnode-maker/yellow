# Architect response — Question 061

**CLOSED — YES.** Apply only the proposed proof correction. Server-computed expiry must
be visible to an operator deciding whether a hold is still useful; visibility is not
client authority. Continue to reject any submitted `expiresAt` field and every browser
`ttlSeconds` reference. Change no product code, recreate the database, and restart all
seven proofs. Answered by OpenAI Codex as temporary architect under D-95/D-115; this is
not independent review.

# Q233 — Complete credit-document native proof admission

## RESOLVED — bounded independent proof for active Order449

Root may prepare, inspect and then execute one frozen signed-session HTTP proof
of complete immutable native-credit document retrieval. Only Order449's existing
actual HTTP test and its bounded two synthetic credit-fixture cohorts are admitted.
Retain all previous Order448, Q229, Q231 and Q232 results and database rows.
No new server, database, container, role, permission dictionary, schema, migration,
fixture cleanup, app restart/promotion, main merge or external provider call.

Reuse the existing native PostgreSQL16.15 server on127.0.0.1:55503, exact cluster
D:\Yellow\temp\order434-production-cluster-20260906 and target
yellow_order446_referee87_20260907 at canonical88. Its current accepted baseline
has1,574 public rows,5 tenants,21 permissions,129 base tables,119RLS/119policies,
28FORCE and2views. Pin a new read-only preflight, don't reuse the older1,375-row
Order448 baseline. The full ledger/source hash of0088 remains
214754e94bdfb0a2163395c9ab4449b0b5e87da7830c45e69d77ac05a2cddb64; projector
b34eaf0095dad0df5cd55453b7e4bd1a42f5ae698a02c5ebca3dcac7645c9f96.

The candidate87, clean88, pristine77 template, every outside database/role/catalogue,
all prior1,574 row hashes, current functions/ACLs/configuration/ledger and live
a1085178/frontier85 app process/ports must be preserved. No prior actor is revoked:
authority-denial tests may affect only their newly created synthetic actor. New
tenant-scoped rows and role grants must belong exclusively to exactly two fresh
default credit fixture tenants. Six new global tx_code definitions are permitted
only as the exact fixture ROOM/CGST/SGST triplet for each new tenant/property,
with every complete dictionary-row hash, account route and scalar reference
verified by Q229's already inspected read-only ownership checks. No blanket global
dictionary exception and no package/payment reference is allowed. All other global
tables remain identical. Preserve every prior row, including all older test data.

Private helpers may be added only under .yellow/evidence/order449. Reuse the frozen
Order447 canonical read-only snapshot and AST-selected protected credential/host
functions; never execute legacy startup/promotion preparation wholesale. Derive
credentials privately from the retained approved environment, never print them.
Before any database test, root reads the complete helper and exact test body,
freezes seven functional source files plus all proof/supporting inputs, runs only
read-only Preflight, and supplies that exact path/SHA for explicit Execute.
After execution, capture and verify all containment before accepting the result;
a failed test or containment stays failed until a separately diagnosed action.

The actual test must prove full issued bytes/hash/lineage, concurrent reads,
tenant/property/missing/noncredit concealment, scope/current authority denial and
unchanged financial/fiscal records caused by reads. Unit/mocked tests and a passed
wrapper alone are not a native database proof. This admission changes no fiscal
business policy or Order449 application scope.

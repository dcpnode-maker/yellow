# Question192 — Order434 preparation integration scope

Status: answered by the implementation coordinator under existing routine
technical-decision authority; no new fiscal/business policy or runtime grant.

Complete preparation needs two dedicated test files:
`tests/india-native-fiscal-invoice-issuance.test.ts` and
`tests/india-native-fiscal-preparation.integration.test.ts`. The coordinator's
initial day/series assignment named the first before noticing it was absent
from the exact order list. Further work was held for this explicit amendment.
No existing proof or source is removed or weakened.

D1359 assigns named lock helpers within the already admitted fragments; D1360
defines the acyclic internal preparation source-basis preimage. These integrate
the same Order434 outcome, not separate completion boundaries, tables or public
APIs. All independent high-risk executable acceptance obligations remain.

Continuation admission: the remaining source-lock worker needs
`tests/india-native-fiscal-source-locks.integration.test.ts`; the final-commit
worker needs `handoff/drafts/order434/0076-native-completion.sql` and
`tests/india-native-fiscal-completion.integration.test.ts`. The coordinator
explicitly admits these paths before implementation, solely to isolate parallel
edits within the existing Order434 prepare/accounting/commit/replay outcome.
No new table, policy, public API, runtime grant or migration-runner entry is
authorized. Root owns outer preparation and integration; completion and source
locking remain subordinate functions, not independently completed orders.

Integrated candidate proof: after all real preparation/accounting/completion
functions compile in the existing synthetic PostgreSQL cluster, the coordinator
may temporarily grant `app_role` execution on exactly the intended four entry
points: `prepare_india_native_fiscal_invoice_v2`,
`consume_india_native_fiscal_accounting_event`,
`read_india_native_accounting_source_closure`, and
`commit_india_native_fiscal_invoice_v2`. Execute the actual runtime command and
revoke these candidate grants after the bounded proof, including failure. No
owner wrapper, impersonation shortcut, fabricated derived evidence, disabled
constraint, production-runner entry or retained-local grant is admitted. This
is executable integration testing of the real candidate, not release authority;
independent high-risk acceptance and migration promotion remain separate.

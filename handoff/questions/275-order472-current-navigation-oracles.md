# Q275 — current market navigation/catalogue compatibility

Order460 root technical scope,13 September2026. Published da744f1f, exact
CI34759204343 failed quality; Windows and local-review passed, downstream jobs
skipped. Five recorded failures count the previous15 destinations/14 ordinary
tabs/10 extension types; another recorded assertion expects the old route text.
No unchanged rerun or claim of fullCI acceptance.

Admit ONLY tests-first current-contract alignment in these ten test files:

- tests/operator-workspace-layout.browser.test.ts
- tests/operator-management-demo-navigation-finetune.intentional-red.test.ts
- tests/operator-layout-composition.test.ts
- tests/operator-workspace-skins.test.ts
- tests/operator-room-outages-label-alignment.intentional-red.test.ts
- tests/operator-room-outages-destination-copy.intentional-red.test.ts
- tests/rate-publication.integration.test.ts
- tests/operator-adaptive-experience.test.ts
- tests/rate-targeting.integration.test.ts
- tests/operator-flagship-motion.test.ts

The last two were found by a targeted scan of the same obsolete assertions:
adaptive ordinary tabs still expect14, and rate targeting still expects10 launch
types. The latter assertion is inside an existing DB-gated test: inspect and
align only its exact catalogue expectation; do not claim local DB execution.
The bounded follow-up scan additionally finds flagship motion's exact-class
domain-icon count12 ->13 due only to Market; preserve its15 distinct symbols.

Root owns this question, Order460, Review460, current-status/checklist,
DECISIONS.log and LEDGER plus bounded private Q275 proof/publication receipts.
Before edits independently reproduce relevant failures. Match exact added market
destination/icon/group/schema and current property-bound route behavior; preserve
every previous destination, claim, permission guard, draft/keyboard/accessibility
assertion and financial test body. No loose greater-than count, derived-from-
implementation expected array, removed assertion or wide snapshot reset.
Retain exact schema identities/uniqueness rather than only changing a count.
No product code, UI design, migrations, environment selectors, timeouts, retries,
DB/private credentials/runtime/setup/state/Docker/WSL action is admitted.
Use focused existing native-safe tests; skip actual DB bodies only under their
existing explicit environment gate, never assert they ran.

Disjoint worker scopes will be assigned explicitly by root. Root personally
inspects/reexecutes combined source/browser proof, then publishes a minimal
successor preserving the six outside staged files and all working residuals.
The Q274 consumed publisher remains historical, not replayable. Native whole-file
selective publication is sufficient for this test-only successor; no new generic
publisher framework. All remaining scopes/phase priorities stay unchanged.

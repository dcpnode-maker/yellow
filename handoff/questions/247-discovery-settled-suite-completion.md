# Q247 — Finish native discovery acceptance after isolated matcher proof

The root isolated successor returned all seven expected errors with complete
snapshot equality (2pass/63 assertions/5.76s). This proves the test can await the
real database operation before asserting it; it does not retroactively pass the
original nine-test suite or prove all its other cases. Production remains frozen.

Root technical resolution under the standing implementation directive:

1. After independent isolated acceptance, amend only rejection assertions in
   `tests/india-native-fiscal-series-discovery.integration.test.ts`: await the
   actual operation to a tagged result, then assert expected class and canonical
   message; retain every case, raw-row assertion, snapshot and timeout. Add the
   same success/wrong-type/wrong-message controls as a single pure test.
2. Prepare a fresh full-suite runner and capture/audit successor in private454
   evidence. Preserve original failed runner/test/log evidence. Preparation may
   use the reviewed complete snapshot/ownership code, with the numeric outbox sort
   correction already proved; it must not compare the populated target to the old
   pristine1264-row assumption.
3. A separately frozen execution admission may authorize exactly two NEW synthetic
   discovery cohorts in the SAME existing referee90 database, not a new database
   or app-data copy. Snapshot and preserve every pre-existing row (including the
   first failed-run cohorts), catalogue and companion/global state. Six new owned
   transaction codes and the existing fixture's legitimate series/one invoice
   footprint only; temporary authority/timezone challenges must restore exactly.
   No migration, seed, cleanup, provider, live runtime or automatic retry.
4. Signed HTTP acceptance uses the existing two retained cohorts and read-only
   requests. Prepare its exact pinned manifest and native runner; execute only
   after root and independent review. No live listener is needed (`app.handle`).

This explicitly records the additional test scope. It does not grant execution
before source freeze, fresh review/preflight receipts and exact root admission.

# Q248 — Complete accepted source release without unfinished mixed hunks

Order460's read-only inventory finds accepted Q211 presentation dependencies and
Orders454/455/458/459 mixed with preserved, still-paused Order445 guest-picker
work. An ordinary all-files commit would publish unrelated work; an old85 UI
backport would omit completed backend development. Neither is the release target.

Root resolution: prepare an explicit current90 candidate rooted at87da26f3. Include
the accepted self-hosted font/icons/licenses and eight interfaces/grouped layouts,
discovery integration and repaired tests. Project only exact paused445 fragments
out of app.ts, operator.ts, operator.css and operator.js; preserve original working
bytes/index semantics. Private pure projectors may be prepared/tested now, but no
candidate/index/ref/publication or runtime action is authorized by preparation.

The embedded `src/project-status.ts` still records Order444, even though canonical
docs record the later source. Before release it must be brought up to date with
explicit distinctions between source-built, independently proved, CI and actual
runtime identity. This needs a separately enumerated source/test scope after the
current455 executable closure is released; do not edit frozen source during its
DB proof or mark unreviewed phases complete. Preserve all older recorded work.

The missing454 GET contract is now documented from inspected implementation and
actual native proofs. The planned458 standalone browser proof was consolidated
in its already-scoped workspace browser file; the order now states that fact.
No missing file/test result is invented. Exact repaired-source CI will run its
existing80→81 upgrade lane17/0, avoiding another local upgrade target.

After actual455 acceptance, root and independent status checks pass. The four-file
mixed projector is independently accepted9/0(51), with no additional missing
runtime/test imports in the provisional inventory. Root explicitly adds current
September9 sections to the design handoff/capability inventory/Order444 so their
older one-theme or paused-design text cannot masquerade as current instructions.
Candidate projection preserves published history and excludes unrelated unpublished
prototype/picker sections. Exact66-path private source preparation is enumerated
in Order460; binary font bytes must never be decoded/re-encoded as text. The
prototype README stays local because its gallery source is not in this release.
This resolution is source preparation, not Git publication or runtime admission.

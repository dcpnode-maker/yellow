# Q274 — exact accepted Order472 publication

Order460 / root technical scope / 13 September2026. Source preparation only.

Q273 manifest8b159b18 was actually prepared, checked and restored: root160pass,
20explicit environment skips,0fail2213 with three real browser journeys, required
Windows9pass/4DBskips/0fail48, strict types202 and23-package licence proof.
Current HEAD is63338ed312f925bddd951665189196e3615a51b9 on
phase-7/operator-invoice-workflow, draftPR92. Preserve that tested product content.
Only subsequent root governance receipts have changed since its source freeze.

## Why ordinary whole-file publication does not fit

Q263 native `commit --only` reads working bytes and would include paused445 in
four shared files plus unaccepted capability prose. An ordinary commit would
include six unrelated existing staged paths. Do not overwrite working files or
silently change those staged entries. The old receiving publisher is stale and
its consumed private artifact/index/object promotion must not be replayed.

## Exact bounded ownership

Builder /root/astra_ultra_handoff owns only these private inert source/test files:

- .yellow/evidence/order460/order472-selective-publication.ts
- .yellow/evidence/order460/order472-selective-publication.test.ts

Root owns this question, Order460, Review460, current-status/release checklist,
DECISIONS.log, LEDGER and private publication manifest/action/receipt files under
`.yellow/evidence/order460/order472-publication-20260913` (no artifact until
separate root admission). Do not alter Q271/Q273 helpers or consumed receipts.

Reuse the native Git commands and read-only git-index-parser; no custom binary
index writer, duplicate object store, source checkout or deployment framework.
The smallest explicit operation may use one new native temporary index:
`read-tree HEAD`, exact `hash-object`/`update-index` inputs, `write-tree`,
`commit-tree`, checked compare-and-swap of the existing branch, and native scoped
index synchronization to that commit. These are newly admitted publication
preparation concepts, not authority to execute against Yellow yet. Normal native
Git creates only the standard content-addressed objects required by the commit.
Reject active custom hooks/signing that this route would otherwise bypass.

Use Q273's114 exact overlay paths plus this Q274 question. The five projected
outputs must be the accepted Q271/capability bytes, not whole working inputs.
All product/test/config inputs must retain their Q273 pins. Only these exact
subsequent root records may receive newer frozen bytes: docs/PROJECT-STATUS.md,
DECISIONS.log, handoff/LEDGER.md, handoff/orders/460-current-source-single-local-promotion.md,
handoff/reviews/460-current-source-single-local-promotion.md and
handoff/reviews/460-release-checklist-20260913.md, plus this new question.
Verify raw candidate bytes equal native Git filtered/stored bytes; do not silently
normalize a pinned source. Unchanged published files remain at HEAD633.

### Explicit single-file Git line-ending projection

Root read-only inspection found exactly one non-identical filtered candidate:
`tests/build-readiness.test.ts`. Its Q273 working bytes are20254 bytes,
SHA256 `afdf59eb83b8b7c0a3df7ff9f0826244c6491e0e8862f4df5514f84b12bc4def`,
Git blob `26292202aac4affda542fa68a7b96a1bdf4465b2`. There are82 CRLF and no
lone CR. Replacing only those82 CRLF with LF yields20172 bytes,
SHA256 `2b62b915b7d71d91ea274ab5f5babeb774fac9a5eb39e8837098834e4cba9f7c`,
Git blob `cd47d69d106d56c523bd9c5e88dd72c115631f82`, exactly matching native
Git filtering under the existing `text=auto eol=lf` attributes. Admit only this
explicit publication projection after verifying both exact pins and byte counts.
The original working file is preserved unchanged. Every other candidate still
requires raw/filtered equality, including the two vendored CRLF exceptions.
Independent proof must verify equal transpiled test code and run the focused
existing test. No assertions, imports, fixtures or product logic may change.

Before any action capture full tracked/selected working bytes, exact source/index/
branch/origin/refs, scoped preexisting index entries and complete outside semantic
entries/modes/blobs/persistent flags/stable extensions. Reject preexisting scoped
staging or conflicts; all six outside staged entries must survive. Verify every
final commit blob, exact115-path allowlist/no extras, parent and full working/index
preservation. Only root may subsequently admit one ordinary non-force push to the
same branch after successful local verification and fresh remote identity check.
No own merge, force push, reset, stash, blanket staging or change to main.

## Executable proof and failure boundary

Builder tests may use only small synthetic native-Git repositories in exclusively
created temporary directories, never Yellow refs/index/workfiles or app data.
Prove disjoint outside staged changes, mixed-file residuals, new paths, exact byte
projection, wrong-head/index/input refusal and changed-path allowlist. Verify any
cleanup target is the test-owned resolved directory before bounded removal.
No WSL/Docker, live DB/runtime, new dependencies or network in builder proof.

Importing the helper is inert. Root independently reads and runs the proof, freezes
the actual manifest and separately admits the one publication action. On failure
retain partial local evidence and stop; no automatic retry, rollback or push.
Fresh remote CI/full canonical database/referee remains mandatory before release;
draft publication itself does not assert reviewability, deployment or phase exit.

# Question 122 — Order 071 model-fixture union

## ANSWERED — TEMPORARY ARCHITECT

**Order:** 071  
**Date:** 2026-08-22

The restarted compiler passed production and stopped only in the expanded all-model proof. The
minimum fixed fixture inferred `package` and `rmsBinding` as permanently null and its component
array as `never[]`, so valid package, RMS and expert variants cannot be assigned in the test.

May the mutable test builder receive one explicit transport-fixture type broad enough for the ten
pre-registered model variants while production inputs remain `unknown` and strictly normalized?


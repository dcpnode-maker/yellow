# Q240 — Combined functional checkpoint after scoped repairs

Admitted by root2026-09-08. Founder direction remains functional build only.
No UI/UX, guest-picker, prototype or animation change is included or authorized.

Orders449/450 native proofs and451 independent correctness, exact old/new parity,
performance and deliberate mutation proofs pass. Q239 preserves all original
browser assertions/deadlines and root focused5/0 passes. The original three failed
449 standing runs remain immutable and are not accepted as green. Parentffb03441
remains published with six CI jobs green. This admission is not publication.

Prepare one new private candidate from HEADffb03441, not the dirty real index or
the superseded f2ada candidate. Include only:

-449/450 complete-credit and paginated-list service/command/index/HTTP/app/test
  paths already named by their orders, plus the single mandatory450 test command
  in the existing CI fresh88 block. Preserve the pre-existing446 invocation and
  correct its accidentally shifted whitespace; no CI step is removed.
-451 two pricing files, new reuse test and append-only preview test.
-Q239 only tests/operator-appearance-geometry.test.ts lifecycle repair.
-Whole backend handoff orders448–451, reviews448–451 and questions228–240;
  include only existing files, never invent a proof or include paused visual work.
-Surgical current metadata/Current task section of docs/PROJECT-STATUS.md, leaving
  the historical tail from the published base untouched.
-Surgical backend contract sections448–450 and their current status, leaving all
  other published text untouched; no broad working CONTRACTS replacement.
-Append-only backend D1430–D1435 rows in DECISIONS.log and handoff/LEDGER.md;
  do not import unrelated historical/UI rows from the working copy.

APP and OPERATOR contain paused guest-picker edits: reconstruct only449/450
imports/methods/routes against published base AND original staged versions.
Every candidate hunk must be attributable to these admitted changes. Whole-file
source inclusion requires inspecting that no unrelated staged/working hunks exist.
Do not include public assets, operator HTML/CSS/JS, design guides or UI manifests.

Use private candidate/projected indexes and generated evidence, preserving exact
real index bytes/entries/flags, every outside staged entry and all working files.
No real Git index/ref/push operation is granted during preparation/validation.
Inspect hashes, candidate diff and projected mixed-file preservation before tests.

Root451 final bounded-code comparison is complete (receipt9b417faa; the earlier
e5f687d7 remains retained), so the one existing .git-free
validation artifact may now be updated from its authenticated f2ada inventory to
the newly reviewed candidate. Retain old manifests, Git blobs, benchmark receipts
and full outputs; do not alter completed evidence. Replace only exact admitted
artifact paths, with validated resolved paths/no reparse ancestors and atomic
file writes; no new worktree, dependency copy or additional full artifact is needed.

Root must inspect and separately invoke the validation helper. Run the normal full
bun test suite with unchanged deadlines/selection, typecheck, import boundaries,
license gate and diff checks on the exact candidate. Preserve Git-history access
as read-only context for tests requiring it, without copying or mutating metadata.
Report environment/database skips separately. Retain all failures and do not loop
unchanged retries without a fresh explanation/admission. No concurrent heavy proof.

Only a subsequent reviewed, exact passing-candidate publication operation may
commit/push to the existing draft PR92. Never merge own PR, promote the live app,
activate a provider, start another server/database or claim Phase7 complete.

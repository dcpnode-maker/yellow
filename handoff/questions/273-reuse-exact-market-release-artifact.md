# Q273 — reuse the existing exact validation artifact

Status: bounded source preparation admitted by root,13 September2026; Order460.
This is a coherent release proof, not a new product order or new local stack.

Astra read-only audit finds existing artifact
D:\Yellow\temp\order447-functional-validation-20260908 contains exactly2120
published87da26f354d08b545bc6b9127031cc29b38881ef files/28,742,174bytes,
treef587df27f1fe7aad046aefea8577632609713b2e. Aggregate SHA256
d7aed8b4a43006103b3e56cfd8fd214b6fd10ec026efa1b0a8c220b69e31ab42
matches its earlier standing receipt. Preserve its five preexisting empty
directories and the sole node_modules junction to the active dependency tree.
HEAD633 has2242 files and no deletions versus this baseline. Current app17936/
parent17096 uses a different immutable D:\Yellow\runtime source; no Bun process
references the validation artifact at this audit. Revalidate before any action.

## Exact source-only ownership

Builder /root/astra_ultra_handoff owns only new private files:

- .yellow/evidence/order460/order472-candidate-artifact.ts
- .yellow/evidence/order460/order472-candidate-artifact.test.ts

Root owns this question, Order460, Review460/release checklist and a future exact
frozen manifest .yellow/evidence/order460/order472-candidate-manifest.json.
Do not create that manifest or mutate the artifact during builder preparation.

Root may also create the private action/receipt record
`.yellow/evidence/order460/order472-candidate-actions.md` after independent
helper proof. This record is outside the candidate source inventory so recording
the exact manifest hash and separate Prepare/Restore admissions cannot invalidate
the frozen source fingerprint. It authorizes nothing until the exact action is
recorded. It must enumerate the fixed artifact, changed paths from the manifest,
their `.order472-owner`, `.order472-next`, `.order472-previous` sidecars and the
single `.order472-directories.order472-owner` marker; no unknown cleanup target.

Implement the smallest inert helper/test needed to prove and prepare an exact
HEAD63338ed312f925bddd951665189196e3615a51b9-plus-accepted-delta candidate in the
existing artifact. No private Git index, object, ref, checkout, archive copy,
dependency installation, WSL/Docker, generalized deployment framework or import
side effects. Read only immutable Git objects with native Git and bounded child
execution. Existing old runner is reference only and must not be executed.

Candidate content is the published HEAD base, accepted472 runtime/tests/assets,
Q270 five-file repair, Q271 two vendor attributes/test correction and four pinned
mixed-file outputs, and the exact independently accepted Q272 source/status/CI
files. Root's narrow receiving/governance records are separately listed and pinned
in the frozen manifest, never selected by broad glob or Git dirty status.

One additional explicitly admitted pure projection is required inside the helper:
docs/design/BUILT-CAPABILITY-MANIFEST.md receives only the complete59-line
Order472/Q268-Q267-Q266 addendum before its9September heading, prepended to the
exact HEAD633 document. Pin full working/base/projected bytes; retain all later
HEAD content. Keep ASTRA-IMPLEMENTATION-HANDOFF.md and all other unaccepted445,
471/prototype/staged work entirely at published HEAD. Nothing is deleted from
the working checkout. Include Q273 itself only when root freezes its exact bytes.

Validate full source/target identities, unique normalized relative paths, no
symlinks/reparse traversal (except the unchanged sole dependency junction), exact
base files, no unexpected target files, sufficient existing resource headroom,
and no process referring to the artifact. All candidate inputs must match frozen
manifest hashes before any write. Preparation replaces/adds exact candidate files
only; no artifact deletion. On mismatch stop, preserve evidence, do not retry.

Provide an explicit reversible restoration plan: baseline bytes are already in
immutable Git; restore only owned replacements and remove only owned newly added
files still matching their candidate hash. Preserve baseline empty directories,
the dependency junction, unrelated data and all working/index state. No automatic
cleanup of unknown files. Tests must prove real byte transformation, preservation,
unknown-path/drift rejection and partial-progress restoration boundaries using
synthetic/in-memory fixtures; no real artifact write in builder tests.

## Required next independent step

Root must read both helper files completely, personally execute proof, inspect
the exact frozen manifest and revalidate actual artifact/runtime identities before
separately recording one Prepare authorization. Then execute selected real market
browser/API/source checks against the materialized candidate, strict types,
boundaries/licences and safe status tests. Never run the unrestricted full suite,
state/setup scripts or local Docker. Record actual results and restore the exact
baseline via the independently proved explicit restoration action. This scope
admits only helper source/tests now; no runtime, DB, Git publication or filesystem
materialization follows until the separate root action record.

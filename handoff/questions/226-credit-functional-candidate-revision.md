# Q226 — Corrected functional candidate and standing evidence

**Status:** RESOLVED bounded Order447 verification/publication scope. 2026-09-08.

Q225's independently diagnosed bigint assertion changes exactly one of the32
functional candidate paths: tests/india-native-credit-submission-recovery.integration.test.ts
from1243d900062b063b3b8c2696b82576a1f643ab8c12bb56bc1a6a4ee3082c885b
to95487aaef107d62c77243f61b4e3a81fc07e19a3f2482efce12511cdda84cdcc.
No production function, signature, child process or database behavior changes.
The final independent corrected recovery is5/0(156); earlier4/1 is retained.

Preserve original candidate manifest a39c937b051f0016775a3d32b5bab780e91f12d1eaea4f79d10214706da5811c,
tree a57ba6c518a287e047a199b09c670b589dc0eb45, archive and all failed logs. Admit
one new candidate-v2 evidence directory under .yellow/evidence/order447, its
private index/manifest/real-index backup, and exact new archive
D:\Yellow\temp\order447-functional-source-20260908-recovery-fix.tar, only if absent.
Reuse the ONE existing owned source-verification artifact
D:\Yellow\temp\order447-functional-validation-20260908; do not create a worktree,
dependency copy, app or database. Verify its complete old2065-blob inventory and
existing validated dependency junction before mechanically replacing ONLY that
test's old bytes with the new Git blob. Reverify every final blob and every one of
the32 functional paths against base850c78c08424f83483ba88908baf08cf9eed5d75.
The other31 source hashes and all unrelated staged entries/flags must remain exact.
Any unexpected file/source/index change stops preparation, not a silent adoption.

The first full unchanged-tree standing run recorded1910passed/1404 explicit
environment skips/1 status-report process deadline failure,34142 assertions.
An unchanged focused repeat also hit the4.5-second process-lifecycle deadline;
direct read-only native status subsequently completed in2706ms with Docker CLI
excluded from that child PATH, and the unchanged focused test passed1/0(25) in
3269ms. This establishes timing sensitivity, not a backend semantic failure or
permission to loosen test deadlines. Retain all results; do not count the original
full suite as green. Run the corrected exact tree in an explicit isolated native
verification environment with no database flags or Docker/WSL startup, then obtain
separate real-CI database evidence. Skips are never counted as DB proof.

Private standing-runner preparation also stopped before tests when raw index
cache bytes changed, then before tests on Windows PATH casing/Git resolution.
Neither writes source or real Git index. Read-only binary classification proves
all2065 entries, stat fields, modes, blobs, stages and flags exact; only optional
UNTR/FSMN cache-extension bytes differed. Keep original backups/manifests immutable.
For publication, explicitly pin the current raw index in the new candidate or a
separate root-reviewed narrow cache-rebaseline receipt. Require exact whole-index
hash plus full outside-entry/flag equality again under an owned exclusive lock.
Never overwrite the index merely to make an old hash pass.

The private refresh/publication/standing helpers and this question are admitted
coordination scope. Exact source publication requires root inspection of frozen
helper/candidate/validation evidence and a compare-and-swap ref/index handoff.
No paused UI hunk, private evidence, credential, automatic merge, push to main,
local promotion or claimed provider certification belongs to this action.

## Corrected execution checkpoint

Candidate-v2 manifest6edae4b8e4a803e31a3b8e8b7ac055e6769946689f87990eda82e1acc64d110e
binds tree849c86e550843a8c71ccfe41389180ead7d9b412. Every2065 extracted blob and
all32 working-source inputs remain exact. The corrected full suite passes1911/0,
1404 explicit environment skips,34153 assertions across537 files in166.13seconds;
log1bb6e8fe7f00a5c71a124ed9841d4139a1ec0cd3ada46abe6141952101952974.
The wrapper then stopped on a Bun synchronous read-only Git subprocess with null
exit code and empty stderr. This was after the successful test result was saved.
No test, source or index was repaired or rerun. Root's separately inspected
remaining-only helper uses asynchronously drained Git output, verifies unchanged
real index entries and all source/artifact hashes, and executes only the three
unrun gates: typecheck,187-file boundaries and licence policy all exit0. The licence
scan reports0 installed packages; it is not claimed as a dependency-content audit.
Final standing receipt SHA256eef4aebb44a9fcf55b2c64f9e403bd9bfe5dab1763a972349d0923093b00ecd7.

Publication preparation then stopped before index/ref mutation because the current
raw index cache bytes differed from the v2 capture. No broad index refresh is
admitted. The existing exact-entry/stat/flags/TREE classifier and a separately
pinned root receipt remain necessary if only optional cache extensions changed.

Independent comparison proves the current index differs only in UNTR/FSMN cache
extensions; every entry/stat/stage/flag and TREE byte remains exact. The separately
pinned root receipt admits raw a7e3d7d69bf704c9f96bc5cfc78929dabd4b1019ff585e4fa2d4d4e69174eef7.
Preparation next stopped, before any real-index/ref write, on an incorrectly reused
outside-path count of2054. Exact native Git classification shows2065 original
entries,23 existing scoped paths and9 new scoped paths:2042 outside entries must
remain exact. Correct only the private assertion and three report values to2042;
the full outside-entry/flag equality checks are unchanged. Retain the stopped
private preparation. Do not call it a source or accounting defect.

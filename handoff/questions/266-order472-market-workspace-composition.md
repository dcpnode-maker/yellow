# Q266 — Order472 startup composition and manual evidence workspace

Status: RESOLVED by primary implementation owner before code, 13 September 2026.

## Gap and resolution

D1489 proves the managed service/API but server startup never composes it and no
operator screen consumes it. Admit the exact files below in Order472. This is
ordinary implementation of the received journey, not permission to change the
running app, automatically grant roles, activate suppliers or redesign themes.

Reuse the existing shell, explicit property context and managed command API.
Add one lazy Market evidence workspace in Rates & inventory: select a regional
snapshot, inspect/filter its source-point map and paginated table, explicitly choose
the own-property record and comparators, review and confirm. Preserve source dates,
identifiers, coverage/completeness and unknown operating status. A coordinate map
without streets is labeled as such; it is not street imagery or physical-identity
verification. No external map provider, dependency, rate shopping or auto-pricing.
The complete Order472 URL/ID suggestions and planner linkage remain required,
not silently removed by this manual workspace tranche.

The existing shell requires availability-property access; this tranche does not
broaden shared availability/property authorization helpers or grant it to market
roles. Explicit market permissions are independently required by the command.
Market-only property discovery remains a tracked integration gap before claiming
the complete role-aware journey. Show truthful missing-scope/unconfigured states.

## Exact source ownership

- /root/astra_ultra_handoff: src/server.ts; new
  src/runtime/market-workbench.ts; src/kernel/build-info.ts (frontier91→92 only);
  tests/build-readiness.test.ts (current frontier assertions only);
  new tests/server-market-runtime.test.ts and
  tests/market-runtime-readiness.integration.test.ts.
- /root/q258_runtime_cutover: src/app.ts (market deep-link and asset only);
  src/http/operator.ts (market static-asset member only);
  new tests/operator-market-assets.test.ts.
- /root/q258_source_adapter: src/http/operator/index.html,
  src/http/operator/operator.js (navigation/lazy lifecycle hooks only),
  new src/http/operator/market.js; src/http/operator/operator.css
  (market-prefixed responsive controls/plot/table only);
  new tests/operator-market.test.ts.
- Root: new tests/operator-market.browser.test.ts, this question, existing
  Order472 receiving/review/status/decision/ledger files, docs/CONTRACTS.md,
  docs/design/BUILT-CAPABILITY-MANIFEST.md.

No worker edits another owner's files. Existing dirty/frozen work remains intact.
No new migration, schema, seed, permission or package change is admitted.

## Runtime and proof boundaries

YELLOW_MARKET_WORKBENCH=1 requires the normal operator workbench and loads both
accepted native artifacts once before any pools/workers/listener. Flag-off does no
catalog read and mounts no market API. Invalid or unavailable enabled catalog fails
startup with a static non-secret error; no empty or partial substitute.

Compose the managed service with the existing registry/events/idempotency. The
market-enabled readiness extension separately verifies its narrow function owner,
search_path and ACL, exact schema type and required permission catalogue; it must
not grant or repair these. Keep this domain-dependent readiness in the runtime
module, not a kernel import of distribution internals. Existing readiness checks
remain unchanged. Retain the server16KiB global body cap and display/enforce it
before confirm; candidate500 is not a promise that500 serialized references fit.

Tests accompany code. Root non-implementer personally executes readiness/HTTP and
browser proof. Integration uses only the exact retained Q265 synthetic target,
explicit opt-in and all three dedicated URLs. Hostile readiness variations must
be rolled back inside test-owned transactions; no persistent privilege changes,
provisioning, destructive cleanup or edits to live/global authority.
Browser tests use synthetic data on a temporary loopback port, never port3000.
No source publication, local migration/cutover or phase closure follows from this
source tranche. Exact-source release gates remain required.

## Executed checkpoint — D1490

Root independently completed136pass/0fail/1311 across19 related files, including
15 actual PostgreSQL cases and9 native filesystem cases, plus strict types and202
import boundaries. The real-shell browser journey passed38 assertions using only
synthetic HTTP. Review472 records exact hashes, failures, corrected test-client
settlement, unchanged deadlines, rolled-back hostile metadata and retained target.
The sole3000 app remains41415/frontier91; no publication or promotion was performed.

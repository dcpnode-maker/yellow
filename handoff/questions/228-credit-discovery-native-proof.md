# Q228 — Existing canonical88 credit-discovery proof

**Status:** RESOLVED bounded preparation; execution still needs frozen root handoff.

Order448's seven typed/API/test paths are implemented and pure tests pass28/0
with3 explicit database skips. Root independently read the query, existing0087
read-authority function, actual signed-session test and credit fixture. The query
always calls the authoritative non-strict function, including a NULL binding.

Prepare a private, explicit preflight/execute helper under .yellow/evidence/order448.
Reuse only the existing native PostgreSQL16.15 server at port55503 and protected
Order442 deploy/runtime credential loader. No credentials appear in source/output.
Intended proof target: yellow_order446_referee87_20260907, whose actual ledger is
88 (the historical name is not its frontier). Confirm current88/b34eaf0095...
projector,129tables/119RLS/119policies/28forced/2views and exact existing adjustment
dictionary prerequisites before admission. Existing clean88, draft candidate87,
pristine77 template, outside database/role state and live app stay untouched.

Use existing read-only canonical snapshot logic to capture target identity,
complete migration ledger/function/ACL/config/catalogue, every existing row,
companion/template/outside roles/process identities and zero sessions. Pin all
seven448 source hashes, fixtures, current migration/schema, runner and helper.
Never execute an older Prepare/Promote/Rollback/native-draft mode.

Execution is ONLY the actual named signed-session HTTP test in
tests/operator-fiscal-credit-note.integration.test.ts, with its explicit mandatory
Order446 deploy/runtime URL flags pointed at this ONE target. It creates two new
synthetic tenant cohorts through existing fixtures/commands, issues one genuine
credit, performs read-only discovery and revokes only the freshly created actor's
roles to prove authorization. No previous row may be removed or changed. No reset,
cleanup, bootstrap, new database/role/permission or migration is authorized.
Assert prerequisite permission rows already exist; fixture ON CONFLICT must not
silently add or adopt divergent dictionary values.

After success OR failure, close pools and compare all prior target rows as exact
multisets, complete target metadata and every companion/outside/process state.
Only new synthetic tenant-cohort rows may be added; all existing rows survive.
Bound process runtime/output, reap only owned test children, and preserve logs.
Do not retry a failed execution without diagnosis, fresh snapshot and new handoff.

Root did not implement the448 backend or test and may personally execute this
independent high-risk proof. It may run against the already-proven native88 while
the unrelated Q227 Linux status-harness repair proceeds; this does not waive
exact-source CI before final acceptance/publication. The order's earlier sequencing
is narrowed explicitly to allow that safe parallel verification.

No UI, app restart/promotion, provider calls/activation, journal mutation by the
discovery operation, main merge or whole-phase completion is claimed.

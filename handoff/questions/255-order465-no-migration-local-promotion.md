# Q255 — Order465 no-migration local promotion preparation

**Status:** completed local promotion and independently verified, 2026-09-13.
The separate source/private/Prepare/Promote admissions below are consumed; do not replay.
**Order:** 460, delivering independently accepted Order465 after Q254 publication.

## Exact target and limits

Prepare the successor to the verified single local: commit
`46004d6f9b61a02f14259fd3f911e85a72ae0c60`, parent
`44ef5e08f985c59d61351ecf32e503233f4038c5`, tree
`5a50788a33b995ff4e22e773870d9655f2a465e1`. The ten-path diff changes only
invoice-print receipt validation, its test, and recorded contracts/evidence.
Migration, server, workers, queries, package/lock and dependency bytes are unchanged.
Exact CI34721116555 completed successfully on this head: all six jobs passed,
including the database referee (11 passed, 0 failed). This is source acceptance,
not a local promotion or permission to execute the helpers below.

Use one fresh immutable source/control pair beneath `D:\Yellow\runtime`, named
`order460-46004d6f9b61a02f14259fd3f911e85a72ae0c60-source` and `-control`.
Only one junction to the existing dependency tree; no new checkout, dependency
installation, database, recovery copy, migration, reseed, provider activation,
Docker/WSL action, cleanup or in-place edit of the serving artifact.
Preserve the current44ef source/control, all consumed receipts, and every unrelated
working/index change. Existing migration91 and worker-settled receipts remain
explicit predecessor provenance, not newly executed evidence.

## Source-only file scope and ownership

Under `.yellow/evidence/order460/`:

- Artifact/adapter builder: `prepare-receiving-runtime-46004-20260913.ts` and
  `prepare-receiving-runtime-46004-20260913.test.ts`.
- Same builder: `current-runtime-46004-adapter.ts` and
  `current-runtime-46004-adapter.test.ts`.
- Cutover builder: `current-runtime-46004-cutover-20260913.ps1` and
  `current-runtime-46004-cutover-20260913.test.ts`.

Coordinator owns this question, Order460/Review460, Order465/Review465,
`docs/PROJECT-STATUS.md`, `handoff/LEDGER.md`, `DECISIONS.log`, and compact
metadata-only proof/admission receipts under `.yellow/evidence/order465/`.
Reuse hash-bound existing artifact/parser, bounded supervisor, host/owned-process,
HTTP and ACL primitives. Do not copy another stage-chain, worker capture/auditor,
recovery or migration implementation. No product code change is admitted here.

## Required behavior and independent proof

Imports and Review must be inert or read-only, never expose private values, and
refuse execution authority. Derive exact Git/archive/tree/file metadata through
bounded reads; prove committed source identity rather than copying the dirty tree.
Prepare preserves all retained environment values except the build SHA. Bind the
same retained database, native PG generation, frontier91, prefilled login, six
approved workers and disabled fiscal worker/providers. Preserve native100ns process
identity and bounded logs/timeouts; fail on stale PID reuse, path escape, wrong
receipt type/hash, changed credentials/source, occupied ports and reused outputs.

The cutover source must support Review/Prepare/Promote/Rollback with explicit
action separation. Before a later admitted actual Promote, independently prove
the rollback route and all executable predicates. Reuse the current stopped/started
process mechanics, not completed Q251 commands. Stop only the exact verified44ef
generation; never run both six-worker sets concurrently. Temporarily stage46004
on3001, verify authentication, invoice reads, genuine fiscal-blocker semantics,
frontier91 and the exact served print-asset bytes. Stop only that owned generation,
launch the same candidate on3000 and repeat the read-only smoke checks. Preserve
old output roots; every new stage/live/rollback generation has fresh bounded logs.
On failure, stop only new owned processes and restore/prove the unchanged44ef
source with a fresh rollback generation. Never reset/reseed or retry blindly.

Root, who implements none of these helpers, must personally inspect and execute
their focused hostile/ownership/rollback proofs. Actual artifact creation,
credential preparation, launch/stop/promotion require separate exact hash-pinned
admissions after final source acceptance; actual cutover additionally requires
all six exact-head CI jobs green. Source preparation is not runtime permission,
successful staging, deployment, debit-note policy approval or phase completion.

## Source artifact creation admitted separately

Root inspected the materializer and personally executed its four focused checks
(4 passed, 0 failed, 10 assertions), including actual shared-dependency inventory.
Exact helper`3cf3117f7e776bd3cd276de958760b86d7aa1ebabc8fa831b35785a5acfc5659`
may now create only the two initially absent source/control roots above, with the
pinned Git archive/tree and existing node_modules junction. Root will check
post-create extracted blobs, source/index/ref preservation and unchanged host.
The exact metadata-only admission is
`.yellow/evidence/order465/q255-source-creation-admission-20260913.json`.
This admission does not permit private environment preparation, app/process or
database actions; cutover remains unexecuted and independently reviewed separately.

## Private configuration preparation admitted separately

Artifact creation has completed and root verified every extracted path/blob,
shared dependency bytes, and unchanged source/index/refs. Root now admits only
adapter730a33480fd8785f643bce9e4bed31565862c59a5dbff6178e0f0d0ec92f39f1's
`prepareCurrentRuntime46004` against artifact receipt377d0111. The reviewed path
checks the exact retained environment hash/ACL and changes only YELLOW_BUILD_SHA;
each new private file receives explicit protected current-user-only permissions.
Root executed its four focused tests (4/0/17) and inspected all preparation guards.
No migration, new credentials, old-file overwrite or app/process action is allowed.
Exact root admission: `.yellow/evidence/order465/q255-private-preparation-admission-20260913.json`.

## Cutover source accepted; generation Prepare only admitted

Final cutover helper55e05bfcfba8fe0a48a79ec89349d9decf33015a98f2409df8baa573d289dc44,
testffa04bd8bf3f77b2ded6ae05293f36cf4160cd2f28e5ad062a49f19fd21168ef.
Root inspected its complete import/guard/action closure and personally executed
all three focused suites:20 passed,0 failed,88 assertions. Explicit strict native
TypeScript check passes (the initial invocation required --ignoreConfig; no source
failure was suppressed). Root also executed the complete authority-free prefix
against real prepared metadata: Get-PreparedCandidate(false), OldIdentity(false)
and Get-CurrentOldLaunch pass, binding the untouched44ef app16604/supervisor11376
and retained PG2340. Actual private ACLs and env-only-build delta independently pass.

Root now admits only action Prepare with the exact pins in
`.yellow/evidence/order465/q255-cutover-prepare-admission-20260913.json`.
It creates fresh bounded stage/live/rollback supervisor generations and a receipt;
no launch, stop or database operation. Actual Promote remains a separate admission.

## Exact one-shot Promote admission

Generation Prepare completed once (receipt887904cf), producing stagea8c6f5d4,
live7b1af5ab and rollbacka7ea495d supervisors. Root separately executed the complete
Assert-PromoteAdmission and Get-CurrentOldLaunch prefix against the real files and
host. Both passed, including saved-credential equality, exact source/receipt/ACL
checks, six-green CI71e7217e and free3001. Nothing was started/stopped by that check.

Root now admits one Promote under exact helper55e05bfc and pins recorded in
`.yellow/evidence/order465/q255-cutover-promote-admission-20260913.json`.
Stop only app16604/supervisor11376 with their exact native starts; PG2340 remains.
Stage46004 on3001, prove login/invoice/fiscal-blocker/asset, stop only that owned
generation, launch the same release on3000 and re-prove. The reviewed failure
branch may restore44ef through its fresh rollback generation only after exact
owned cleanup is proven. No concurrent worker sets, migrations, reseed, providers,
main merge, retry of a consumed generation or financial-policy change is admitted.

## Actual outcome — admission consumed successfully

Root personally executed Promote once; exit0, receipt023e0f4e. Published46004
now serves3000/frontier91 with app14944/supervisor6812. Root separately verifies
the receipt hash, /ready, exact new print bytes, native starts, sole listener,
old/stage process absence and unchanged PG2340/55503. Saved login, invoice reads,
issued readiness and honest supplier-status blocker pass; providers stay empty.
See Review460 and .yellow/evidence/order465/q255-promotion-proof-20260913.json.
No new database, migration, reseed, dependency copy, actual rollback, main merge,
provider activation, debit economic-policy decision or phase closure occurred.

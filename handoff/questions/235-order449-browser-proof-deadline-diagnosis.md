# Q235 — Order449 bounded browser-proof deadline diagnosis

## RESOLVED — exact diagnostic and single unchanged validation admission

Order449's exact seven-path candidate tree
`f2ada4bb904216198a3d3c735cad7b94515776bf` passed its independent native
signed-session proof, typecheck, 187 import boundaries and the zero-installed-package
local licence census. Its first exact-artifact standing run is retained at
`.yellow/evidence/order449/validation/standing.json`, SHA-256
`53d8a7457566e869153864ac0508a0cc11bfae5e8dad7b2c5b399b4aaea734ed`.
That run reported 1,929 passed, 1,407 explicit database/Unix skips, two failed and
34,517 assertions across 537 files. The failures were browser proof-process
deadlines, not Order449 assertions:

- `tests/operator-appearance-geometry.test.ts` reached its unchanged 30-second
  outer timeout in the Chromium geometry case without reporting a geometry
  assertion failure;
- `tests/operator-invoices.browser.test.ts` reached the existing owned-process
  lifecycle deadline with empty stdout and only Chromium background-service stderr,
  before reporting its Q208 deep-route result.

The failed standing receipt, all four logs and prior Order448 failure evidence are
immutable inputs to this diagnosis. Do not edit application, UI, tests, browser
helpers or deadlines and do not classify either failure as passing from log review.

One private helper under `.yellow/evidence/order449` may expose two separately
invoked modes after root reads it completely:

1. `DiagnoseFocused` runs only the exact retained artifact files
   `tests/operator-appearance-geometry.test.ts` and
   `tests/operator-invoices.browser.test.ts`, sequentially, with the candidate's
   private Git index and all database, provider and runtime authority removed. Each
   process and log is bounded. The helper may terminate only its own still-running
   test process tree at its outer deadline and must prove the owned root process is
   reaped. It records both results, preserves the failed standing evidence and
   verifies the artifact, dependency junction, native evidence, real index, HEAD,
   flags and tracked worktree before and after. It does not start or stop an
   unrelated browser, application, Docker, WSL or database.
2. `ValidateUnchanged` is admitted exactly once only after root separately reviews
   the successful focused receipt hash and supplies it explicitly. It requires both
   focused files to have exited zero without a helper deadline, then runs the same
   unchanged four standing gates against the same candidate artifact: full Bun
   suite, typecheck, boundaries and licence policy. It records actual pass/fail/skip,
   assertion, file and installed-package counts. It does not retry a failed focused
   file or standing gate.

Both modes use new, non-overwriting evidence directories. The first failed standing
and focused evidence remain retained regardless of outcome. Any further failure
requires diagnosis and a new written admission; it is not authority to raise a
deadline or alter UI/product source. Source publication remains forbidden until the
unchanged full standing receipt is green and independently reviewed. Exact-source CI
after publication remains mandatory and no merge, deployment, provider activation,
local promotion or Phase 7 completion is admitted here.

# Q252 — current Q251 single-local promotion source

Disposition: technical source-only scope admitted by primary owner under the
standing implementation directive. No new founder policy or live action approval
is inferred. The actual promotion remains a separately reviewed, hash-pinned root
handoff after accepted current-source staging and worker proof.

## Why explicit scope is needed

Order460's current Q251 controller scope is Review/Stage/StopStage only. Its
frozen predecessor had a separate Promote tail, but importing that tail unchanged
would retain old source/frontier and dated fiscal-fixture assumptions. The new
receiving source44ef5e08f985c59d61351ecf32e503233f4038c5 now has exact green CI,
verified retained recovery90, canonical0091/no-op and completed preparation91.
The current Stage/Settled91/Stop proof has not yet executed. Do not modify the
stage controller after its source/receipts are frozen.

## Exact addition

Only these two new private evidence files may implement the promotion successor:

- .yellow/evidence/order460/current-runtime-q251-promotion-20260913.ps1
- .yellow/evidence/order460/current-runtime-q251-promotion-20260913.ps1.test.ts

Reuse the final independently reviewed Q251 controller's guard/import/HTTP/owned
process functions and the old accepted Promote tail mechanics; prefer guarded
in-memory imports to a new framework. Keep Review and Promote separate. Preserve
all original source, failed receipts, stage controller and migration/worker proofs.
Root records this question and the matching Order460 scope addition; no production
code, schema, fixtures, dependencies or package changes belong to this question.

## Required proof and runtime constraints

Synthetic/AST proof must execute actual receipt/type/process-identity predicates,
reject stale90/547 evidence, absent or mismatched stage/worker/stop receipts,
string-coerced Boolean/integer fields, live3001, changed source/credentials and
unknown fiscal responses. Keep exact current91 readiness, native100ns timestamps,
bounded HTTP/logs, original tenant and prefilled credentials, six approved workers,
fiscal worker/providers off. Reuse a reviewed native-precision supervisor only;
the unprojected parent still writes CIM timestamps and must not reintroduce the
already observed precision defect. Any reused stage supervisor must prove fresh
port3000-specific status/log paths while preserving all consumed3001 evidence.

Before actual Promote: independent final helper proof, exact source/CI/artifact,
five migration-proof pins, prepared candidate/environment pins, accepted current
Stage/Settled91/Stop pins, current stopped host, fresh output absence, and a
separate one-shot root admission are mandatory. Success may leave only proved
loopback3000 running. Failure may stop only the exact launch-owned generation;
never generic PID termination, reset, reseed, retry, provider activation or merge.
Source preparation alone authorizes no credentials, DB, app or process action.

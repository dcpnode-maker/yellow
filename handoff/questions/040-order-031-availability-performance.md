# Question 040 — Order 031 misses the 500-space budget

Semantic proofs P1-P7 passed, but P8 measured max 1588.05 ms against <1000 ms. May the
query aggregate overlapping occupancy once per space and join it to mappings, preserving
the exact D-129 arithmetic and budget? May P8 count exactly the performance unit type's
500 options rather than seeded/fixture options?

## RESOLVED

Answered by D-130 and `040-ARCHITECT-RESPONSE.md`.

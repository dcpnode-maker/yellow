# Q276 — inert native91-to92 preservation and metadata preparation

Order460,13 September2026. Q275 exact feff6dc8 published; CI34760154085 running.
This scope prepares the remaining release gates in parallel, not a live action.
Root read full existing q251 capture/verifier and reviewed their fixed90-to91
wrapper. It cannot be replayed against current41415/frontier91 or new92 source.

Admit only these five NEW ignored source/proof files:

- .yellow/evidence/order460/market92-capture.ts
- .yellow/evidence/order460/market92-verify.ts
- .yellow/evidence/order460/market92-preservation.test.ts
- .yellow/evidence/order460/market92-registration.ts
- .yellow/evidence/order460/market92-registration.test.ts

Root owns this question, Order460, Review460/checklist and coordination records.
No published product source, migration, existing consumed helper or snapshot edit.
All sources are inert on import. No actual database connection, clone, migration,
metadata registration, process stop/start, private-input read or output materialization
is admitted by this source order. No new dependency/worktree, provider or Git action.

## Capture/verifier ownership: q258_runtime_cutover

Adapt existing reviewed q251 semantic capture/verification without changing their
files. New explicit serving91/recovery91/serving92 contracts must bind the existing
serving database yellow_order444_review_a10851786f17 and one future collision-free
recovery database yellow_order460_recovery_feff6dc86982_20260913. Do not create it.
Pin candidate feff6dc86982a5386968674b6c7077721a701785, exact0092 checksum and exact
function body/owner/privileges/config/identity. Reuse all historical row hashes,
raw and normalized migration ledger, sequences, schema object definitions/ACL,
roles/memberships and companion-data preservation. Capture no secrets/raw hotel
records in output. Keep existing resource/session and transaction bounds.

Only migration delta: one exact schema_migration92 append plus new exact
public.assert_market_compset_authority(uuid,uuid,uuid,text). No prior object
change, new table/role, seed row, financial/occupancy/fact/outbox change permitted.
Exact forward/no-op and recovery equivalence receipts are separate; acceptance
does not authorize launch. Unknown/missing/extra fields and mismatched closure fail.
Independently test hostile altered rows/sequences/ledger timestamps/checksums,
function body/ACL/search_path/owner/signature, tenants and inventory. No fixture
exception that treats arbitrary catalogue hashes as proof of exact new authority.

## Metadata ownership: q258_source_adapter

Prepare one small INERT registration function with transaction seam, plus tests.
It inserts-or-exact-verifies only market_compset with the current exported
MARKET_COMPSET_SCHEMA and these exact permission identities/descriptions:
distribution.market:read / Read confirmed market competitor evidence;
distribution.market:write / Confirm market competitor evidence.
Use atomic existing SQL patterns, serialize concurrent registration, and fail
closed on any conflicting schema/description. Return only explicit added/existing
metadata; never modify conflicting rows. No extension instance, tenant/property,
user/role, role-permission grant, authorization, seed41 instances or evidence.
No CLI/live execution, URL or credential access in this preparation. An actual
registration requires independent relevant proof and a separate root admission.
The existing synthetic fixture is design reference only, never executable live.

Root independently inspects and personally executes pure proof before accepting.
Actual retained native recovery/registration proof and live orchestration remain
separately scoped after source acceptance and exact-head release CI. Do not claim
mock SQL execution is PostgreSQL proof. Keep Phase7/18-phase scope and priorities.

## Root-only read-only catalogue diagnostic

After source review, root may perform bounded metadata-only queries on the
already retained synthetic Q265 database yellow_order472_compset_20260913 at
127.0.0.1:55503. Reuse protected existing authority in the child environment,
validate its exact target/role through the existing Q265 parser, and require
native16.15/server cluster identity. Use a read-only transaction with existing
5second connect/10second statement bounds. Read only0092 function signature,
owner/ACL/config/body hash and current migration frontier to check real PostgreSQL
shapes against the source checker. Do not query hotel records, print credentials,
create files/databases/roles, register metadata, migrate, stop/start a process,
replay Q265 Prepare, or change the live91 database. This does not authorize the
full preservation capture or any actual recovery/registration execution.

Actual root diagnostic exposed owner EXECUTE in proacl and JSONB privilege-object
key ordering omitted by the original fixture. One corrected-checker replay on the
same metadata is admitted; no new target or different data scope. Preserve both
actual RED results. Definition hashbf649258f3c7d10a1c0a9eec839d786eeb2cc33f3c54085dcb7838b3387d0979
is now measured from the native16.15 function, not a guessed fixture constant.

## Read-only recovery sizing admission

While Q280 CI repair proceeds, root may inspect only native cluster metadata and
serving release metadata for recovery preparation. Reuse the same protected
yellow_deploy authority, first verify exact127.0.0.1:55503/160015/data-directory
identity through a read-only postgres transaction. Inspect sizes/owners/session
counts of Yellow databases and serving yellow_order444_review_a10851786f17
frontier/count plus existence/exactness of the three market registration rows.
No hotel row values, full preservation capture, role/grant write, schema change,
connection termination, recovery creation, app restart or helper replay.
Keep5s connect/10s statement limits and bounded sanitized output. Source/recovery
pins still await eventual exact accepted successor; measurements are not release
approval and must be revalidated before a later separately admitted action.

Actual first all-database sizing query failed within the bounded diagnostic;
no size/result was accepted from it. Narrowed serving-only size + catalogue
inventory succeeds in2.22s: serving21,740,567bytes/frontier91,29sessions,
market_compset type0 and market permissions0. Other64 Yellow databases have0sessions.
Native identity matches. No hotel data was read/changed. This makes explicit
metadata registration necessary for activation; Q279 isolated proof is not
evidence that those missing live rows exist.

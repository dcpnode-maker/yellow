# Q232 — Bound the native Windows optional status probe

## RESOLVED — scoped technical repair for Orders447/448 and Q231

Root executed Q231's exact backend-only tree
`c3f69e9a6b173617691a8114dd33090c78247766`. The full standing suite returned
1,919 passes, 1,407 explicit skips and one failure: the canonical status report
exceeded its existing 4,500ms owned-process deadline on Windows. Captured output
reaches Historical records, but not Service lines. Types, boundaries and licence
checks pass. Preserve the failed standing receipt and every log.

The native script still performs unbounded optional command discovery, Docker
availability and Compose queries. Diagnose the specific delay first. Admit only
`state.ps1` and the corresponding regression cases in
`tests/project-status.test.ts` to bound the whole optional probe, including command
discovery and any table-count query. Preserve valid metadata, historical counts,
current-order validation, footer, caller environment and fail-closed report errors.
Do not raise the existing outer deadline, remove probes, skip assertions, start
Docker/WSL/services, or terminate unrelated processes. Missing, failed or timed-out
optional probes mean unavailable. A successful bounded probe must retain service
and table-count reporting. Test deterministic unavailable, success and slow cases,
with no surviving owned child. No UI, database, fiscal or runtime change.

After root inspects and executes the focused repair, Q231 may produce a revised
ten-path candidate: its eight admitted functional paths plus these two files.
Preserve staged UI/history hunks in the mixed test file; use only the reviewed
repair delta. Reuse the same source artifact and existing dependency junction,
first verifying its complete c3f69e9a tree. Keep the old candidate and failed proof;
write a new scoped manifest and run the unchanged four standing gates. Do not
silently repin a changed source or retry a failed run without diagnosis.

Coordination scope includes this question and factual updates to Order448,
review448, docs/PROJECT-STATUS.md, DECISIONS.log and handoff/LEDGER.md. Private
validation/publication helpers may be revised under Q231 with explicit root
inspection before execution. Publish only accepted functional source to existing
PR92 for exact-source CI; no main merge, live-app promotion, provider activation or
phase-completion claim.

## Focused proof accepted — 2026-09-08

The final native script shares a650ms optional-query budget and a1,000ms total
owned-process lifecycle budget, without changing the4,500ms outer deadline.
Command discovery, both Compose queries and conditional table reporting are
bounded. Cleanup addresses only the owned wrapper tree; a naturally exited
wrapper's taskkill128 is accepted only after HasExited, and normal results still
require output EOF. The successful shim preserves the quoted SQL as one argument.

Frozen SHA256: state.ps1
00d38b22a18e47fc5d2de4fb0e3296970022431c7f7dc9cd25ab99e9ed2bbf6f;
tests/project-status.test.ts
63a91d8237d3f64869fd058d7bdd1278b1ebcfff8bd57f3428fd132c694ad461.
Root personally executed the full status file:7 passed,4 explicit Unix skips,
0 failed,117 assertions. The non-implementing credit_note_sql_builder agent
inspected the frozen repair and independently executed the same file with the
same counts, plus passing typecheck and scoped diff-check. Source hashes stayed
unchanged. Slow initial and conditional queries proved their owned child absent
and no delayed marker. This independent review covers Q232, not that agent's own
Order448 implementation. It neither executes Unix proofs nor asserts a service
start or a new database proof; the canonical report retains bounded optional
discovery/read behavior with its inherited PATH.

Earlier attempts remain failures: an external hold process could outlive the
wrapper; the replacement builtin hold needed CALL for batch return, CRLF-safe
test parsing and trailing-whitespace-safe sentinel matching. A later cleanup
failure was a naturally exited wrapper race (taskkill128 at370ms), not a reason
to raise the budget. Temporary diagnostics and an experimental longer budget
were removed before the accepted runs. One overlapping-source run was rejected.
The complete revised ten-path standing run and publication are still separate
pending gates; this focused pass does not overwrite Q231's failed receipt.

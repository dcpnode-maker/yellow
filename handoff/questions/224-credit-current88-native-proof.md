# Q224 — Exact canonical88 native proof admission

**Status:** RESOLVED technical execution scope, Order447/Q223. 2026-09-08.
UI, app promotion and real provider activation remain paused/out of scope.

The coordinator personally read both complete private canonical helpers, reviewed
the pristine77-null-projector and exact historical-ledger-prefix repairs, and
admits independent execution by native_helper_release_proof (not a domain or
runtime-readiness implementer). No whole-phase or external-certification claim.

## Frozen inputs and initial read-only truth

Runner .yellow/evidence/order447/canonical-run.ps1 SHA256
0681c19cfb63c2c390bf400700c05e879bfe53ac965d57c824a38a8374d14d44.
Proof .yellow/evidence/order447/canonical-proof.ts SHA256
2cb3428f9f8751ee1b6f7d080cd89abe7a53bc88c842731176c2148fa7934618.
Preflight canonical-logs/20260907-202831-299-PreflightUpgrade.json SHA256
3b7e9b9e1c10b4519d9ff74ef42d28299e0e8da6cc58dd727aefc1ced050b02e
pins all13 proof/source files, all88 migration hashes and full target/outside
snapshots. Canonical0088 is the unchanged independently tested214754e9 draft.

Only the existing native PostgreSQL16.15 server127.0.0.1:55503, postmaster15956,
at D:\Yellow\temp\order434-production-cluster-20260906 is admitted. Credentials
remain in the existing protected loader and memory, never output or Git.
Live app7568/parent9508, creation08:09:36UTC and sole3000 listener must stay exact;
3001 must remain absent. Do not start Docker/WSL, a new cluster, worktree or app.

- Preserve patched candidate yellow_order446_credit_upgrade_20260907 at87/b34,
  1950 existing rows,17 permissions,129tables/119RLS/119policies/28FORCE/2views.
- Upgrade only yellow_order446_referee87_20260907 from genuine original87/ca6,
  1374 existing rows and21 proof permissions. Never migrate the patched candidate.
- Preserve pristine yellow_order434_production at77:127tables/117RLS/117policies/
  26FORCE/2views,11permissions,90rows,0tenants,projector absent,0other sessions.
- Exactly one new clean database yellow_order447_referee88_20260908 is admitted
  only after upgrade evidence review, full fresh preflight, confirmed absence,
  zero template sessions and at least2GiB free on D. Clone only the pristine77
  template. It is retained proof evidence, not a duplicate application runtime.

## Three separately frozen actions

1. Upgrade: require exact initial snapshot immediately before writing. Use the
   production migration runner to apply only0088, then a genuine no-op. Preserve
   all preexisting129-table rows, first87 ledger rows including applied_at, all
   nonfunction catalogue and every other function's body/identity/ACL/config.
   Only the exact owner-private projector body becomes b34eaf00 and one ledger
   entry is added. Dump and normalize the resulting canonical88 schema; emit a
   fresh PreflightClean. Stop for coordinator inspection before action2.
2. CleanReferee: independently recheck that fresh snapshot and exact accepted
   normalized-upgrade-schema bytes. Create only the absent name above; prove the
   exact77 clone, migrate78–88 and no-op while preserving the first77 ledger and
   prior rows. Require88/129/119/119/28/2 and15 canonical permission rows. Its
   normalized schema must equal the populated upgrade byte-for-byte. Collision
   check then execute the unchanged seed once transactionally and personally run
   the unchanged referee11/11, preserving all prior rows/catalogue/ledger. Emit
   fresh PreflightReadiness; stop for coordinator inspection before action3.
3. ReadinessDrift: use the exact clean88 snapshot and separate yellow_runtime
   login. Demonstrate ready baseline,13 committed one-at-a-time projector
   metadata/body/owner/ACL drifts denied, exact finally restoration and ready
   after each. Close both pools before full session-sensitive final equality.
   Never grant persistent runtime authority or modify other functions/roles.

Each mode rechecks sources, all migration bytes, full candidate/template/outside
state and host identity. No concurrent proof writes. Every failure retains its
target and evidence for read-only classification: no blind rerun, reset, drop,
termination of other sessions, weakened precondition or adopted replacement hash.
The coordinator must inspect exact failure state and document any repair first.
Q225 recovery may change only new candidate cohorts after these three actions;
its source/target admission remains separate.

After byte-equal executed canonical schemas are verified, copying the generated
normalization to tests/schema/expected.sql is admitted by Q223. Source-only tests
and absent-database skips are never reported as native acceptance. Record actual
commands, reviewer, counts, hashes and preservation evidence in review447.

## Action3 helper-only repair

The first execution completed12 denials/restorations, then its constructed body
probe failed before mutation with42601. JavaScript replacement-string dollar
tokens altered the PL/pgSQL text. This was private proof construction, not the
canonical function. Failure log994d0d0ac6ebb7dffd8e5a67149c7b339bc896b5378619291581498a1ad040ba
and failure JSONc787cd6e7203f7822f1b6d8738d5debd448b3dc5f63523fdf7f25fdbd0f3fd76
are retained. A read-only fresh snapshot is byte-identical to the full pre-action
state72b8ec620377ea0f51491279bd63f3146e7612f8a00bac3495915e806cb21dee:
all targets, functions, ACLs, sessions and outside state were exactly restored.

Admit only replacement-string→callback in canonical-proof.ts so the deliberately
added comment preserves every literal dollar byte. No production SQL/source edit.
The reviewer must personally check the repaired splice against exact definition
prefix/body/comment/suffix, freeze the changed helper and obtain a new read-only
source/state preflight. Root must inspect its hash and unchanged database payload
before explicitly admitting the full13-case retry. Earlier12pass+1error remains
recorded and is not retroactively counted as a passing run.

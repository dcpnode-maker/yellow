# Q243 — Exact87da CI failure repair scope

## RESOLVED — root routine technical authority, 2026-09-08

Exact87da26f354d08b545bc6b9127031cc29b38881ef CI34244482395 is red.
Independent log inspection establishes two separate failures, not new fiscal
business policy. Preserve the failed run; no automatic rerun or assertion waiver.

Windows job102122825304: the high-output supervisor test hits its10,000ms test
deadline while its synchronous child has the same10,000ms bound. Bun kills one
dangling process and exitCode is null instead of7. The phase that stalled is not
known. Admit test-only bounded reproduction/diagnostics first; retain the Unicode
payload, dual streams, file retention/tail assertions and overall10-second ceiling.
Reserve diagnostic/cleanup time inside that ceiling, not by increasing it. Do not
change the production supervisor or infer that a deadline is harmless.

Database job102123780249: the signed-display equality passes, but the subsequent
substring scan across serialized values fails. Opaque signed JWS strings may
contain the token SEK. GitHub redacts those values, so the exact matching substring
is unavailable. Admit exact allowed shape/key checking plus deterministic hostile
field and allowed opaque-value regressions, preserving signature/receipt equality.
No crypto, SQL, disclosure policy or production acceptance may change.

These files are outside454's scope. Order455 separately enumerates both repairs.
Production supervisor changes or other files require a further explicit question
and scope decision. Native454 source/fixture/helper pins must remain untouched.

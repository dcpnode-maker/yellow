# Q272 — current92 release and in-app status alignment

Status: bounded source preparation admitted by root,13 September2026.
Order460 integrates accepted Order472; HEAD63338ed3/source92, sole local41415/91.
No applied migration, financial policy or historical proof boundary changes.

## Status tranche — exact routine ownership

Builder /root/q258_runtime_cutover owns only:

- src/project-status.ts
- tests/current-source-status.test.ts
- tests/project-status.test.ts
- tests/current-management-demo-status.intentional-red.test.ts
- tests/founder-status.integration.test.ts

Append source records471 (frozen/unaccepted, built_unverified) and472 (independent
source acceptance only) without altering historical records. latestBuiltOrder472,
currentOrder460 (release integration), activePhase14 (bounded current priority).
Preserve all18 named phases and existing states except14 planned -> active;
no phase becomes reviewed/completed. Phase7 remains unfinished/active.
Contiguous independent review remains91 and dynamic runtime build/frontier
remains authoritative. Source review is not remote CI, deployment, RMS completion,
rate collection, automated pricing or provider acceptance.

Describe accepted identity/compset/map/attributes/planner scope and exact Q267/Q268
evidence from Review472. Record remaining publication/local/market-quality and
broader RMS work, retained fiscal471 and subsequent dependency-gated11→13→17.
Use a short clear current label; do not embed a claimed permanent live revision.
Existing470/source history stays intact. No native/runtime/business behavior edit.

Tests first, genuine stale-snapshot RED then focused green. Never execute
tests/project-status.test.ts or any state.sh/state.ps1/WSL path locally;
edit only its current snapshot expectations and leave real execution to CI.
Other selected DB-gated cases must retain explicit skips, not be called passes.
Root independently inspects final diff and executes safe focused tests/types.

Root owns this question and Order460/checklist/review/current-state/decision/ledger
records. Release compatibility and actual isolated CI database wiring are being
audited separately; no additional paths are admitted until enumerated below.
No live database/process/Git/index/provider/credentials/dependency/cleanup action.

## Current-schema compatibility tranche

Read-only audit confirms canonical migration0092 adds a function, not a table;
current table count remains129. Admit builder /root/q258_runtime_cutover to these
additional exact source-only files after freezing the five status files:

- .github/workflows/release.yml
- scripts/local-review.sh
- setup.sh
- setup.ps1
- tests/release-workflow.test.ts
- tests/free-host-arm64.test.ts
- tests/setup-current-catalogue-oracle.test.ts
- tests/migrate.integration.test.ts
- tests/database-acceptance.integration.test.ts
- tests/build-readiness.integration.test.ts
- tests/fiscal-retry-readiness.integration.test.ts
- tests/india-gst-accommodation-final-component-tax-recording.integration.test.ts
- tests/india-gst-accommodation-quoted-rate-applicability-recording.integration.test.ts
- tests/native-fiscal-release-containment.integration.test.ts

Align only full-current migration ledger/frontier/checksum expectations91→92
and append0092 where actual current-schema test setup requires it. Preserve all
historical prefix fixtures, named previous release proofs,129 table/RLS/policy
counts, and all behavior assertions. Do not execute setup, local-review, fiscal
native helpers or state scripts locally. Capture focused source-test RED/GREEN;
actual PostgreSQL results require separately executed appropriate proof.

## Isolated market CI tranche

Builder /root/q258_source_adapter owns only:

- .github/workflows/ci.yml
- tests/helpers/market-compset-environment.ts (new)
- tests/market-compset-environment.test.ts (new)
- tests/helpers/market-compset-fixture.ts
- tests/market-compset.integration.test.ts
- tests/market-api.integration.test.ts
- tests/market-runtime-readiness.integration.test.ts
- tests/ci-market-proof-routing.test.ts (new)

Keep the native proof parser/preparer unmodified. Introduce a tests-only strict
environment selector: default delegates to the existing native exact55503 target;
CI mode requires explicit market integration opt-in, a dedicated exact CI-mode
sentinel, CI=true and GITHUB_ACTIONS=true, and three explicit role-bound URLs for
one fixed ephemeral database yellow_order472_market_ci. Require loopback address
127.0.0.1 with a valid explicit port matching the CI POSTGRES_ADDRESS value.
Reject arbitrary host/database/roles, query/hash, missing passwords or ambiguous
mode. Never fall back to DATABASE_URL or another generic authority source. Errors
and test receipts must not expose secrets. Fixture writes must verify the selected
exact synthetic target and deploy session/effective role before any mutation.
Do not add an arbitrary database-name override to fixtures.

Add a required CI database step using existing remote provisioned PostgreSQL and
the repository's exact-owned ephemeral database pattern. It creates that one fixed
database, migrates all92 with deployment authority, sets the explicit test URLs,
executes all three real compset/API/runtime-readiness integration suites, and
cleans only its exact ephemeral CI database. Keep native runtime/catalog loading
disabled in normal Linux app startup. Align only normal CI ready frontier91→92.
Preserve Q270 required Windows routing and immutable P0 proof, every historical
prefix lane, assertions and deadlines. New routing/selector tests must exercise
rejection cases and actual required opt-in wiring, not solely inspect a label.

No local CI-mode execution against55503, no new local cluster, no native parser
relaxation, no production migration or source publication. Root independently
executes selector/routing tests and the unchanged native-authority integration
path before acceptance; actual CI execution remains a separate release gate.

Root review rejected the builder's accidental replacement of the untracked
four-case Q266 hostile readiness suite with a one-case happy-path test. Builder
is frozen on that file. Root restores its complete captured pre-edit text and
verifies the recorded0e14d896 SHA before returning ownership; this is restoration
of prior accepted proof, not new test authorship. The only later admitted changes
are parser import and exact validated target substitution. Preserve this failure
in Review460; no acceptance is based on the discarded weaker result.

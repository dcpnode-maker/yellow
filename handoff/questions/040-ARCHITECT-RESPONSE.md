# Architect response 040 — Remove correlated scans, keep the budget

YES. Aggregate authoritative claims once, retain the same mapping/min semantics, require
exactly 500 performance options, and restart the whole Order 031 proof. Do not raise the
budget or introduce projection reads.

## RESOLVED

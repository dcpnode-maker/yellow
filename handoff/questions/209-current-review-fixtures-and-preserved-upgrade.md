# Question209 — Current review fixtures and preserved predecessor upgrade

**Accepted technical implementation direction, 2026-09-07.** Parent Orders440
and444; primary owner Codex. This extends implementation scope before any edit.
PR92 at3a0a01316fbf8acc760e1cba38384042edbb86d2 is a separate frozen development
candidate with CI34087312357 in progress. Do not claim its checks cover later edits.

## Intent and boundaries

Make completed functions usable in the single founder preview and prove that
upgrading preserves genuine pre-existing invoices and receipts. Existing primitives
and services solve both needs: no new table, financial engine, event type, business
policy or provider activation. Canonical1–85, stable Order442 source/database/
credentials and old start/resume/supervisor remain unchanged. Neomorphism remains
deferred and no global expertise mode is restored.

The previous turn was concrete progress: actual UI/mobile repairs, frozen84-file
source publication, draft PR92 and live CI, not a stopped builder or completion.

## Exact file ownership

```text
native_resume_builder — new native review tooling only:
  scripts/run-order444-native-review-bounded.ps1
  scripts/order444-native-review.ps1
  tests/order444-native-review.test.ts
provider_transport_builder — current synthetic operational seed only:
  scripts/seed-review.ts
  scripts/seed-fiscal-review.ts
  tests/review-seed.integration.test.ts
  tests/fiscal-review-seed.integration.test.ts
fiscal_http_acceptance — populated predecessor proof, no production edit:
  tests/india-native-fiscal-populated-upgrade.integration.test.ts
  handoff/reviews/440-operator-invoice-workflow.md (own executed evidence only)
coordinator:
  handoff/questions/209-current-review-fixtures-and-preserved-upgrade.md
  handoff/reviews/444-native-review-and-ci.md (own independent execution receipt)
  handoff/orders/440-fiscal-submission-lifecycle.md
  handoff/orders/444-partner-review-and-astra-ui-integration.md
  .github/workflows/ci.yml (new required proof wiring only, after test freeze)
  docs/PROJECT-STATUS.md
  docs/design/BUILT-CAPABILITY-MANIFEST.md
  DECISIONS.log
  handoff/LEDGER.md
```

Read the referenced production/fixture/helpers for reuse; do not change their
scope silently. Independently execute high-risk seed and historical preservation
proof; a builder's posted result alone is not acceptance.

## Review seed contract

Baseline review role gains only the five already-defined fiscal permissions:
documents read/issue and submissions read/request/retry. It already has native
valuation finalize. No post-seal, approval, superuser or new global-role grant is
implied. Existing seed users/grants and approver separation stay intact. Canonical
migrations must continue to assign no role permissions automatically.

Separate deterministic fiscal review seed runs only in an explicitly identified
new synthetic review database after baseline seed/review setup. Reuse real Party,
reservation/folio/charge/intake/valuation/configuration/series and native-issue
services. Provide one genuine issued invoice and one eligible unissued source;
retained reruns preserve identities and documents rather than reset/delete them.
Do not import random test factories as runtime seed logic. Reuse their validated
topology without fabricating preparation/timing/document or provider receipts.
Use exact existing dated statutory configuration; do not introduce a new tax rule
or infer legal buyer, supplier status, SEZ, fiscal location or holidays. Explicitly
mark synthetic data. Setup-only series authority does not belong to the operator.
Show meaningful real list/detail/readiness/print from signed-session HTTP, deny an
unauthorized/cross-tenant user and preserve immutable totals/rows on seed rerun.

## Native prepare/promote/rollback tooling contract

Implement Prepare/Promote/Rollback using existing Bun1.3.14/native PG16.15 at
127.0.0.1:55503, not Docker/WSL or a new cluster. Accept an exact40-hex candidate
commit only with its matching protected source/control receipt; do not assume a
moving branch is a release. Before any actual execution root must append exact
source SHA, archive hash, source/control paths, new database names, port and all
commands/rollback to Order444. This question admits code, not runtime mutation.

Derived source/control roots must be distinct validated children of
`D:\Yellow\runtime`, using an Order444+SHA namespace, never the workspace/root,
old Order442 controls or a supplied broad arbitrary path. Prepare creates only
absent new targets; never overwrites an existing database/source. Two targets are
allowed in the later runtime admission: synthetic review and separate canonical
invariant database, each cloned from independently verified pristine77 and migrated
forward unchanged. Canonical seed/referee runs separately from operational fixtures.
Read protected values internally; no secrets in command-line arguments, console,
Git, screenshots or logs. Log files stay bounded with owned-child cleanup and no
automatic crash restart. Background helpers use hidden windows.

Before promotion prove exact source+frontier85 readiness, real prefilled sign-in,
authorized list/detail/readiness and empty provider directory on one short-lived
loopback staging port. Revalidate old3000 listener PID/start-time/command/receipt;
stop only that exact old app/supervisor, never PostgreSQL or unrelated processes.
Start candidate3000; repeat real proof. On failure terminate only exact candidate
processes and resume unchanged Order442 helper/source/database. Promotion and
rollback must reject foreign listeners, identity drift and stale receipts. Final
topology has one founder app; retain previous source/database for rollback.

## Populated81-to85 preservation proof contract

Current migration tests start from empty81, so they are not populated preservation.
The new proof must seed genuine81-compatible native invoices and fiscal submissions/
attempts/accepted signed receipts through existing production paths, capture their
original bytes, durable response bodies and financial/numbering lineage, then apply
unchanged82–85 and compare retained artifacts. No new grants from the migrations;
any exact synthetic test role grants occur separately and are recorded. Prove old
idempotency keys retain original outcomes and new85 reads/confirmation compose with
retained data under current tenant/property/actor authority. Do not delete cache or
financial records to manufacture success or substitute only row counts for content.
Use real crypto with fictional provider credentials locally, never external calls.

The proof's CI mode may use its disposable cluster. Native execution must be
separately admitted to exactly one new target after DB-lane coordination; never
run historical1–12/global-role mutation against the shared retained cluster.
Use a verified pristine77 clone, then78–81, populate, then82–85. Preserve template,
all other database inventories, global roles and canonical input hashes. No cleanup
of unrelated or prior proof targets is authorized. Before native mutation the owner
reports exact target/preconditions to root for recorded technical admission.

No founder choice is missing for this technical work. External provider onboarding,
legal policy and spending remain separate action-time authority, not assumed here.

## Exact native preservation-proof admission — 2026-09-07

Root admits the fiscal proof worker to the sole new database
`yellow_order440_q209_populated_review_20260907` on the already retained
`127.0.0.1:55503` PostgreSQL cluster. Before allocation, prove that exact target
is absent, `yellow_order434_production` is canonical frontier77 with zero tenants
and no other sessions, and record canonical migration hashes plus the complete
database/global-role/membership inventories. Stop on any mismatch; do not reset
an existing target. Clone that verified template once, apply unchanged78–81 with
the production runner, then run the scoped populated81–85 test. Never apply1–12
or change cluster roles, the template, old review databases or retained runtime.

Use only protected in-process environment values
`YELLOW_ORDER440_Q209_DEPLOY_DATABASE_URL` and
`YELLOW_ORDER440_Q209_RUNTIME_DATABASE_URL`, with
`YELLOW_REQUIRE_ORDER440_Q209_DATABASE=1`; never emit their values. Independently
recheck the migration inputs, template and complete outside-target inventories
after execution. Record failed attempts as well as the final result. No cleanup
is admitted. This is the sole active native DB lane; seed and runtime workers
must remain DB-read-only until root releases it. The test author's execution is
development evidence; root must execute a separate high-risk acceptance proof.

### Independent repaired-target execution

The first author run stopped after116 assertions: the test incorrectly expected
an explicit detail permission error where canonical82 and the retained service
deliberately return no document. Root personally verified that policy and the
existing revoked-authority proof. Frozen corrected test
`6104d175a9db09d30be0190236abb1b355f782a15d7319f3d45237db8e964d91`
retains both NULL detail denials and adds the distinct list permission-denial
assertion. Preserve the first target at85; do not reset or reuse it.

Admit root, who did not implement the test or fiscal services, to execute the
same preflight/prefix/populated proof independently against the sole new target
`yellow_order440_q209_populated_repaired_review_20260907`. This target must be
absent. All other boundaries above apply, including unchanged pristine77,
89-input inventory, global roles and outside-target databases. Root uses the
existing protected Order442 seed/app env loaders in-process and the byte-checked
81-prefix directory `D:\Yellow\temp\q209-populated-review-65095b713571494088f44024d558f596\migrations-81`.
No other allocation, cleanup, privilege or app mutation is admitted.

### Independent synthetic fiscal seed proof

Root's populated repaired-target run passed2/0 with143 assertions. The DB lane
is released solely to nonimplementing `fiscal_http_acceptance` for a new absent
`yellow_order444_q209_seed_review_20260907` on retained127.0.0.1:55503. Verify
pristine77/template/input/global-role/outside-inventory preconditions above,
clone once and apply unchanged78–85. Run the full baseline review-seed test then
fiscal-review-seed test in separate serial Bun processes, with split deploy/runtime
URLs and respective required flags. Generate a synthetic proof password only
in-process; do not replace, disclose or use the founder password.

Existing baseline fixture setup removes two exact synthetic checker/operator
assignments and one synthetic room-condition row; its rate test restores its
temporary synthetic extension change in finally. These are admitted only inside
this new proof database, never the retained app or a populated prior target.
The separate fiscal seed is additive/idempotent and must preserve original issued
documents and journal rows on rerun. Neither test may reset/drop the database or
change cluster roles. No cleanup or runtime promotion is admitted here. If source
fails, report the defect to its implementer and re-freeze before acceptance.

The independent baseline25/2 run found a real WIP checker-grant defect and an
invalid test array binding; fiscal tests did not start. The same new synthetic
seed-proof target is retained at85/one tenant/zero documents. Admit rerun of the
repaired baseline and fiscal tests on that target, with no reset. Scope includes
removal of only the five accidentally granted fiscal permissions from the exact
synthetic checker role during seed reconciliation; operator grants and all other
roles/permissions remain. No retained founder database or cluster grant changes.
Cross-day source anchoring must preserve original financial/service evidence,
not rewrite dates or idempotency inputs to make a rerun pass.

The first seed harness generated its synthetic login password only inside a
terminated process. Do not rotate/reset that target's login to manufacture a
successful rerun. Supersede the full baseline-rerun target with one new absent
`yellow_order444_q209_seed_repaired_review_20260907`, same pristine77→85 and
inventory boundaries. Preserve the original failed seed target unchanged. For
the repaired proof only, the reviewer may create one unique `q209-seed-proof-*`
child under `D:\Yellow\temp`, current-user-only protected, containing a single
small proof env artifact to retain generated synthetic passwords across retries.
Never use/print founder credentials, sync this artifact or stage it in Git. All
test reruns must reuse this exact new target and protected proof identity; no
further database proliferation, deletion or auth rotation follows this admission.

The repaired fiscal setup reached two real services/payments/valuations before
Bun's generic five-second beforeAll deadline expired. Independent read-only
census confirmed progress and zero residual sessions; no invoice had been issued.
Admit an explicit60-second maximum for this test's beforeAll only, which builds
two production-composed sources and repeats the seed for immutable replay proof.
Preserve the failed run. This is a fixture-construction bound, not an application
latency claim, production timeout change or relaxation of any assertion. Re-freeze
the test and execute it on the same retained target and protected identity.

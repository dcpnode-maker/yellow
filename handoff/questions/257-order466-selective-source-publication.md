# Q257 — Selective publication of independently accepted Order466

**Status:** admitted technical successor publication scope, 2026-09-13.
**Order:**466 release continuation. Authority is the coordinator's existing
source-publication mandate and completed nonimplementer Review466, not a new
financial policy, database, provider or runtime admission.

## Exact ten-path inventory

Parent must be46004d6f9b61a02f14259fd3f911e85a72ae0c60 on the existing
phase-7/operator-invoice-workflow branch and open draftPR92.

- src/http/operator/invoice-print.js
- src/http/operator/invoices.js
- tests/operator-invoice-credit-note-parity.test.ts
- tests/operator-invoice-credit-note.test.ts
- tests/operator-invoice-credit-note.browser.test.ts
- docs/CONTRACTS.md (Order466 append and accurate completed465/Q255 release note)
- handoff/orders/466-invoice-credit-note-read-workflow.md
- handoff/reviews/466-invoice-credit-note-read-workflow.md
- handoff/questions/256-credit-delivery-browser-validator-reuse.md
- this question

Product/test hashes must match Review466. No selected path may have an unrelated
cached change. DECISIONS.log, handoff/LEDGER.md and docs/PROJECT-STATUS.md are mixed
MM files and explicitly excluded; preserve all of their index and working bytes.
Their current local truth is also carried in the order/review and existing PR body.
All paused445 and other unrelated changes remain untouched.

## Procedure and limits

Reuse the proven native Q254 procedure, not its consumed admissions or receipts:
inspect selected diffs and new files; capture complete tracked working-byte hashes,
selected new-file hashes, outside-scope index entries/persistent flags and relevant
refs before changes. Use intent-to-add only the seven new tests/order/review/questions
when needed for native Git's path-scoped commit. Commit --only the exact ten paths,
with a [codex] message. No add-all, whole-index commit, reset, clean, new worktree,
dependency copy, forced push, remote-history rewrite or own-PR merge.

Verify exact parent/changed paths/committed blobs and preservation of all original
working bytes and outside-scope staged entries/persistent flags. Native Git's
disposable filesystem-monitor cache bit may refresh, as Q254 recorded; do not
claim its raw physical index remains unchanged. Non-force push only the existing
branch to its verified existing origin, keep PR92 draft, and verify exact successor
CI. Parent CI is not successor evidence. A failed check stays failed until repaired;
publication is not source acceptance, CI acceptance, main merge or local activation.

Scoped metadata-only receipts may be written with apply_patch beneath
.yellow/evidence/order466/. Never copy source/index/dependencies/private runtime.
The ten selected files and PR body may record checkpoint truth. Runtime stays
46004/frontier91 on the single localhost3000; Q255 cannot be replayed. No restart,
new artifact, database, migration, reseed, provider or credential action here.

## Precommit metadata discrepancy and receiving boundary

The first exact seven-path intent-to-add/dry-run preserved the complete source
digest, every outside staged mode/object/stage entry and unrelated refs. Its
normalized outside flag digest changed from c8287968 to bc1e51ac, however. The
initial probe retained only a digest, not raw rows, so the cause and initial flag
identity cannot be proven; it is NOT silently called a filesystem-monitor change.
Read-only reconstruction did not identify it. No commit or push occurred then.

An independent reader and root both now observe every outside flag as zero or
transient fsmonitor-valid only, with the expected seven intent-to-add entries
exactly in scope; all ls-files -t -v tags are H. No skip-worktree/assume-unchanged
or outside intent-to-add state currently exists. No source or staged content was
lost. Root admits continuation from this explicitly inspected receiving state:
capture the full raw and semantic per-path flags in a new metadata baseline and
compare it after commit. This does not claim the earlier flag discrepancy is
explained or byte-identical. Do not restore invented flags or alter outside entries.
The original source/stage/ref digests remain required across the entire sequence.

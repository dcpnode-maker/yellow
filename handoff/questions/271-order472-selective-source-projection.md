# Q271 — Order472 selective source projection

Status: source-only technical preparation admitted by root,13 September2026.
Order460; receiving HEAD63338ed312f925bddd951665189196e3615a51b9.

Astra's read-only audit matches53 latest source/test pins and identifies paused445
guest-picker fragments in four files also changed by accepted Order472. Whole-file
publication would include work not accepted by Review472. Frozen471 is excluded
entirely. Preserve all original working files and all existing staged entries.

## Exact ownership and bounded source preparation

Builder /root/astra_ultra_handoff owns only these new ignored private source files:

- .yellow/evidence/order460/order472-release-projection.ts
- .yellow/evidence/order460/order472-release-projection.test.ts

Implement a pure source projector for exactly:
src/app.ts, src/http/operator.ts, src/http/operator/operator.js and
src/http/operator/operator.css. Read the existing mixed-file projector as a
reference; do not modify it or inherit old source pins.

Take immutable published HEAD and current working bytes as inputs. Admit only
known exact input identities, preserve all published behavior and every accepted
Order472 hunk, and exclude only precisely identified paused445 fragments from
the returned candidate bytes. Reject unknown input drift/ambiguous matches.
No generic substring stripping, mutation of original files, Git staging or
runtime feature disablement. Include exact input/output hashes, explicit
before/after fragments and tests for drift, missing/duplicate matches, retained
market routes/imports/lifecycle and excluded picker dependency closure.

Root independently reads the final transformation and personally executes tests.
Pure acceptance is not isolated release standing or a production-review waiver.
The final complete candidate must run its actual market/browser regressions
without picker assets; whole-working-file proofs cannot establish that outcome.

This admits only the two pure helper/test files and root updates to this question,
Order460, release checklist and review460. No source artifact materialization,
new index/object/ref/checkout, commit/push/merge, database/runtime/credential
access, dependency installation or cleanup. Additional mixed test/document paths
need explicitly enumerated scope before projection, not inferred exclusion.

## Root-owned vendor-byte release correction

Read-only Git hash-object proves the accepted upstream Leaflet CSS and LICENSE
have CRLF bytes that the repository's default text filter would normalize during
publication. Raw/filtered Git blobs differ (CSS2961b761/9ade8dc4;
LICENSEbcb3ba1a/7b41f200), despite current publisher SHA pins matching.
Explicitly admit root-only .gitattributes and tests/operator-market-assets.test.ts:
add exact-path -text exceptions for those two immutable vendor files and a small
byte-pin/attribute regression. All other text policy remains unchanged; do not
edit or rehash the vendor originals. Capture the regression RED, then prove
raw and filtered Git blobs agree without writing any object or changing index.

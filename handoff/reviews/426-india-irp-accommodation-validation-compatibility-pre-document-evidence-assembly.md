# Order 426 — India IRP validation-compatibility pre-document evidence assembly review

**Verdict:** PENDING FRESH INDEPENDENT TIER-3 REVIEW

**Implementation owner:** `/root/order426_implement` (not eligible to approve)

## D1282 implementation evidence

The exact export first failed its dedicated intentional-red test `0/1`. The product
implementation then passed focused `22/0` (59 assertions), Orders413–426 composition
`107/0` plus seven expected database skips (934 assertions), and the standing suite
`1,444/0` plus 1,054 expected database skips (20,629 assertions; 2,498 tests across
462 files). Strict TypeScript, 159 import boundaries, 23 dependency licences,
production audit zero, image pins, protected-input comparison and diff checks pass.

Permanent subprocess-isolated probes coherently recompute mutated child evidence and
accept only the exact Order426 validation class/message. They cover Order424 and
Order425 independently, including source, count/order, family, INR/B2B, inherited
Order419 ancestry, enriched item content, Qty, Unit and child evidence hashes.

No migration, schema, database, Docker, runtime, local app or `.yellow` state was
changed. A fresh non-implementing Tier-3 reviewer must personally execute the complete
proof, challenge the guards and record an exact candidate hash before approval.

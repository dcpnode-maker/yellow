# Order 414 — Different fresh independent Tier-3 rereview

**Verdict:** APPROVED — D1235

**Reviewed candidate:** `461a495`

**Approved base:** `4969c8a`

**Reviewer:** `/root/order414_d1234_rereview`, different fresh independent non-implementing Tier-3 reviewer

## Verdict

No finding. The repaired candidate closes D1233: the complete balanced journal
topology and every reproducibly verifiable nested tax-detail scalar, cross-link,
amount, route, component, jurisdiction and posting field are now bound. The permanent
correctly-rehashed hostile matrix rejects malformed, duplicated, reordered,
unbalanced and semantically changed journal/tax-detail graphs.

D1234's pure-authority boundary is accurate and contained. One-use journal-line and
route-mapping UUIDs remain canonical, unique and cross-link coherent, but a coherent
replacement of a distinct random UUID remains valid integrity lineage because the
caller can recompute the documented unkeyed hashes. These opaque identifiers are not
returned and grant no authorization or monetary/statutory authority. Malformed,
duplicated and broken-cross-link identifiers fail closed.

## Reviewer-personal executable proof

- exact Order414 focused suites: **14 passed, 0 failed (197 assertions)**;
- permanent correctly-rehashed journal/tax-detail mutation matrix, including the
  former root-description escape: green;
- full standing suite: **1,347 passed, 0 failed, 1,054 skipped (19,872 assertions)**;
- TypeScript strict check, **151-file** context-boundary check, **23-package** licence
  policy, container-image digest pins and exact diff/scope checks: green;
- isolated official PostgreSQL **16.15**, current frontier migrations **1–73**,
  acceptance **23/0 (65 assertions)** and byte-exact schema snapshot: green;
- isolated official PostgreSQL 16.15 Order413 live source: **5/0 (197 assertions)**;
- compatible isolated frontiers: Order367 at migration 70 **17/0 (693 assertions)**,
  Order407 at migration 71 **15/0 (144 assertions)**, and Order408 at migration 72
  **7/0 (92 assertions)**;
- separately rebuilt referee fixture: **11 passed, 0 failed of 11**.

Docker Desktop was unavailable, so the reviewer used the repository's already-pinned
native official PostgreSQL 16.15 toolchain on isolated port 55760. The first current-
frontier acceptance attempt omitted `pg_stat_statements`; it failed only that exact
preload assertion and was discarded. A clean restart with the required preload passed
23/0. An initial adjacent run used the wrong current frontier and retained fixture
rows; those results were discarded, and fresh exact migration-70/71/72 databases
passed independently. An initial referee run used the ordinary seed rather than the
protected referee fixture; it was discarded, rebuilt separately, and passed 11/11.

All reviewer-created databases were dropped and the isolated cluster was returned to
its stopped state. Stable/default databases, localhost:3000 and `.yellow/` were not
read or mutated. Order414 is approved only for its pure numeric item-source contract;
no ItemList, document, provider, API/UI/local, deployment, merge, push, Phase7 or
application-completion authority follows.

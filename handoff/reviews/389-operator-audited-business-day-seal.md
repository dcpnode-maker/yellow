# Order 389 — fresh independent Tier 3 review

**Verdict:** APPROVED
**Base:** `b4af963`
**Executable candidate:** `6f82c20`
**Reviewer:** `/root/order389_fresh_review`, fresh non-implementing Tier 3

I independently approve the audited operator business-day seal journey. The exact
zero-byte POST is bound to an authenticated actor, edge
`financials.business-days:seal` scope and exact property grant before the unchanged
database capability independently rechecks `business_day.seal`. The adapter derives
tenant, actor, audit envelope and idempotency server-side, passes the middleware-owned
`context.tx` directly, and returns only the minimized seal receipt. Query authority,
nonempty bodies including `{}`, malformed dates/keys, missing actor/scope/property
grant, unwired service and private service failures fail closed.

The browser exposes the irreversible action only from authoritative ready truth. A
native dialog names the selected date, cancel and native close issue no request and
restore focus, stale view/property/date/generation suppress the command, ambiguous
outcomes retain the exact retry key, definitive outcomes clear it, and every current
outcome refreshes persisted workbench truth with sealed-day rediscovery. Chromium
personally executed these behaviors and contained geometry in all six approved
appearances at 390 and 1280 pixels, including the 44/48-pixel target contract.

Reviewer-personal database proof used a new official Windows PostgreSQL **16.15**
cluster on loopback port 55511 with SCRAM authentication and
`pg_stat_statements` preloaded. Migrations 1–67 applied. Results were:

- migration regression **39/0 (187)** and review seed **25/0 (113)**;
- separately clean database acceptance **23/0 (65)**;
- close readiness plus workbench **20/0 (185)**, including the 366/367 open-day and
  500/MAX+1 candidate boundaries;
- isolated audited seal **8/0 (69)**, covering owner/capability containment, atomic
  latch/fact/outbox/idempotency evidence, exact replay, hostile authority/readiness,
  distinct-key and same-key concurrency, unpublished-event race, rollback and retry;
- native PG16.15 normalized schema byte identity, SHA-256
  `a5efaaae5ad3d2315cf2fc62a7dd2352e3992b9643f91784ca70994d1f89e8a9`;
- separately migrated and fixture-loaded referee database **11/11**.

Reviewer-personal application proof passed focused Order389/ancestor composition and
Chromium **31/0 (219)**, a broader non-database operator sweep **549/0** with **117
expected skips**, and the complete standing suite **1,274/0** with **989 expected
database skips** and **18,925 assertions**. TypeScript, 142 import boundaries, the
23-package licence policy, production dependency audit (zero vulnerabilities), exact
base-to-candidate scope and diff hygiene all pass.

Adversarial inspection traced each load-bearing mutation to executable proof: removal
of `parse:none` or the request-byte check admits a nonempty representation; removal
of actor/scope/property checks reaches the service; replacing `context.tx` or omitting
server injection breaks exact composition; response expansion or private error text
breaks bounded HTTP assertions; and removing readiness gating, confirmation/focus,
stale guards, ambiguous-key retention, refresh or 404 rediscovery breaks the real
Chromium result. The underlying PostgreSQL suite independently kills permission,
readiness, serialization, concurrency, rollback and evidence mutations.

One deliberately combined run reproduced the already documented ancestor-suite
teardown contamination: `business-day-seal.integration.test.ts` removes the catalogue
permission after its cases, which conflicts with review-seed role grants in that same
database. This is not product behavior or Order389 evidence. A new isolated migrated
database then passed the exact seal suite **8/0 (69)**; acceptance and referee were
also run in their own clean databases.

No product, test, migration, snapshot, seed, stable local, `.yellow`, Docker,
port3000, deployment, merge or push surface was changed by this reviewer. Only this
order-authorized review record was added; governance closure remains root-owned.

# REVIEW 045–091 — Wave D Windows state, review seed, calendar and AI boundary

**Reviewed by:** OpenAI Codex independent non-implementing reviewer
(`/root/review_order_092`) · **Date:** 2026-08-23

**Implementation commit:** `4874f5cd8052435d5c5d2f67698f6088cff502fe`

**Verdict:** **APPROVED**

## Exclusive discharge scope

This review approves only Wave D's exclusive owners: Orders **045, 046, 089 and 090**.
Supporting UI/HTTP assertions are cited from this reviewer's same-commit Wave A–C
executions and do not change their final-discharge ownership.

## Exact implementation and clean tree

Linux commands ran in the clean native clone
`/home/astha/projects/yellow-order-091-final-5d3f137` with Bun 1.3.14:

```text
git rev-parse HEAD
4874f5cd8052435d5c5d2f67698f6088cff502fe

git status --porcelain=v1
<empty>
```

The Windows continuation copy of `state.ps1` was independently proven byte-identical to
the implementation commit before native execution:

```text
git rev-parse 4874f5c:state.ps1
8f00c5d4b725082854e98a3a3644432bc6c23ef0

git hash-object state.ps1
8f00c5d4b725082854e98a3a3644432bc6c23ef0
```

## Order 045 — native Windows fail-closed state

### Valid checkout

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\state.ps1
```

The exact blob ran in the native Windows Git checkout, printed the real branch, head and
dirty count, completed the Open work/service/phase/reading/referee footer, and returned
`VALID_WINDOWS_EXIT=0`.

### Deliberately invalid Git environment

The reviewer constructed a child environment by removing every `PATH` entry containing
`git.exe` or `git.cmd`, confirmed `Get-Command git` returned false, and invoked the same
exact `state.ps1` blob with native Windows PowerShell.

Result:

```text
GIT_PRESENT=False
YELLOW state report failed: The term 'git' is not recognized ...
INVALID_GIT_EXIT=1
```

The incomplete path printed no normal Referee completion footer. The parent environment
was restored in `finally`. This reproduces Order 045's failure mechanism and proves the
script fails closed rather than inheriting or resetting a successful native exit.

## Order 046 — deterministic local-review seed

This reviewer personally executed `tests/review-seed.integration.test.ts` during the
same-commit Wave A 13-suite gate. The runner recreated and migrated a dedicated isolated
database, forced `YELLOW_REQUIRE_REVIEW_SEED=1`, supplied the exact database URL and
non-production password, and returned **11 pass, 0 fail, 39 assertions** with no skips.

The reviewer inspected every Order-046 assertion:

- two deterministic property-scoped requester/approver identities, two unit types, five
  rooms and five sellable units are exact;
- each inventory aggregate joins one exact fact to one outbox event by aggregate and
  correlation/request id;
- an identical second run is a byte/count no-op and reports all objects as existing;
- changed requester or approver passwords and a shared secret fail atomically while the
  canonical hashes remain valid;
- requester and approver login tokens bind distinct users, the exact tenant and the
  exact least-scope permission set;
- the property list is exact and truth availability returns exactly five real bookable
  options.

The later Order-077/078 assertions in the same file were used only by their owning waves.

## Order 089 — strict calendar boundary

Reviewer-executed command:

```bash
bun test tests/operator-calendar-validation.test.ts
```

Result: **4 pass, 0 fail, 16 assertions**.

Assertion fidelity:

- February 30 is rejected with HTTP 400 at all seven shared consumers before the test
  transaction handle can execute;
- April 31, `24:00`, leap seconds, invalid minute offsets and offsets beyond ±14:00
  fail closed;
- valid leap-day, precision and exact ±14:00 boundaries parse and proceed to the stubbed
  service boundary;
- invalid local projection dates return 400 while a valid leap date proceeds.

This proves rejection by calendar validity rather than endpoint-specific patches or
JavaScript date normalization.

## Order 090 — portable AI proposal boundary

Reviewer-executed command:

```bash
bun test tests/rate-intent-provider.test.ts tests/rate-intent.test.ts
```

Result: **15 pass, 0 fail, 116 assertions**.

Assertion fidelity:

- omitted provider selects the deterministic local adapter and the injected network
  function is called zero times;
- external selection rejects unknown providers, missing/unsafe endpoints, embedded
  credentials/fragments, invalid model/auth/deployment labels and timeouts outside
  500–30000 ms; exact loopback HTTP without an API key is the bounded on-prem exception;
- outbound provider input contains only intent plus the minimized current proposal
  command, excludes tenant/property/actor/token/approval/availability authority and
  does not serialize the API key into prompts;
- API-key authentication stays only in the authorization header; no-key configuration
  emits no authorization header;
- malformed/oversized/hostile candidates, provider errors and timeouts return generic
  unavailable/refusal results without secrets, configuration or provider output gaining
  persistence, approval or publication authority;
- service guardrails run before adapters and reject secrets, card data, executable
  formulas and automatic-publication intent;
- architecture assertions distinguish this proposal inference from deferred RMS,
  training and autonomous authority.

## Supporting UI/HTTP inspection

Same-commit database suites already executed by this reviewer prove authentication and
property/scope authorization precede mutations; malformed requests return bounded 400s;
publisher/database failures return generic 503s without embedded secret text; browser
assets use same-origin requests and contain no local/session storage, cookies or direct
domain authority. Reservation-offer and rate-intent HTTP proofs remain finally owned by
Waves B and C. Operator mutation files remain finally owned by Waves A/B. No ownership
is reassigned here.

## Scope and verdict

The implementation clone remained clean. This review changed no implementation, test,
migration, manifest, decision or order. Only this review record and its one ledger line
are reviewer-authored continuation metadata.

No findings remain. Wave D's exclusive orders are **APPROVED** at `4874f5c`. Together
with the recorded Wave A–C reviews, every Order-045–091 manifest row now has an approved
exclusive owner; absent Orders 087/088 remain sequence gaps rather than review rows.

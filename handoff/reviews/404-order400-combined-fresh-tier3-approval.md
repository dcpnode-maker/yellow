# Orders 400-404 — fresh independent Tier-3 review

**Verdict:** APPROVED-CLOSED-D1188

**Reviewed candidate:** `b6dacc57011e7e48d6fe007a74087710bbd6409a`

**Substantive base:** `417c84d`

**Reviewer:** `/root/order404_fresh_tier3_review`, fresh independent
non-implementing Tier 3

## Findings

No blocking finding remains. The combined candidate closes the previously reproduced
catalogue-frontier, permanent-hostility, Section14/calendar ancestry, governed-payment
truth and replay-child ambiguity defects without widening the 51-argument capability,
the three-table schema or its restricted authority surface.

Migration 0069 reconstructs the complete approved Section14 predecessor graph from
persisted typed rate, extension, classification, component, service, recipient,
payment, invoice, proviso and governed-calendar evidence. It compares the exact
reconstructed hash before writes, applies the approved payment-date rule, qualifies
both replay child aliases and preserves deterministic exact replay, divergent
SQLSTATE `23505`, same-key serialization, forced RLS and insert-only ancestry. The
exact migration checksum is
`622AED18C22BAF6793CC8FF7AC8DEA8B9AF4C1688CED5DC341F21B6F3305688E`.

The exact `417c84d..b6dacc5` delta contains only the admitted Orders400-404 migration,
permanent proof/oracle/snapshot and governance paths. No unrelated product source was
changed.

## Reviewer-personal execution

I read `PROJECT.md`, current state, Phase 7, Orders400-404 and paused Order367,
D1174-D1187, both prior reviews, roster/workflow and the complete Yellow PostgreSQL,
compliance and entity skills. I did not implement the candidate.

Using isolated disposable PostgreSQL **16.15** resources with
`pg_stat_statements` preloaded, randomly rotated deployment/runtime/registrar
credentials and no retained `.yellow` access, I personally obtained:

- official `setup.sh --db-only`: migrations 1-69, 119 current tables and
  **11 passed, 0 failed of 11** referee;
- exact catalogue **69/119/109/109/18/2**;
- permanent Order400/402 matrix plus intentional-red contract: **22 pass, 0 fail,
  1,031 assertions**, covering the 366/367 bound, every governed-calendar field,
  stale/foreign/forged selectors, dense night/component gaps and duplicates, exact
  and divergent replay (`23505`), concurrent same-key arbitration, `pg_temp`, RLS,
  raw UPDATE/DELETE denial and zero-write censuses;
- all **39** distinct migration-regression cases green with **182 assertions**. The
  full run proved 38 cases; its authentication-negative case was invalid only under
  the disposable server's temporary host-trust configuration, so I switched that
  isolated host to SCRAM and personally reran only the still-unproved case, which
  passed 1/1 with 5 assertions;
- canonical database acceptance **23/23 (65)**, seed **10/10 (63)**, app-role
  non-login containment **5/5 (25)** and runtime authority **10/10 (88)**;
- normalized fresh schema byte-equal to `tests/schema/expected.sql`: **781,902
  bytes**, SHA-256
  `2658fcd106d5929eb1656280e7bb132021009bfc45b0b49eddb382329739649d`;
- standing suite **1,302 pass, 1,002 expected skips, 0 fail, 19,427 assertions**;
- current catalogue oracle **1/1 (6)**, strict typecheck, 144-file import boundary,
  23-package licence policy, dependency audit with no vulnerabilities, merge-base,
  exact diff and whitespace checks: green.

Earlier attempts against a vanished WSL lifecycle resource, the two-tenant referee
fixture and a baseline-only database were invalid environment/fixture selections and
are not counted as product evidence. Each required gate was then executed on the
correct fresh migration-only, canonical-seed or fully migrated database and passed as
reported above.

The disposable database/container and detached exact-head review tree were removed
after proof. The stable loopback port-3000 app and its Order311 PostgreSQL/provider/
Valkey services were rechecked and remained untouched. No product, source, test,
migration, schema, stable database, local app, deploy, merge, push or credential
mutation was performed by this review.

Orders400-404 are approved and closed. Order367 is explicitly authorized to resume
from exact approved head `b6dacc5` as migration 0070 within its existing D1174
boundary; this does not authorize local promotion, deploy, merge, push or Phase 7
completion.

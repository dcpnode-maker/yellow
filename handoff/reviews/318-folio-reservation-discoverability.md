# Order 318 fresh Tier 2 review

**Disposition: APPROVE**

**Reviewer:** Codex, fresh non-implementing Tier 2 reviewer

**Candidate:** `e46af12016ea0b0811315619f0344a5482c4930c`

**Governance head reviewed:** `429df962725dcf1bade49a427dda6c36176976a8`

**Approved base:** `d1b9cc7`

The earlier WITHHOLD was an isolated-harness construction failure, not a product
finding. I rebuilt the disposable proof from ordinary files (no encoded script
transport), personally executed the required browser matrix, and approve the exact
unchanged candidate. No product finding remains.

## Revision and scope

- `git rev-parse HEAD` returned the exact governance head above and
  `git merge-base --is-ancestor e46af120... HEAD` exited 0.
- The reviewed product delta remains one eligibility-qualified paragraph, one quiet
  semantic button, three bounded presentation rules, and one handler that calls the
  existing dirty guard before canonical Reservations navigation and heading focus.
- It adds no endpoint, request helper, identity, authority, mutation, database,
  statutory workflow, status claim, or post-310 capability.
- I did not access, restart, replace, or mutate port 3000. The disposable server used
  `127.0.0.1:3318` and stopped after proof.

## Personally executed isolated-browser proof

I served the exact candidate HTML/CSS/JS from a disposable Bun server with bounded
in-memory login, two-property, empty reservation-board, and folio-statement responses,
then drove installed Chromium through the DevTools protocol.

- Both properties were exercised in Simple, Advanced, and Expert: all six combinations
  exposed exactly one `Find via reservation` control and exactly one described copy.
- In every combination, an empty Folios lookup navigated to the selected property's
  canonical `/reservations` route, focused `reservations-title`, and browser Back
  restored that property's canonical `/folios` route.
- A clean loaded `FOL-318` workspace retained exactly one bridge; activation navigated
  canonically to Reservations and Back restored the exact singular folio route.
- With an unfinished folio-organize reason of `retain draft`, dismissal retained the
  exact `/folio/<id>?tab=postings` route, draft value, bridge focus, and one confirmation.
  Acceptance then navigated exactly once to Reservations; the confirmation count moved
  from one to two and no duplicate history navigation occurred.
- Direct human-readable lookup remained canonical, resolving `FOL-318` to the exact
  singular folio route with `?tab=postings`. Existing contextual reservation/departure
  return precedence remained green in the adjacent permanent navigation proof.
- At 375 portrait, 812x375 landscape, and a 640 CSS-pixel / 2x scale proof, the bridge
  stayed visible and horizontal overflow was zero (the landscape scrollbar measurement
  was -15, meaning the document was narrower than the viewport).
- Apple, Android, Win95, Glass, Neo, and ERP each retained the visible bridge and exact
  root theme identity. Reduced-motion and forced-colors media queries both matched.
  Keyboard focus landed on `folio-find-via-reservation`.
- Browser console warnings/errors: **0**.
- Business mutations: **0**. Bridge activation introduced no bridge-specific network
  call; it caused only the existing bounded reservation-board GET owned by the reused
  Reservations view. Login POSTs were harness authentication only.

The complete disposable request log contained only static assets, harness login,
property grants, existing bounded reservation-board GETs, and bounded folio-statement
GETs. There was no charge, correction, transfer, settlement, creation, or other
business write.

## Re-executed permanent proof

Command:

`bun test tests/operator-folio-reservation-discoverability.intentional-red.test.ts tests/operator-management-demo-navigation-finetune.intentional-red.test.ts tests/operator-folio-workbench.integration.test.ts tests/operator-reservation-workspace.integration.test.ts tests/operator-appearance-geometry.test.ts tests/operator-adaptive-experience.test.ts`

Result: **52 pass, 6 database-gated skip, 0 fail, 796 assertions**, including installed-
Chromium geometry at contract widths, dirty-family guards, direct/contextual folio
history, explicit shell routes, six appearances, and responsive/accessibility rules.

## Approval boundary

**APPROVE** exact candidate `e46af12016ea0b0811315619f0344a5482c4930c`.
This approval is limited to Order 318's presentation/navigation bridge. It grants no
local refresh, deployment, downstream product, financial, statutory, database, status,
or post-310 authority.

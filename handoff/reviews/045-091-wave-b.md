# REVIEW 045–091 — Wave B tenancy, occupancy and reservation transitions

**Reviewed by:** OpenAI Codex independent non-implementing reviewer
(`/root/review_order_092`) · **Date:** 2026-08-23

**Implementation commit:** `4874f5cd8052435d5c5d2f67698f6088cff502fe`

**Review-plan commit:** `d50517c6ba3e99412cb005b6e189cc298f5067b3`

**Verdict:** **APPROVED**

## Exclusive discharge scope

This review approves only Wave B's exclusive owners: Orders **047, 049, 053–056,
058–062, 080–082 and 084–086**. Wave A, C and D ownership remains separate.

## Environment and method

The reviewer executed every proof against the clean native-Linux implementation clone
`/home/astha/projects/yellow-order-091-final-5d3f137` with Bun 1.3.14. Before execution:

```text
git rev-parse HEAD
4874f5cd8052435d5c5d2f67698f6088cff502fe

git status --porcelain=v1
<empty>
```

Every database-gated file received its fixed `yellow_review_NNN` database. The
`fresh_db` helper force-dropped only that named disposable database, created it anew,
and applied migrations 0001–0006 before its one proof file ran with the exact
`YELLOW_REQUIRE_*` fail-closed flag. No named assertion was skipped.

## Reviewer-executed results

| Command | Owned order | Result |
|---|---:|---|
| `bun test tests/reservation-state-machine.test.ts` | 080 | 5 pass, 0 fail, 131 assertions |
| B0 `tests/idempotency.integration.test.ts` | 047 | 6 pass, 0 fail, 44 assertions |
| B1 `tests/operator-restrictions.integration.test.ts` | 049 | 6 pass, 0 fail, 39 assertions |
| B2 `tests/operator-operational-blocks.integration.test.ts` | 053 | 7 pass, 0 fail, 42 assertions |
| B3 `tests/operator-oos-policy.integration.test.ts` | 054 | 6 pass, 0 fail, 28 assertions |
| B4 `tests/operator-holds.integration.test.ts` | 055 | 7 pass, 0 fail, 48 assertions |
| B5 `tests/hold-expiry-worker.integration.test.ts` | 056 | 6 pass, 0 fail, 30 assertions |
| B6 `tests/availability-projection.integration.test.ts` | 058 | 6 pass, 0 fail, 45 assertions |
| B7 `tests/availability-projection-consumer.integration.test.ts` | 059 | 6 pass, 0 fail, 30 assertions |
| B8 `tests/operator-projection-bootstrap.integration.test.ts` | 060 | 6 pass, 0 fail, 50 assertions |
| B9 `tests/availability-scaling.integration.test.ts` | 061 | 2 pass, 0 fail, 19 assertions |
| B10 `tests/offline-leases.integration.test.ts` | 062 | 6 pass, 0 fail, 70 assertions |
| B11 `tests/reservation-commit.integration.test.ts` | 081 | 5 pass, 0 fail, 106 assertions |
| B12 `tests/reservation-commit-http.integration.test.ts` | 082 | 5 pass, 0 fail, 61 assertions |
| B13 `tests/reservation-offers.integration.test.ts` | 084 | 6 pass, 0 fail, 76 assertions |
| B14 `tests/reservation-lifecycle.integration.test.ts` | 085 | 5 pass, 0 fail, 62 assertions |
| B15 `tests/reservation-segment-changes.integration.test.ts` | 086 | 6 pass, 0 fail, 108 assertions |

B9 additionally reported bounded work `n250=2012`, `n500=5497`, ratio `2.732`.

## B13 precondition correction and restart

The first execution of the then-published B13 command recreated and migrated
`yellow_review_084`, but stopped before named assertions with:

```text
Canonical launch seed is absent; run bun run db:seed first
0 pass, 1 fail
```

No approval was written and B14/B15 were not run. The review plan was corrected at
`d50517c` to declare the existing Order-084 launch-seed precondition. The reviewer did
not reuse the failed database: B13 force-dropped and recreated `yellow_review_084`,
reapplied migrations, then personally executed:

```bash
DATABASE_URL=postgres://yellow:yellow@127.0.0.1:5442/yellow_review_084 bun run db:seed
YELLOW_REQUIRE_RESERVATION_OFFERS=1 \
YELLOW_RESERVATION_OFFERS_URL=postgres://yellow:yellow@127.0.0.1:5442/yellow_review_084 \
YELLOW_RESERVATION_OFFERS_PASSWORD=YellowReviewOnly2026! \
YELLOW_RESERVATION_OFFERS_APPROVER_PASSWORD=YellowReviewOnly2026!Approver \
  bun test tests/reservation-offers.integration.test.ts
```

The fresh restart passed 6/6. B14 and B15 then ran from their own fresh databases.

## Assertion-fidelity inspection

### Tenant isolation and fail-closed authority

- B0 proves identical idempotency keys are tenant-local and that `app_role` cannot
  forge tenant access; the negative path returns SQLSTATE 42501 and the migration proof
  checks exact RLS, grants, constraints and tenant-leading index shape.
- Operator proofs execute missing-scope, wrong-property, foreign-tenant and malformed
  requests and compare domain, fact, outbox and idempotency counts before and after.
- Projection replacement is granted only through its tenant-local function; the proof
  checks `app_role` lacks direct projection and occupancy delete authority.
- Reservation HTTP/lifecycle/segment proofs reject foreign tenant/property, illegal
  state, stale value, caller-owned identity and malformed-reference paths before leaks
  or durable artifacts.

### Occupancy choke point and concurrency

- The same reviewer personally executed TC-12.4 at the exact implementation commit in
  Wave A: direct `space_occupancy` INSERT as the application role failed with 42501.
- Source assertions forbid direct reservation/HTTP/browser occupancy SQL and route test
  setup through `record_occupancy()` / `release_occupancy()` where an administrator must
  establish a competitor.
- B2 and B4 execute twenty-way OOO/hold contention and require one winner, one canonical
  claim and artifact-free losers. B10 does the same for offline leases.
- B11 requires one concurrent hold-conversion winner while preserving exclusive
  occupancy. B12 requires two last-unit HTTP commits to produce exactly one durable
  winner. B14 reinstate and B15 room-change races each require one winner and exact
  rollback of losing/conflicting work.

### Fact/outbox atomicity, replay and rollback

- Successful mutation proofs count exact facts and outbox events alongside domain and
  occupancy rows; exact idempotent replay is byte-equivalent.
- Injected publisher failures at the first, middle and final publication boundaries
  prove domain, occupancy, facts, outbox and idempotency claims roll back together, then
  the same key succeeds on a clean retry.
- Projection-consumer failures roll projection, cursor and processed markers back as
  one unit; concurrent drains serialize and replay is byte-equivalent.

### Reservation transitions and offer truth

- Order 080 mechanically matches every immutable-schema reservation status and every
  documented transition/event, freezes the registry and fails closed for all absent
  edges.
- B11 converts held occupancy atomically; B12 uses the same canonical command for held
  and direct commits and bounds positional-capacity retries.
- B13 proves published two-night offers contain exact money and attributable evidence,
  live PostgreSQL occupancy/restrictions override favorable cached projection, and a
  stale-low projection cannot block fixed truth. Reads remain deterministic and linear.
- B14 proves confirmation freezes policy, cancel releases exact claims, non-zero/legacy
  policy requires an exactly bound different-operator waiver, and reinstate re-arbitrates.
- B15 proves extend/shorten re-arbitrate exact claims, move closes immutable old history
  and opens the next sequence, and occupied/OOO conflicts leave no partial transition.

## Scope and clean-tree confirmation

The implementation clone remained clean after B15. This review changed no product
source, test, migration, manifest, decision or order. Only this review record and its
single ledger entry are reviewer-authored continuation metadata.

## Findings and verdict

The B13 matrix precondition omission was corrected before approval and its proof was
restarted from a newly created database. No implementation assertion failed. Wave B's
exclusive orders are **APPROVED** at `4874f5c`; all other manifest ownership remains
UNVERIFIED until its assigned reviewer wave completes.

# Order 184 independent real-browser acceptance

conclusion: APPROVED — exact product `3cffb5fa3254d951d6130dff597f5d616b15c2a1` is independently approved after the corrected static review and a real authenticated Chrome matrix over the sole healthy loopback3000 API/database. No product, local service, database or credential file was changed.

evidence: The browser fulfilled only the operator document, `/assets/operator.css` and `/assets/operator.js` from the exact checked-out product; all authentication and same-origin API requests continued to the sole healthy `http://127.0.0.1:3000`. All sixteen skins produced sixteen unique computed material/layout signatures at each 375/768/1024/1440 viewport. Every one of 64 combinations had zero document/body overflow, zero visible control below 44x44, a visible 3px theme-selector focus outline, unchanged property/route/reservation view/expanded-workspace/unsent date draft, and property-before-navigation DOM/focus/visual order. Glass computed real `blur(24px) saturate(1.6)` navigation and `blur(22px) saturate(1.55)` cards with translucent layers/highlight/depth; forced colours removed shadows/filters across all sixteen; Aurora reduced motion computed `animation-name:none`; exact solid Glass fallback declarations rendered opaque readable surfaces with no filter. Personally inspected 1440 Yellow/Glass/Win95/Neo/Skeuo/Material, 375 Glass, DPR2 zoom-equivalent Yellow and solid-fallback Glass screenshots; their materials and compositions are visibly distinct and contained. Console errors, page errors and failed requests were all zero.

files_and_lines: Product `src/http/operator/index.html:91`, `src/http/operator/operator.js:419`, `src/http/operator/operator.js:5708`, `src/http/operator/operator.css:995`, `src/http/operator/operator.css:1327`, `src/http/operator/operator.css:1354`, `src/http/operator/operator.css:1425`, `src/http/operator/operator.css:1439`, `src/http/operator/operator.css:1447`; prior independent static record `handoff/reviews/184-material-theme-skins-corrected-independent.md`; this additive browser record and `handoff/LEDGER.md` only.

tests_or_checks: Static proof remains focused 12/12 (716), standing 241 passed plus 481 database-gated skipped and 0 failed (3,512), typecheck, 66 boundaries, 23 licences, audit, contrast and gzip 97,940/98,304. Browser command used bundled Playwright via `NODE_PATH=C:\Users\astha\.cache\codex-runtimes\codex-primary-runtime\dependencies\node\node_modules`, installed Node `C:\Program Files\nodejs\node.exe` (the advertised bundled `node\bin\node.exe` path was absent), installed Chrome `C:\Program Files\Google\Chrome\Application\chrome.exe`, and temporary harness SHA256 `E555A82FA8625A2E5C327B1E631D145F1EDA61A30DC9AA9FC5930DBA76C6E746`. Exact matrix result SHA256 was `9339190AD61A9742B369EA3F0EF724E15D45CEF8133784EFC5B74A0168B332A3`. Chrome retained standards-level backdrop support even with disable-feature flags, so the unsupported-engine check applied the candidate's exact `@supports not` declarations as a browser simulation after statically proving the guard. The 200% reflow check used a 375-CSS-pixel viewport at DPR2, equivalent to a 750-physical-pixel viewport at 200%, and passed zero overflow/zero small targets with focus and state preserved.

risks: Only 364 reviewer-measured gzip bytes remain, so the next operator asset change must re-run the exact ceiling. Chrome cannot natively turn off its standards-level backdrop-filter implementation, and browser UI zoom is not exposed to Playwright; the fallback and 200% checks therefore use the explicit simulations described above rather than mislabeling them as native unsupported-engine/browser-toolbar actions. Those limitations do not leave a product defect: the exact fallback guard is statically valid, the exact fallback values rendered legibly, and the stricter reflow layout passed in a real engine.

recommended_parent_action: Mark Order184 independently approved for exact product `3cffb5f`. The coordination owner may update governance and replace only the existing sole loopback3000 app image over the preserved Order182 database/credentials, then smoke-test the promoted exact asset hashes and health. Do not broaden this approval to API/schema/data changes, a second local, public bind, merge, push or production deployment.

# Order460 — current-source publication and retained local promotion

## Current disposition — 9 September 2026

Exact-source publication and CI are complete. Retained-data promotion and local
runtime acceptance are **not complete**. This record does not approve a live
upgrade, startup, merge, provider activation or Phase7 completion.

## Published source

- Primary owner: Codex `/root`.
- Commit: `547cf3d335afdcec7ae4b7d05517a2153b342d0e`.
- Tree: `8ca1e16cdae35d6dfe753a1f262789cbe369d87b`.
- Existing branch: `phase-7/operator-invoice-workflow`; existing draft PR92.
- Ordinary non-force push, no merge. The selectively published76-path candidate
  preserves unrelated staged/working changes and excludes paused445 picker work.
- Independent nonimplementer inspected the source projector and publisher and
  personally executed the relevant pure/native proofs. Root executed publication.
  Private completed receipt: `.yellow/evidence/order460/current-publication-r5/completed.json`.

The published source contains current frontier90 fiscal functionality, accepted
454/455 fixes, and eight distinct selectable light layouts with five workflow
navigation groups. Source/UI tests are not founder visual acceptance.

## Executed acceptance

- Exact candidate standing:2019 passed,1472 explicit existing DB/platform skips,
  zero failed;40893 assertions/556 files/196.45s. Typecheck,194 module boundaries
  and dependency-license checks passed. Validation artifact and dependency state
  were restored; earlier RED attempts remain recorded.
- Exact GitHub CI34300623098: all six jobs succeeded (quality,windows-state,
  local-review,database,container-smoke,free-host-arm64). Root read the run and
  actual database job102307049964 log. The receipt80-to81 lane reports17 passes;
  its already-current lane separately reports16 passes. The canonical referee
  reports11 passed,0 failed. CodeQL and all three Analyze checks also succeeded.
- Order459 actual Chrome synthetic-source proof:1 pass/1143 assertions; focused
  UI27 passes/588 assertions and geometry5 passes/49 assertions.
- Discovery454 SQL10 passes/253 assertions and signed HTTP5 passes/66 assertions
  were executed independently; Order455 native16 passes/one upgrade-only skip/
  zero failed/4177 assertions remains distinct from the green exact-head CI upgrade.

## Retained local and space work

The retained serving database is `yellow_order444_review_a10851786f17` on the
existing native PostgreSQL55503 cluster. The retained source is a108517/frontier85;
its stopped supervisor receipt remains preserved. App7568 and supervisor9508 were
the former identities. No source-only receipt implies that this data is migrated.

Independent relocation-r3 Review passed all ten historical evidence inventories,
native metadata/ACL/ADS/link/process checks and space floors. Its sole Execute
attempt failed in the first copy before any source rename, junction or removal:
all ten C: sources remained intact, no staging paths existed, and only an empty
first destination existed on D:. The same copy failure was reproduced on an owned
two-file fixture. Captured native output established ERROR5 during Robocopy security
metadata copying despite an exit code of zero. DAT-only copying succeeded on that
fixture; explicit file metadata restoration is being verified separately. R4's
synthetic attempt then failed on an imported-function empty evidence-root binding;
R5 corrected that and reached a file-metadata identity mismatch. Both failed proof
destinations are retained, and no real relocation retry has occurred. Neither
MAX_PATH nor a product/database fault is established as the cause. No broad cleanup
occurred. A fresh C: observation fell below the ten-leaf plan's required final floor;
additional completed evidence is being inspected, without lowering that floor.

Root's subsequent owned-fixture diagnostic proved exact descriptor preservation:
DACL-only `SetFileSecurityW` passed in3030ms using helper
`abe44f515e817f1bb66b8d16f6875b8412c182f969e4e42e760a53e643e9db7d`.
Only two fixture destination files needed that metadata action; all ten entries
then matched raw security bytes/control/revision/SDDL/owner/group, and non-security
fields plus the complete original source inventory remained unchanged. No privilege
change or production relocation occurred. The earlier OWNER/GROUP/DACL API attempt
failed and remains recorded separately; its exact native error code was not captured.

R6's complete two-file/long-path synthetic copy then passed root execution in
3583ms (helper8642294b/synthetic a722a41f; native receipt
`97215ed70bf61aa06aace353f9fd0fd8ac15f9f059bc47fd3aefd12bc22f6ec1`).
All original fixture bytes and earlier failure destinations stayed intact.
The first real R6 **Review**, not Execute, stopped on the first partial destination's
root metadata. Read-only inspection confirms that the retained R3 destination is
still empty and ordinary, with the old copy's creation-time/ACL mismatch; no source
was renamed, copied or removed. A surgical empty-destination metadata repair is
being prepared; the full source/destination identity checks remain unchanged.

The separately frozen empty-root repair2cf60607 passed root's read-only Review.
Its sole Execute restored the exact creation/write times and ACE contents but
failed its final identity check: the source has DACL control33796 (`D:AI`), while
the still-empty destination has32772 (`D:`). The raw DACL API used successfully
for the earlier opposite-direction fixture does not establish this case. Root
personally recaptured both native states; destination length/allocation remain0,
children0, and all C: source contents remain present. No real relocation Execute
has run. The failed repair is retained for a focused metadata diagnostic, not
declared complete or blindly retried.

Root then fully inspected and executed the separately frozen managed-ACL
diagnostic8b657f15: Review passed; its one Execute failed the ACE equality check.
Immediate read-only recapture explains the two earlier outcomes: managed Set-Acl
restores `D:AI`/33796 but substitutes D:'s parent-inherited ACEs, whereas the old
raw API restores the ACE list but clears `AI`. The destination remains empty,
ordinary and timestamp-identical, with unchanged owner/group; C: remains intact.
A bounded correction of the exact owned empty destination container's inheritance
is being prepared. Neither relaxing the original security oracle nor repeatedly
running the failed helpers is admitted.

The separate parent-alignment0320c692 passed root Review. Its Execute restored the
six required grants and correctly propagated a byte-identical child descriptor,
but its final parent literal ACE-order assertion was RED. Root's independent
read-only tuple comparison proves the same six SID/right/inheritance/propagation/
allow grants, protected parent and unchanged owner/group; only all-allow order
differs. No parent-wide permission broadening or source mutation is claimed.

Root independently inspected the R7 exact delta from fully reviewed R6 and ran
its full real Review green: all ten inventories match, projected/free-volume
checks pass. Its one actual Execute stopped before source rename/removal on
`capture-profile-1788773623488/component_crx_cache/23759e3f6d0119a00038b963cec9fb7abafd7b65a35a7e83c7697511db59e6e7`'s
raw descriptor. Preserve that partial destination and original prototype. Q246
now admits preparation of a nine-leaf successor that leaves this historical
browser-profile leaf deferred, rather than making it a runtime prerequisite.

The independent native-capture reviewer personally ran27 pure tests/126 assertions
and the credential-free Review action successfully. The separately admitted sole
Capture85 attempt then failed on a PostgreSQL query syntax error before returning
a snapshot. Its protected53-byte RED log is retained (SHA256
`a7f419a0d732e944babec2ea6d2c58d2cb5c040cf3b612d9a20254e41f607c28`).
Capture90 was not run in that failed attempt. This was read-only SQL; it did not
migrate, clone or change hotel data. The separately named r2 successor with explicit
SQL aliases and query-stage diagnostics subsequently passed independent native
execution, once each, after root admission. Root personally inspected both receipts:

- Retained serving85:128 tables,6666 catalogue keys,58 companions,zero other
  sessions; exit0 in86.0s. Receipt SHA256
  `c622fae10531047672492a8e2daed7ae14f5181c6005e47b11b23aeb652260db`.
- Separate clean90 reference:129 tables,6793 catalogue keys,58 companions,zero
  other sessions; exit0 in83.3s. Receipt SHA256
  `7638af13c54e6175663090f679071c0f1cad7fa24807b3ed127d0d911ec7f080`.
- Both retain identical before/after global digest
  `f66538cd0af1aee9753d93f9f0c787d92f2ea7dd8f58c9ef57cb1ae472fdaa21`
  and companion digest
  `2f6321d0317477f8d0e674dd1384421164a25a765353ac40e78e245c6c9a42e3`.
  Database owner/schema owner/locales and sequence definitions agree. The
  distinct retained sequence values65/38 are recorded, not normalized away.

Protected receipts are under
`D:\Yellow\temp\order460-retained-serving-capture-r2-20260909`.
The r2 helper SHA256 is
`1e3d0f3cbe0d22edf6143ddb9ee7a4684f5b969cbbb879ccf615936d9cccadcc`;
wrapper SHA256 is
`ad9abd0e40a3ee464d7948261c183732592b1f767757aaa3937bc10e9eb7eae3`.
These are read-only captures, not a recovery copy, migration or runtime launch.

Root inspected the immutable-source artifact helper and personally passed8 pure
tests/33 assertions. No source-artifact Review/Execute or runtime preparation is
claimed until the space prerequisite and exact subsequent handoff are satisfied.

Private pure worker/recovery classifiers are preparation only. Availability proof
still requires trusted raw PostgreSQL timestamp/row/cursor/event evidence; normalized
hashes or a claimed complete-interval flag cannot authorize convergence or launch.

Root's full review of the frozen recovery workflow found two pre-execution defects:
its PowerShell parameter named `$args` lost every psql argument (personally proved
with the exact function and a harmless native stub), and its source-artifact ACL
reader expected a one-principal protected file despite the producer's three-principal
directory/inherited non-secret receipt contract. A separately named successor is
required; no recovery database or migration was attempted. Historical credential
ACL checks must remain current-user-only.

Root independently executed the corrected recovery/migration r4 credential-free
Review successfully (wrapper
`544f63d3ab3d48912aea8ce38e1f98407d7da6f2c1c1e8e3465dc215faa23bff`).
R2's pure/native-stub tests pass14/0(90), but its actual Review failed on the
PowerShell method-call replacement expression. R3 fixed that and the author's
native Review exposed a missing transitive `Get-Sha256Hex` import; r4 includes
that exact existing function. All predecessors are retained. These are helper
readiness results only; recovery creation, migration and runtime remain unexecuted.

Root also personally executed the frozen r4 verifier/static/native suite:
17 passed,0 failed,98 assertions in5.74s, including the actual credential-free
Review and harmless native psql-argument stub. This closes the helper's preflight
defects, not its space/source-artifact prerequisites or database execution.

## Remaining acceptance

**Current actual checkpoint — September9.** Source-artifact R3 completed with
receipt165f9a35. R5 Recovery personally executed by root and passed equivalence:
capturea4a6cbe3, verification7b64afd7, equal semantic/content06533290. R5 Migrate
then applied86–90 (backend11924, all five transaction PIDs identical), and the
second runner returned applied0/no-op (backend424). Serving90 capture07d54c2c
and migration receiptb7ac9b53 are retained. Final verification failed closed:
catalogue mismatch against clean90; failed receipt222e56ee remains untouched.
Root's independent captured-data comparison confirms all original business table
row hashes unchanged, only schema_migration85→90, all sequence state/database
properties/cluster roles/companions equal. Catalogue diagnosis remains outstanding;
no app start, automatic rollback or second migration attempt occurred.

Semantic capture R4 b30e14c8 corrects the missing imported-helper temporary root
and pins transitive legacy audit source before evaluation. Independent Windows
review and root personally pass native synthetic4+5 checks and credential-free
Review; root total6.58s. These are helper checks, not live worker acceptance.

R8 actual root execution is GREEN: all nine scoped leaves reached Complete with
exact destination inventories and junctions,517,138,912 allocated C: bytes
released; C:3,848,663,040 and D:23,780,794,368 bytes free at final observation.
All nine bounded native copies returned1 with no native error/timeout. Deferred
continuum source49395824/destination261f6896 retained identical payload1cf49f5d
and remained untouched. This is nine complete plus one deferred, not ten complete.
Old C: duplicate staging copies were removed only after full destination proof;
all content remains recoverable through D: and the original junction paths.

Root then personally passed source-artifact R2 Review (7.78s) with exact published
547/tree8ca1 and unmodified index/refs/dependencies. Its admitted Execute stopped
on a generated PowerShell ACL parser error before SetAcl/archive/source creation.
Only the new control directory and1,238-byte failed.json exist; source is absent.
Preserve this failure. A separately named successor must parse/test the entire
embedded filesystem script and resume only this exact owned failure residue;
no runtime, database, credential or recovery operation occurred.

Root independently read the current-runtime adapter/control and the imported exact
startup/stop functions, then executed the runtime and recovery R5 suites:25 passed,
0 failed,227 assertions in12.64s, including both credential-free native Review
paths. R5 a20a8ed8 differs from accepted R4 only by the admitted3GiB C: floor.
The source-artifact R2 read-only Review then stopped at the storage-floor check;
no source/control directory was created. The runtime HTTP proof still needs a
successor: its historical readiness assertion expects `ready` with no calendar,
which the published current discovery contract correctly does not return. Preserve
the frozen package and this finding; no Stage is admitted on that package.

Native capacity check before recovery admission: the entire existing cluster's
base directory contains64,492 files /1,348,654,489 logical bytes, versus
23,780,786,176 D: free. The one serving-database copy is smaller than that whole
cluster bound. No backup is created by this read-only measurement. Root also
personally passes the final exact-micro semantic auditor tests25/0(136),194ms;
the same-transaction native capture and live worker proof remain separate.

1. Prove and execute the bounded historical-evidence relocation, preserving paths
   through exact junctions and every retained byte; recheck runtime space floors.
2. Prepare the exact published immutable source artifact, not dirty working bytes.
3. Capture retained data and companions; create and prove one named private recovery
   copy before applying exactly86–90 through the unchanged production runner.
4. Verify retained business rows, ledger prefix, full catalogue/sequence state and
   exact second-run no-op. A committed partial migration leaves the app stopped.
5. Independently prove normal-worker convergence and protected data, then test actual
   authentication, assets and journeys before one local3000 is presented to founder.

No founder action is currently required for these technical preparation steps.

## Restart-bound runtime and semantic successor review — 9 September 2026

Independent non-implementing reviewer `/root/restart_runtime_proof` approves only
the bounded `Prepare` retry of restart controller
`8c940aa44d982c2cf26f9824790199575ce435da5a59753e4f21cf3a28be1900`.
This does not approve `Stage`, `StopStage`, `Promote`, SQL/database access, a
runtime start/stop, process termination, or local completion.

The frozen semantic TypeScript source
`8995464284089087641cf814d520f2f3a5933da0702eb91fb8a14472b0f60554`
and restart successor
`96c1bfe96ad772e1a0fa75bc1e9a8aa9bad46bf9637b676217e9b19f9f77260d`
were compared byte-for-byte. The successor contains only the explanatory header,
two PID15956→9880 substitutions, the one exact PostgreSQL start-time substitution,
and `.simple()` on the sole existing three-command read-only transaction setup.
Bun1.3.14's installed `sql.d.ts` exposes `Query.simple()` and its bundled docs
require it for multi-statement queries. All other SQL, source/query pins, raw data
contracts and semantic-auditor assumptions are identical; the fixed60-second
baseline-to-settled proof window remains.

Review initially found and withheld approval for four executable defects: the
runtime controller passed only four of six shared host-guard parameters and lacked
the shared helper's app-branch bindings; both semantic wrappers executed the shared
helper before hashing it; the real final receipt's exact
`clean90CatalogueSha256` field was absent from the frozen validator; and five
inherited PowerShell `return$...`/`return[pscustomobject]...` tokens were commands,
not return statements. The final successors repair these narrowly. A subsequent
failed `Prepare` stopped before secrets/SQL because imported old-runtime readers
saw current revision547; final `OldIdentity` locally binds a108 while preserving
the outer547 revision. The failed attempt's protected `runtime-private` directory
is admitted only with exact creation time, strict private ACL and zero children;
an independent native read found directory=true, reparse=false,
creation `2026-09-09T09:48:25.6470008Z`, childCount0, protected ACL and one rule.

The complete pinned chain is: frozen runtime R2
`d6e52c126eddec03d4b8ecf99fda512ed5006d4f4547555df837c2423f24621c`,
shared restart binding
`16f118db7a4b05948da0aa7ab20f9e58c0d800247580bafd59946044a1903757`,
semantic r3
`900804ce3654b06862a94eb9f4905681160703bc39a78c0334a2899fbb22dbef`,
semantic r4
`29a1574629eb20357f7e21df08d3cd56ee0358dc5ae842b655fa310615e5b9be`,
semantic TS above, and adapter
`4d9de42a5c3a84e8003b51209716c7d200fbdd5cf0eccef1cf6eb65ba6ddbe56`.
Both semantic wrappers now hash/regular-file/reparse-check the helper before
dot-sourcing it, pass all six host arguments, use string-preserving JSON parsing,
and contain no active stale15956 binding. The shared helper's single15956 literal
is only the exact frozen-source transform needle.

Reviewer-personal commands and results:

- exact `C:\Users\astha\.bun\bin\bun.exe test
  ./.yellow/evidence/order460/restart-runtime-successors.test.ts` passes11/0,
  63 assertions, one file,7.54s; test SHA256 is
  `091d2e1c993807386824f5330c4f0b50c6586389d71edbf3c52642eb8457b2e6`;
- the suite executes exact PowerShell AST functions for unchanged-R2 equality,
  bounded transforms, all repaired return values, a108 reader scope/outer547
  preservation, exact-empty/wrong-generation/nonempty resume handling, exact
  restart-receipt shape, stopped/staged host bindings, wrong pins/stage/host and
  3GiB/2GiB resource denial;
- it hash-pins and invokes the actual trigger/final/serving/clean receipt validators,
  rejects extra shape, false authority/equivalence and catalogue drift, and invokes
  `ProofGuard` over all seven actual bounded receipts with no authority;
- exact bundled PowerShell7.6.5 `Action Review` with runner8c940aa4 and adapter4d9de42a,
  without `RootHandoff`, passes and reports `DatabaseAccess=False`,
  `RuntimeStarted=False`, `Writes=False`, source547, stage3001/founder3000,
  six workers on and fiscal/providers off.

The first hidden-path Bun invocation matched no tests; later in-progress regression
runs failed on test-harness token/mocking mistakes and then exposed the real
PowerShell return defects above. They are not counted green and were not hidden.
No reviewer command accessed SQL, credentials, or changed/stopped/started a process.

### Prepare ACL successor and first worker-runtime attempt

The preceding Prepare-only approval is superseded by independent review of the
exact four-artifact partial-completion controller
`13ab98960200bb394e1a80285f5e1e46cad707b8be1ee8a42003e234a30ecf2a`.
The admitted partial directory had the exact protected current-user parent,
creation `2026-09-09T09:48:25.6470008Z`, four names and no candidate receipt.
The two adapter logs were already private and matched
`ff15c0764133da726d403bb0e68d5dec6f9381be6707ad0caa14e7097a960498`
and `4e1625503e23bbb940484fade7af3cf6e2e1e33b5b6e70e8248f97d6d865279f`;
the inherited current-user-only environment and supervisor matched
`77d7f3832c0c3ec4a83693ffe463670eb9858a36061174c84e24102b1d427e5a`
and `d000021a63b6e768b76e9c1ec50de946f155b84a791966f1cc4ea21f58e9e42b`.
The branch cannot rerun adapters, overwrite/delete files, admit an extra file or
candidate receipt, or proceed before owner/hash/build-only projection validation;
it explicitly protects both generated files before writing the one receipt.

Reviewer-personal exact bundled-PowerShell `Action Review` passed with no handoff
and reported database/runtime/write false. The source-only companion
`079469c94f5c21a26e75eb836a4d547e77ba3b83f2370fa8054d62a16d82b534`
passed 13 checks. Exact Bun successor regression
`af8832128d9eef17dba678c4e301df7c61e0d523206217d886643e2b7681a46c`
passed 12/0 and 67 assertions, including a native NTFS current-owner regular-file
to protected one-rule ACL transition and directory denial. The exact adapter suite
also passed 5/0 and 53 assertions. Root then executed the independently cleared
Prepare-only completion successfully without starting the app, producing candidate
receipt `18cbe1518e0a06897b0c62ca82a6c6825fbf468e210fa129d4ab028ff308fa49`.

After a separate explicit root admission, reviewer `/root/restart_runtime_proof`
executed the one-shot no-Promote native stage wrapper
`121aaf4aa5dd8f557a50f21fb018c3ee69808013d5bb6c049aacec1e317fddfc`.
It encodes Baseline90, immediate Stage3001, receipt-derived Settled90, and an outer
finally-owned StopStage once a stage receipt exists. Baseline90 succeeded read-only
and produced private 782,987-byte receipt
`261303e05b8cceff54ba7b542b5813f5b80e8b5b6413db3f5e17cd4264572288`:
frontier90, 129 tables, table-inventory digest
`37db05a23f865dfab3728cdce69a96684203282424cace1440edc1e3d83978c9`,
outbox head65, authoritative availability all-match, pending as required, with
snapshot `2026-09-09T10:23:13.544446Z` through
`2026-09-09T10:23:14.338065Z`.

Stage3001 passed ready, home, three assets, login, issued-document detail/list and
issued-folio readiness, then failed the separately asserted eligible-folio oracle:
`Eligible-folio calendar requirement differs`. It therefore never wrote or
accepted `stage.receipt.json`; the controller's own inner finally performed the
only exact owned stop. No outer StopStage receipt is expected without an accepted
stage receipt. Independent post-failure read proves port3000 and port3001 each
have zero listeners, while stage/stopped/settled receipts are absent. Preserved
supervisor status `c777933defa898fb23dfea8b816517b33d76a94e748be4150ed251ab5816f947`
records the single non-restarting launch; its `running` body is the last write
because the verified controller stop terminates the owned child and supervisor.
The baseline and status generation are consumed and must not be deleted, changed,
or retried. No migration, schema, seed, Promote, generic process kill, raw-row or
credential output occurred. This runtime acceptance remains RED and no Promote is
approved.

The failed stage can have made legitimate worker-owned writes before the HTTP
oracle stopped it; the baseline is not evidence of no PMS change. None of the
frozen accepting auditors can honestly certify a newly captured stopped state as
the consumed Settled90 proof. `native-worker-semantic-audit.ts` requires a literal
Settled90 receipt, two empty due probes more than one second apart, and a raw
baseline-ended to settled-ended interval no longer than60 seconds.
`trusted-worker-availability-capture-binding.ts` requires the same bounded window,
two due probes and live interval closures. `worker-row-audit-r2.ts` is pure and can
run offline over two normalized snapshots, but it also requires an independently
admitted no-more-than60-second mutation window and does not convert or bind literal
raw captures itself. A future stopped-host capture must therefore be labelled
preservation/classification-pending, use the unchanged hash-pinned capture SQL,
bind both the preserved baseline and failed supervisor status, and leave
launchAllowed/observedConvergence false. Relabelling a late capture as Settled90,
forging due samples or treating an offline delta classification as convergence is
not approved.

Source-only stopped-host preservation is prepared but not authorized or executed.
Wrapper `failed-stage-after-preservation-r1.ps1`
`f9be71a992325a418d2c49303889ec28e9ebba2de6e652a79f4040c9908d05bb`
reuses the exact `96c1bfe9...` capture in its unchanged stopped `baseline90`
mode and contains no SQL. Its distinct outer receipt is explicitly
`failed_stage_after90`, read-only/non-mutating, accepted false, classification
pending true and launch allowed false. It binds the preserved baseline, candidate,
failed status, source artifact, controller and restarted PostgreSQL generation;
requires the exact failed child generation and supervisor absent, ports3000/3001
closed, and stage/stopped/settled receipts absent; uses bounded private output and
rechecks source/host in finally. PID reuse is handled by matching the failed Bun
path and exact start time, not the obsolete number alone. Focused test
`f8c04cff0895a4fb63d2ba50011157a3b0311b88cde1b1596f045b28d3167097`
passes 5/0 and50 assertions, including authority-free Review, authority and wrong
self-pin denial, no output creation and exact structural guards. No capture or SQL
was run; both proposed output paths remain absent.

### Stopped-host preservation capture after the failed stage

The final sentence above records the pre-execution state and is superseded by this
entry. After root inspected and accepted the exact wrapper/test sources and gave a
one-use admission, reviewer `/root/restart_runtime_proof` personally executed exact
PowerShell7 `CaptureAfterFailedStage90 -RootHandoff` with wrapper
`f9be71a992325a418d2c49303889ec28e9ebba2de6e652a79f4040c9908d05bb`.
It completed exit0 in 12.95 seconds, reported read-only true, mutation false,
accepted false, classification pending true and launch allowed false. The unchanged
capture produced raw preservation
`93848283c59e1d18d98ac92be414c612d900660c3a43dab8e088ecb9d5f97ba2`;
the distinct outer `failed_stage_after90` receipt is
`89a36a1ae01cdcbc6b5fb7ce012ce9d8875c2a330dfa14b38070462628b4ce4f`.
The wrapper's pre/final host guards proved both app ports closed and the exact failed
child/supervisor generation absent. It did not start a runtime or mutate application
data.

Reviewer then compared the preserved baseline and late raw capture offline only,
without SQL and without emitting raw rows. Both have the exact 129-table catalogue
digest `37db05a23f865dfab3728cdce69a96684203282424cace1440edc1e3d83978c9`;
all table definitions and all 117 non-worker table closures are unchanged. Of the
12 worker tables, only `fact_log`, `outbox` and `business_day` changed, each by two
added keys and zero removed or updated keys. Counts are respectively 112 to114,
65 to67 and6 to8. The sole sequence retained its definition and called state while
advancing 65 to67. Both consumer cursors are byte-equivalent. Property-date and
latest-reservation semantic closures are unchanged (3 and9 rows respectively).
The production-derived authoritative-availability result remains all-match for its
one scope, with identical scope digest/literals, source and SQL pins, and PostgreSQL
type contract.

This is preservation and delta classification only. Baseline end
`2026-09-09T10:23:14.338065Z` to after-capture end
`2026-09-09T10:47:11.503172Z` is 1,437,165.107 ms (about23m57s), no Settled90
receipt exists, and the original due/convergence conditions were not captured.
Accordingly no frozen auditor acceptance is claimed, runtime acceptance remains RED,
and no Promote or retry is approved from this evidence.

### Current-source readiness diagnostic, source-only independent review

Reviewer `/root/restart_runtime_proof` independently inspected frozen wrapper
`ba94b3134897c78b5de621fb270289e192052e2facd24794a49aa0f484b4122e`,
diagnostic `40b5e34d59d3e79812afad5336a202920da52df198d2aa7b78bf06e3afdbcdde`
and test `23bc0dde205fd9e31d361c088160446f0f96870f740f1e6a029576706fb3bb01`.
The wrapper pins the current immutable archive/tree, retained manifest/receipt,
candidate receipt/environment, operator source, exact native executables and
restarted PostgreSQL generation. It validates the private environment's exact key
set and non-secret identities, passes the database URL and manifest only through the
bounded child's environment, sanitizes the six-key result, and asserts the stopped
host before and after. `Review` neither starts the app nor enters the database.

The diagnostic begins a single-connection transaction with `SET TRANSACTION READ
ONLY` before any application query, applies 10-second statement and2-second lock
timeouts, resolves the retained tenant, uses transaction-local `set_config`, changes
to `app_role`, and proves role/tenant/database/loopback/port/read-only state before
calling the exact current-source `IndiaNativeFiscalOperatorReadService.discover`
seam. It always throws a private sentinel so the successful probe is rolled back;
the database read-only state independently prevents a called routine from writing.
The returned projection exposes only `{ok,errorCode}` or readiness kind, optional
bounded blocker and value-key names; identifiers and ready confirmation contents are
not emitted. The connection is closed in finally and both Bun and the wrapper impose
bounded execution.

Reviewer-personal exact Bun test passed4/0 with22 expectations; strict no-emit
TypeScript compilation passed; PowerShell AST parsing passed; exact bundled
PowerShell7 `Action Review` passed with readOnly/mutation/host/inputs all true; and
an all-zero runner pin was generically denied. These commands did not run SQL.
No source finding blocks the one already separately admitted read-only diagnostic
observation, but this review does not classify that observation as runtime acceptance
and grants no Stage, retry, or Promote authority.

### Read-only eligible-readiness diagnosis after the failed stage

The failed HTTP oracle did not capture its actual response, so no response value was
inferred from that failure. Root subsequently inspected and admitted the isolated
read-only diagnostic wrapper
`ba94b3134897c78b5de621fb270289e192052e2facd24794a49aa0f484b4122e`
and source
`40b5e34d59d3e79812afad5336a202920da52df198d2aa7b78bf06e3afdbcdde`.
The wrapper pinned the complete frozen source archive/tree, exact retained manifest
and current candidate environment, fixed Bun/PostgreSQL/PowerShell binaries, and the
restart-bound PostgreSQL generation. It proved ports3000/3001 closed before and after.
Its one Bun transaction set `READ ONLY`, bounded statement and lock timeouts and the
real `app_role`/tenant context, called the frozen
`IndiaNativeFiscalOperatorReadService.discover` with the exact retained eligible
tuple and null calendar evidence, then deliberately threw a private sentinel so Bun
rolled the transaction back. It started no listener, worker or provider and emitted
no identifier, confirmation, row or credential.

Root executed that exact read-only Run once. It completed exit0 in 5.38 seconds and
returned only the sanitized service result: `ok=true`, `kind=blocked`,
`blocker=business_day_unavailable`, keys `blocker,kind`. Root also executed the
focused source contract `23bc0dde205fd9e31d361c088160446f0f96870f740f1e6a029576706fb3bb01`:
4 passed, 0 failed, 22 assertions. This is diagnostic truth, not runtime acceptance,
and it authorizes neither a seed change nor a business-day mutation.

The source explains why the retained fixture can age from seed-time `ready` to this
current blocker. `read_india_native_issue_authority` derives `invoiceIssueDate` from
the transaction timestamp in the property's timezone on every call; the manifest
does not persist an issue date. The fiscal review seed likewise derives its issue
date from the property-local current date and inserts only that date's business-day
row before proving the eligible source ready. Its retained service/night dates remain
immutable, but its issue-date readiness is deliberately current. The discovery path
then calls `read_india_native_document_context_candidate`, which requires exactly the
current derived business-day row with `sealed_at IS NULL`; absence or a sealed row
raises `P0011`, and discovery maps that exact exception to
`business_day_unavailable`.

The enabled business-day roll worker is a later mutating convergence path, not a
seed-time or HTTP-oracle guarantee. Its due source lists active properties for which
the current property-local date has no business-day row, and its first drain calls
`open_current_business_day`, which inserts that row. The worker starts asynchronously
during `runtimeApp()` construction before the listener call; an immediate HTTP proof
can therefore race its first drain. A sealed current row is not due under that query
because a row exists, and the opener does not unseal it. The sanitized result alone
does not distinguish missing from sealed, and no extra SQL query was needed or run.
Consequently the controller's fabricated exact
`working_day_calendar_required` fixture is disproved for this saved state; merely
replacing it with an unconditional timing-dependent pre-worker assumption would not
prove later worker convergence.

### Offline classification of the failed-stage business-day writes

Reviewer `/root/restart_runtime_proof` compared only the literal rows already bound
by baseline `261303e05b8cceff54ba7b542b5813f5b80e8b5b6413db3f5e17cd4264572288`
and late preservation
`93848283c59e1d18d98ac92be414c612d900660c3a43dab8e088ecb9d5f97ba2`.
No SQL or runtime action was used. The classification was checked against exact
current-source business-day roll
`69f99d6bee0bc33f25852d5e0faa7e6d897d5fcc3d9b61f2c008ad24595cdcd6`,
due-scope adapter
`036844fdef43364a3fe0e5bc461503751c37c659f47028abc6ebe8b42bb1b244`,
fact writer
`297a39c9bf63d38ac80910a668a66830641bf5eebdde96061083b2d0ba4ee7e1`,
outbox writer
`dfff59cb3be5f33e541f3f0516a91e6489ceb7e6323a56bd34ec59ed50a2d36a`,
due/open migration
`50cf8593ac385b74fbe61da9d28f0ecf59b78297c7aff46ad073f34409efc34f`
and its contention repair
`8e28af137263ff23ecacb1f9e49b4f48b203d5f8c3773d1c2471c5a78cae331a`.

The complete six-row delta is one exact worker pattern for two distinct properties.
At baseline each tenant/property/local-date key was absent; each retained tenant is
active, each node is a tenant-matched property with a valid captured timezone, and
each added date is the one exact canonical `2026-09-09` returned by the contemporaneous
property-date semantic capture. Both new business days are open and unsealed. Their
`opened_at` values are `2026-09-09T10:23:33.079870Z` and
`2026-09-09T10:23:33.676073Z`, respectively18,741.805 ms and19,338.008 ms after
the baseline ended.

Each day has exactly one new insert-only `business_day.opened` fact and one new
`business_day.opened` outbox event, both attributed to the fixed business-day worker
actor ending `0061`. The fact subject and event aggregate are the same property;
tenant, property, business date and actor match across each pair. Each fact has null
valid-to/supersedes and exactly the three worker payload fields plus the fact writer's
request ID. Each outbox event has the exact three-field worker payload, event version1,
null causation/published values, and correlation equal to that fact request ID. The
payload's `opened_at` matches the worker's expected JavaScript millisecond
`toISOString()` projection (`...33.079Z` / `...33.676Z`), while the day, fact and
outbox timestamp columns retain the mutually equal PostgreSQL microsecond values.
Outbox sequence identities are exactly66 and67.

All six pre-existing business-day literal rows are byte-equivalent after the attempt;
no old day was removed, updated, sealed or unsealed. Every other worker-table closure
is unchanged, and all two facts/two events are consumed exactly once by the two pairs.
This fully classifies the observed writes as the source-prescribed automatic current-
business-day openings. Although the row timestamps fall within60 seconds of baseline,
the after snapshot itself was captured about23m57s later and contains no accepted
Settled90/due-probe evidence. The frozen worker auditor was therefore not invoked with
an invented window, and no timed-convergence or runtime acceptance claim is made.

### Independent retry-r4 source review before any live retry

Reviewer `/root/restart_runtime_proof` independently reviewed the fresh-attempt
controller, projected supervisor, semantic capture wrapper and one-shot chain. The
review initially rejected several source generations: unreachable inherited
Prepare/Promote bodies, malformed readiness predicates, parent-supervisor binding in
the settled proof, a chain that could report completion with a rejected worker proof,
the shared host guard's stale base-supervisor path, an overly broad semantic port
surface, and two mutable `$candidateSupervisor` uses that broke post-launch derivation
and pre-stop candidate validation. Those findings were repaired by the independent
implementer; no rejected generation was launched.

The accepted source identities are controller
`a7c90bebb36b023b3cb8abe766cf6c86a0029d191cdeb8c50529e98c1736d937`,
attempt projector
`acb64865e855d39f380012791d757ba68313e60b81d8d489717fa725cbd2cbb0`,
semantic wrapper
`1831e867cf477adc7cffbde428c1eb66dee470a4a689639441ca0e51970f2f61`
and chain
`e8d6fba83184130ee9fe764b941f8f5c7e75cc6f84ddc31e2818420e8c08276b`.
The projector performs one exact UTF-8/no-BOM runtime-root substitution and the
reviewer independently recomputed the projected supervisor digest as
`757094c730633d11acd9d48ae42ee4309476518daff692045d8307a4aecd0fcf`.
The controller physically exposes only Review, Stage and StopStage; the chain has no
Promote path. Parent candidate validation and projection now use immutable
`$parentSupervisor`, while the mutable path is assigned to the projected attempt only
immediately before the imported start/stop command-identity helpers. Both controller
and semantic wrappers make the shared guard's one exact supervisor-path dependency
explicit, and live validation requires port3001 and that exact projected hash. The
chain requires a fully linked `workerProofAccepted=true` stopped receipt before it can
report completion.

The reviewer personally ran the exact bundled PowerShell source tests:
`current-runtime-attempt-supervisor-retry-r4.test.ps1` (5 assertions),
`retained-worker-semantic-capture-retry-r5.test.ps1` (8),
`current-runtime-stage-chain-retry-r4.test.ps1` (9),
`current-runtime-control-retry-r4.readiness.test.ps1` (6), and
`current-runtime-control-retry-r4.sequencing.test.ps1` at
`61208bd872ec1d8b93eaf1c3040ce05604b75fa914211edb1e9d1dcc34248200`
(11). The last test AST-extracts and executes the actual Stage and StopStage clauses
with temp-only mocks: successful Stage reaches projected start and receipt writing,
the outer stop guards observe the parent path before StopStage binds the attempt path,
and a post-launch HTTP failure performs exactly one owned stop. No real HTTP, process,
database or runtime operation occurs. Exact Bun ran
`test ./.yellow/evidence/order460/current-runtime-control-retry-r4.static.test.ts`:
4 passed, 0 failed, 17 expectations. Native PowerShell parsed all four sources and the
sequencing test with zero errors, the controller rejected a wrong self pin, and
credential-free Review passed independently for the controller, projector and
semantic wrapper with database/runtime/writes all false. This is source clearance for
root to consider a separately pinned one-shot live chain; it is not live-execution or
promotion authority.

### One-shot retry-r4 execution result

After root appended the exact execution admission, independent reviewer
`/root/restart_runtime_proof` invoked bundled PowerShell7.6.5 exactly once with
`current-runtime-stage-chain-retry-r4.ps1`, expected runner
`e8d6fba83184130ee9fe764b941f8f5c7e75cc6f84ddc31e2818420e8c08276b`
and `-RootHandoff`. The reviewer first confirmed that controller
`a7c90bebb36b023b3cb8abe766cf6c86a0029d191cdeb8c50529e98c1736d937`,
projector `acb64865e855d39f380012791d757ba68313e60b81d8d489717fa725cbd2cbb0`
and semantic wrapper
`1831e867cf477adc7cffbde428c1eb66dee470a4a689639441ca0e51970f2f61`
matched the admitted pins and that the attempt/capture roots were absent.

Baseline90 completed read-only with no mutation. Its receipt is
`ff797bf978bdf333abc2f8de15056377b0d661fb487994606f3461a3fa3318a1`,
captured at `2026-09-09T11:47:05.471130Z`, and remains deliberately
`accepted=false`, `classificationPending=true`. Stage started the projected
supervisor, but the exact HTTP classifier rejected the eligible-folio result with
`Eligible-folio readiness differs`; the chain exited1 at Stage3001. The response body
was not retained, so its variant is not inferred. Stage never became owned by the
outer chain and no stage, Settled90 or stopped receipt exists.

The projected supervisor remains exact at
`757094c730633d11acd9d48ae42ee4309476518daff692045d8307a4aecd0fcf`.
The preserved bounded status receipt is
`c8d1cd5cc277774a2e0589b5ca52aa0a7f7003271106608a6d1b743d31da9416`:
one launch, child PID11128 started `2026-09-09T11:47:22.9480880Z`, supervisor
started `2026-09-09T11:47:18.7672070Z`, and automatic restart false. Both bounded
logs are empty with the standard empty-file digest
`e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855`.
The controller's actual failure `finally` completed owned cleanup: child11128 is
absent, zero non-inspector processes have command lines bound to attempt-r4, and
ports3000/3001 have zero listeners. PostgreSQL remains the exact generation PID9880,
start `2026-09-09T08:57:25.3781778Z`, as the sole listener on55503. No retry,
promotion, SQL follow-up, repair or cleanup of the preserved evidence was performed.

Root separately admitted and executed the stopped-host preservation wrapper
`c161ce886fb30b6dc39bf1b90125c365d2709032683617d688dafa2bc90ef741`.
It produced raw capture
`40ed07018bcc2ce187a37138a24956cb306b4c17f0ad081dad84babac8b0c268`
and pending outer receipt
`26768f482f0800d566ac1c88264491c5a4d766ae2e8ca812c8dfed3d7aae12db`.
The independent reviewer compared only those saved bytes with baseline
`ff797bf978bdf333abc2f8de15056377b0d661fb487994606f3461a3fa3318a1`;
no additional SQL or runtime action was used.

Source/query pins, the `yellow_order444_review_a10851786f17` database identity,
129-table inventory `37db05a23f865dfab3728cdce69a96684203282424cace1440edc1e3d83978c9`,
reservation semantic and authoritative availability closure are identical. Exactly
five table closures changed and the other124 are byte-identical. Business-day rows
increase8-to9, fact rows114-to115 and outbox rows67-to68, with no removed or modified
old row. The sole trio is the exact business-day worker pattern for property ending
`d0c4` and its unchanged captured local date `2026-09-09`: one open/unsealed day at
`2026-09-09T11:47:24.086252Z`, one insert-only `business_day.opened` fact and one
matching v1 outbox event, all actor ending `0061`. Tenant/property/entity/date and
column timestamps align; the fact has null valid-to/supersedes and only the three
worker payload fields plus request ID; the outbox has the three worker payload fields,
null causation/published values and correlation equal to that request ID. Its sequence
is68; the sequence definition is unchanged.

The two event-consumer cursors advance65-to68 and `consumer_processed` adds six
markers: both `availability-projection` and `arrival-pickup-task` acknowledge the two
pre-existing, already-classified business-day events at sequences66/67 and the new
event68. The pinned outbox consumer marks selected events before calling handlers;
both handlers ignore `business_day.opened`, explaining why no derived domain table
changes. The row evidence is consistent with normal workers, but the preservation
snapshot was late and there is no Settled90 receipt. No <=60-second convergence or
runtime-acceptance claim is made.

### Retry-r5 source-only independent review

Independent reviewer `/root/restart_runtime_proof` reviewed the fresh, non-live r5
successors: controller
`756b1082b7f5ffdf0e95a9b4be37b5611d64a9a6e7619541a4e87e79b4596231`,
attempt projector
`6ed70be428027a0ed3c16aee4ee074512100f4a2dc8020c276aef5efa2e964fd`,
semantic wrapper
`02cc9a616b849dbd116e4bb3916838a27789fa6b9db2759116fa4a0634d74406`
and chain
`56809a2bbf2ad6ca8f7263cc51786c1efb0e72f083514914db9fab7cf974fead`.
The projector and semantic changes are exact fresh attempt-r5/capture-r5 path and
pin substitutions. The controller still exposes only Review, Stage and StopStage;
the chain retains Baseline90 -> Stage3001 -> Settled90 -> finally owned StopStage,
with no Prepare, Promote or port3000 launch path.

The review initially found and rejected three defects before clearance: array
comparison in the retained classification validator, case-insensitive blocker
membership, and a double-escaped invalid .NET blocker-catalogue regex. It also found
that malformed nested ready fields could fail before the new diagnostic was saved,
and that untrusted values could reach the purportedly redacted receipt. The final
bytes use exact case-sensitive membership, execute the hash-pinned
`india-native-fiscal-operator.ts` catalogue extraction during credential-free
Review, defensively project nested confirmation fields, and retain only fixed safe
kind/blocker/key projections. Exact ready and each of the 12 source-defined blocked
variants can satisfy launch health; only exact ready is marked invoice eligible.
Unknown, uppercase, extra-key and malformed variants retain a bounded one-shot
diagnostic and reject the launch. Stage, host, chain and StopStage validation bind
the classification receipt hash and its exact semantic fields.

The reviewer personally ran exact bundled PowerShell tests at hashes
`76959c44e24fd4f6eb3e34998fdfa885b15c03a47c0cf5ab5cad44430423da3c`
(classification and real pinned catalogue, 24 assertions),
`4c26c89d6b03ab739c7d01a9c545058f9ba1db75ae9b583716b98ce0ff7b60c8`
(source-derived HTTP ordering and hostile/malformed persistence, 12),
`ca2047f4d47dba10f310f41b56d75831fa261846ae433439357d98cf73b88860`
(source-derived Stage/Stop sequencing, 11),
`044be54d8c1025787dd7a20ffb0fe710242dd920a044f8694f9d43f8186e7219`
(projector, 5),
`8f21a89ff4860b552e9f7752a34c9cfc97329d8ee721427a56f6f31d2b7ff9e5`
(semantic, 8), and
`c7489b3d054df01db1f88934ba5fe65b655c7be1c86d952bbd459765a59d2eff`
(chain, 11); all passed. Exact Bun ran static test
`4a8f7b298b77da63a4c178d7372f51e090895584ce8324daf0f123be2ad83a45`:
4 passed, 0 failed, 24 expectations. All four PowerShell sources parsed with zero
errors. Credential-free controller Review personally passed with
`FrozenBlockerCount=12` and database/runtime/writes false; projector and semantic
Reviews also passed with no runtime or database access. Wrong-runner and missing
root-authority invocations failed closed with exit1. This is source clearance only
for root to consider a separately pinned one-shot r5 chain; no SQL, real HTTP,
process launch, runtime mutation or promotion was performed.

### Native PostgreSQL crash-recovery review and execution — 12 September 2026

Independent reviewer `/root/runtime_r6_review` inspected the recovery-only wrappers
and the unchanged inherited launcher without SQL, application startup, migration,
manual PID-file deletion, WAL reset or data repair. The first wrapper
`recover-native-postgres-20260912.ps1` is
`589344a3db5c0b453860c1197fb49a3138b6ee97973456b659a1c2520f6a6a61`; its
mocked guard test
`77d45d87fee6efad35398ba5d8e7f8c16413f472d482c1ea718fcc8178362bf4`
personally passed 19/0. Credential-free Review, the inherited source test (16/0),
wrong-pin denial and missing-authority denial also passed. The wrapper preserves the
trusted restart receipt's exact 20-key schema and records unclean-start facts in a
separate recovery-admission receipt, so the downstream exact-key reader is not
weakened.

After root recorded a one-use admission, this reviewer invoked that exact wrapper's
Start once. It failed closed at `pg-ctl-start` after the pinned `pg_ctl` returned
exit1. The retained failure receipt is
`D:\Yellow\temp\order460-native-postgres-recovery-20260912\restart-failure.json`,
SHA256 `c12f099184a093a62071f658e514ba5964e13d827bb07d620a56f45954bce587`.
The initially observed recovery generation PID4616 subsequently exited, its
`postmaster.pid` disappeared, port55503 had no listener and the minimized control
state was `shut down in recovery`, system identifier
`7682140314900366080`, with control receipt
`88b114f773d7f5ba0f027edce653842679c40d442da3ae878fa96e421686c98e`.
No orphan remained and no automatic/manual retry or forced stop was used.

The bounded r2 successor `recover-native-postgres-20260912-r2.ps1` is
`39eece0269037504b4e62e909d5fd7ee2fc78926db5e5a48c2c12b4fedad7262`.
Its executable projected-source test
`5bce23bcc1b2d8d2bbc2c77ae9b867a89ccc6e4e805d16eac8d57bc85c3e1c`
personally passed 28/0 and derived projected source
`6a63b57af8bf04ab2596e2f83c723058bb2e556c200d7c9771cb864740376b2a`.
Credential-free Review, wrong-pin denial and missing-authority denial passed. The
test proves the exact absent-lock/prior-failure/control-state bindings, unchanged
20-key success receipt, separate exact 8-key recovery admission, unchanged 30-second
cleanup bound, and absence of SQL or destructive recovery commands.

After a second exact one-use root admission, this reviewer invoked r2 Start once. It
completed successfully without retry and restored the sole native PostgreSQL16
listener on `127.0.0.1:55503`: PID2340, exact seven-tick start
`2026-09-12T17:14:09.3086384Z`, executable digest
`324ac242d623edcf82822f0c59c5dc2f8e74049d9dbb7ebc738bdaf5824e2ae6`.
The unchanged 20-key restart receipt is
`D:\Yellow\temp\order460-native-postgres-recovery-r2-20260912\restart-receipt.json`,
SHA256 `563fc1645160b23ab63f04eb9371b3ee6a57dd3cbb11dd501b0159eb479f16a5`.
Its separate 8-key `recovery-admission.json` is
`9b5592a1df2afdfdb024eab710df4d73c2ed2e1286c17b0b37bb84dfd497e5e7`
and binds prior state `shut down in recovery`, prior PID4616, prior control digest
`88b114f773d7f5ba0f027edce653842679c40d442da3ae878fa96e421686c98e`,
the first failure digest above and `businessDataEquivalenceVerified=false`. The
pre-start `pg-controldata` receipt is
`223247f4710679a284201c8f16a284d464d938c26f573a0ec7115a45537c0220`;
the exact prior-failure binding file is
`42633a6d0d04014dea81b7c271093a5225da531b9a47d5dce13f872fdee614d5`.
Post-start minimized control state was `in production` with the same system
identifier. This restores host availability only; it is not a database-equivalence,
application-runtime, readiness, worker-convergence or promotion proof.

### Late retry-r5 failed-stage preservation — offline classification

Reviewer `/root/runtime_r6_review` inspected only the already-saved files; no DB,
Stage, Start, process mutation or new capture was used. Baseline
`D:\Yellow\temp\order460-worker-semantic-capture-r5-20260909\baseline90.json`
is `baf1c714c6a68b69b64516c742e804cc2764431ce0750cba84c926b999d3286e`.
The late raw capture `after-failed-stage90.raw.json` is
`9ec3aaae50c2a0065919ad7a4325b2f32f2d4d8f19fa630e6969f4e6902b5472`;
the exact 26-key outer receipt `after-failed-stage90.json` is
`5fb7e58125e2378d04f01c2833b9ef26c381ef808eb31e2da602a3470a86be48`.
The outer receipt's embedded raw text is byte-identical to the raw file and its
baseline, raw, failed-status, failed-stage, eligible-classification and owned-cleanup
hash bindings all match. It remains truthfully `failed_stage_after90`, read-only,
non-mutating, `accepted=false`, `classificationPending=true` and
`launchAllowed=false`.

PowerShell7 `ConvertFrom-Json -DateKind String` preserved the failed generation's
exact identities, including child PID17356 at
`2026-09-09T12:42:54.0004210Z` and supervisor start
`2026-09-09T12:42:50.6148280Z`. The exact stage classified the sanitized readiness
as `blocked_supplier_issue_status_unavailable`, launch-health accepted but invoice
ineligible. Its separately hash-bound owned-cleanup receipt
`ccf515c2ca976c177f2b1282e1ec265507a8fcf342924568614e2b2c1ca2c56c`
records `ownedStopAccepted=true`; no Settled90 or stage-stopped receipt exists.

The complete offline comparison found no retained data delta. Both snapshots contain
the same 129-table inventory digest
`37db05a23f865dfab3728cdce69a96684203282424cace1440edc1e3d83978c9`;
all 129 table definitions, counts and row closures are identical. The sole sequence
is exactly unchanged at68, outbox head remains68, both consumer cursors are
identical, and the production-derived authoritative-availability closure is
identical. Property-date semantic closure remains3 rows at
`5f6513b40352f80098830db7b616e2abb7fe0cf1a0b9bbed54fd5cdbacf6a650`;
latest-reservation remains9 rows at
`84397ab35a8610712801a85064a03f3fb72c3473cbff1298c19b543cc09d5f5e`.

This proves zero observed retained-state delta between baseline end
`2026-09-09T12:42:36.196119Z` and late capture end
`2026-09-09T13:01:21.504731Z`, an interval of 1,125,308.612 ms (about18m45s).
Both raw receipts deliberately remain `baseline90` and contain zero due samples.
Running the unchanged pure native semantic auditor over those exact saved bytes
returned `accepted=false`, `launchAllowed=false`, reason `settled receipt identity`.
Therefore the files prove preservation and a zero net saved-state delta only. They do
not prove a <=60-second Settled90 window, due-probe separation, convergence, runtime
acceptance, invoice eligibility or promotion safety.

### September 12 restart-bound retry source clearance

Reviewer `/root/runtime_r6_review` independently checked the narrowly revised
successors after the native recovery: controller
`69982dcae7e8cddc1fb02716d2bfd65a46a0ad85ed21ed159fa5b3841ab7b492`,
attempt projector
`c3bda4d23bd015a1c0ee9a5ba0bcd043673b898fd23ffe32ec69d065adf7d309`,
semantic wrapper
`91da5515b398954d90d27e9afdd7ac793f7f147f60f6adae23a47b42d6ff055b`
and one-shot chain
`c68d18a2bd2f8d0cfff093bdf630b3e35746f2be6ae737a943dfbbe4e1423382`.
The common recovery binding remains
`b40646de518261dfcc9e36b8a269197aaee5364f35453710a542a37b29a8ee15`.

Review initially rejected an import-order defect: dot-sourcing the new recovery
binding after projecting `Assert-RestartBoundHost` restored the original hard-coded
supervisor path. Final controller and semantic bytes now import the recovery helper
first, then reconstruct the hash-pinned shared guard with
`$supervisorPath=$ExpectedSupervisorPath`, and immediately inspect the effective
function body to fail if the original path has been restored. Executable actual-import
test `0132f508aa3b539571eca2e3aff306f983fe1a757acdc769c99d46700644480d`
personally passed4/0. Controller, projector and semantic credential-free Reviews
personally passed with database/runtime/writes false. The recovery binding's original
receipt-only test passed15/0; reviewer hostile test
`06b530926c0b4090f9b012d71ee9568f7698c06693fc849983bd9d9c567739d9`
passed17/0, including missing/extra admission keys, every critical recovery field,
non-null prior lock, false-equivalence enforcement, admission/pre-control/restart
hash denial, exact seven-tick identity and wrong host-start denial. A live read-only
stopped-host guard also matched PID2340 and
`2026-09-12T17:14:09.3086384Z` without SQL or mutation.

The seven focused PowerShell suites passed85/0 total (24 readiness,12 HTTP ordering,
11 sequencing,14 status JSON,5 projector,8 semantic and11 chain). The status suite
retains exact string timestamps including `.0004210Z` and `.1234567Z`. Corrected Bun
static test `5c42047f33a7ffca0d45e4933d6a93f2717ad58f173e21e8002edaeb70d95b55`
passed4/0 with26 expectations; all four final PowerShell sources parse with zero
errors. This clears only one separately root-admitted exact retry chain using the
saved candidate/frontier90 and six workers. It grants no port3000 start, promotion,
or acceptance without the new Baseline90/Settled90 and owned-cleanup receipts.

### September 12 retry-r7 actual execution — Baseline90 RED

After root recorded the exact one-use admission, reviewer
`/root/runtime_r6_review` invoked bundled PowerShell7.6.5 once with chain
`c68d18a2bd2f8d0cfff093bdf630b3e35746f2be6ae737a943dfbbe4e1423382`
and `-RootHandoff`. It failed at Baseline90 before Stage or any application launch.
The sole bounded output is the 75-byte protected log
`D:\Yellow\temp\order460-worker-semantic-capture-r7-20260912\baseline90.json`,
SHA256 `2ce6087405f83ec930c2df0b1bdec35d010439fe6a42c0719aa27e3c9d4ca0c8`,
containing only `Order460 semantic worker capture failed: runtime identity contract
differs`.

The concrete source cause is in unchanged capture
`retained-worker-semantic-capture-restart-r3.ts`: its runtime-identity validator still
requires PostgreSQL PID9880 and start `2026-09-09T08:57:25.3781778Z`, while the new
semantic wrapper truthfully supplied recovered PID2340 and start
`2026-09-12T17:14:09.3086384Z`. The wrapper updated the outer host guard but did not
project those two frozen TypeScript constants. The attempt-r7 runtime directory was
never created; ports3000/3001 remained closed and port55503 remained solely owned by
PID2340. Consequently no Stage, worker proof, stage-stopped or owned-cleanup receipt
exists or is required for a launched generation: no app process was launched and no
orphan remained. The consumed capture root must not be replayed. Any retry requires a
fresh successor/root that exactly projects or replaces both frozen TypeScript host
fields and proves that projection with an executable baseline fixture test.

### Fresh retry-r8 source review after the Baseline90 failure

Reviewer `/root/runtime_r6_review` verified capture successor
`retained-worker-semantic-capture-restart-r4-20260912.ts`, SHA256
`88ff183246d3fb9d3a96b6884db4615dd549bbb1037117a0c0cb151ac7268c20`,
against frozen predecessor `96c1bfe9...`. Its byte delta is exactly three intended
host-literal replacements: both PID checks now require2340 and the runtime identity
requires exact start `2026-09-12T17:14:09.3086384Z`. No SQL, capture query or auditor
logic changed. Actual-function fixture test
`972e7042eb8fe2a3344a92b2a858952ac0d1dc86a2f515e5c92626c941f46c17`
personally passed11/0 with19 expectations: complete compile and exact byte
back-projection, current baseline/settled acceptance, old PID/start and wrong-tick
denials, four other hostile identity shapes, and actual retained-baseline reader
acceptance/rejection.

Fresh controller
`6c138acddcdce56393fb170f04e0123c542c9bc653de3249be61c753e9eaf92e`,
attempt projector
`aa9ac29d7cb749cacd65c579795eb4fb577a9bc042c5017bad58c1e412373f06`,
semantic wrapper
`811ea1dc430813b43e503365563a0d9d0a13249b1a1bd157a4b8e474edb824ec`
and chain
`543141fcf1eaa13c273d421d4652e144a241a2b2606456ed9ad890ac1575ac0b`
only advance to fresh attempt-r8/capture-r8 paths and the new capture/supervisor
pins. No old PID/start, old capture entrypoint or consumed r7 root remains in the
complete new executable path. Controller, projector and semantic credential-free
Reviews personally passed; semantic Review accepted the exact new capture path/hash.
Seven PowerShell suites again passed85/0, Bun static test
`c29d440a656c9d396dc014b8b611fd4fb504b45e2bd91a8f18d4fa75daabb904`
passed4/0 with26 expectations, and all four PowerShell sources parsed without error.
Both new output roots were absent. This is source clearance for one separately
admitted exact chain only, not authority to replay the failed r7 root or promote.

### Retry-r8 actual execution and exact owned cleanup

After root recorded a new one-use admission, reviewer `/root/runtime_r6_review`
invoked exact chain
`543141fcf1eaa13c273d421d4652e144a241a2b2606456ed9ad890ac1575ac0b`
once with bundled PowerShell7.6.5 and `-RootHandoff`. Baseline90 succeeded read-only
with129 tables and intentionally pending/non-accepted receipt
`15780e1eccfe033cceb346805c7eebd6ef334f635b9a4f6f1439f2d5051f85e4`.
Stage then succeeded on3001 with child PID9376, supervisor PID3828 and receipt
`a0b600dc3fdc63aed1bcc88e54d98b2034c0ca371d7ff3510b0af1e9fc0b0489`.
The exact sanitized readiness was `blocked_business_day_unavailable`, launch-health
accepted but invoice ineligible; classification receipt is
`f86d9931ca7cadb564d46a0739771bdce83d1ea1301ac16efeec9ababa380471`.

Settled90 stopped before capture because the shared host guard compared the receipt's
CIM-derived starts `2026-09-12T17:47:50.2077800Z` and
`2026-09-12T17:47:46.5671100Z` with native `Process.StartTime` values
`2026-09-12T17:47:50.2077807Z` and `2026-09-12T17:47:46.5671104Z`.
This is lost CIM sub-microsecond precision, not equivalent-instant trailing-zero
serialization. The chain's original finally path encountered the same guard before
it could stop the processes, so child9376/supervisor3828 initially remained exact and
owned. No Settled90 or worker-proof receipt was produced.

Reviewer authored narrowly bound cleanup
`current-runtime-owned-cleanup-r8-20260912.ps1`, SHA256
`e757b3b98e908ec0795240e5fdd1d24bcff3f05864382705d35e917d0994be5b`.
It pins the exact stage, unchanged running status
`da9e68e3b89a0af01b303183ef84283aa9837da4667b897af71cd07178fecb96`,
classification, controller, projected supervisor and executable hashes; retains the
original CIM timestamp/command/parent/listener checks; and additionally requires the
two exact seven-tick OS starts above before stopping only those PIDs. It neither
normalizes nor substitutes the OS starts into the CIM checks. Pure source test
`7b569d8bf3eee102e478081907d06c85cbc782b38afc379be2c3df47828bdd3d`
passed21/0.

Root independently inspected and Review-ran the exact cleanup, separately admitted
one Stop, and executed it once. It completed in4.65 seconds with cleanup receipt
`f4d6d558fb0afd00e5783791e26058fb6061bf9e77d3a05a63f8bbd836fe3e4c`
at `2026-09-12T17:53:00.2218608Z`. Root independently confirmed PIDs9376/3828 and
port3001 absent, sole55503 listener PID2340, and the original running-status bytes
still retained at `da9e68...`; cleanup proof is a distinct receipt. This is exact
owned cleanup after a failed staged observation, not convergence or promotion.

### September 12 native-timestamp repair source clearance

Reviewer `/root/runtime_r6_review` independently inspected the frozen precision
repair after retry-r8 exposed the CIM/native sub-microsecond mismatch. The exact
projected supervisor is
`6ec24a0d026b51fcf9cedda41e0ee78afeb84ef6aa415d132c3111d6437a122c`,
controller-r9 is
`477c0dc38326f34ecfdf1ca02fd6843661323cb3fa65f2c295aef1c7da223516`,
attempt-projector-r9 is
`e0b273c6ef91f4d5981ea8347ace30e223b0e8f07236bfddf5be270fe7680d88`,
semantic-r10 is
`91cb1fb8a1f3ed2e152756da8e3b46ccb2e3f058cc0b8e25c53fd3cbb61da15d`,
and the one-shot chain is
`0eac9fe7c4d5b51bd814700949738e651fb143a23bf0e49f05d9206a14d70bc8`.

The repair preserves the existing CIM command-line, executable, PID, parent and
listener checks, while the projected writer records native `Process.StartTime` and
all three launch/stop consumers pass that native value through the existing
`ActualStartedUtc` argument. No timestamp normalization or equivalent-instant
fallback was added. The actual writer/controller hostile seventh-tick regression
`current-runtime-timestamp-projection-r9-20260912.test.ps1`, SHA256
`cb1ef0f3d75b92bcb080716dec8cabd7d07ff65fe287ba02f40f4fcdb74c6fcb`,
personally passed14/0. The unchanged focused PowerShell suites passed85/0;
actual projected-import behavior passed4/0; Bun static test passed4/0 with28
expectations; all final sources parsed and credential-free Reviews passed. This
cleared one separately admitted chain only and did not itself prove runtime success
or authorize promotion.

### Retry-r9 actual Baseline90 → Stage3001 → Settled90 → owned StopStage

After root recorded the exact one-use admission, reviewer
`/root/runtime_r6_review` invoked the chain above once with bundled
PowerShell7.6.5, its exact `ExpectedRunnerSha256`, and `-RootHandoff`. No automatic
retry, port3000 start, migration, seed or promotion was performed.

Baseline90 completed read-only over all129 tables. Its deliberately pending
baseline receipt is
`D:\Yellow\temp\order460-worker-semantic-capture-r9-20260912\baseline90.json`,
SHA256 `b614fa0550b5616c7c79a7d7a97b14637307f3269f77b05c25f4f32786aa2e24`.
Stage3001 then succeeded with child PID5620 at exact native start
`2026-09-12T18:01:02.5578541Z` and supervisor PID8272 at
`2026-09-12T18:00:59.4471982Z`. Stage receipt SHA256 is
`44545529c8b273bcbf8414880eba3e40592ce0dbbccde4fddaac79e57ccac12b`;
its separately bound classification receipt is
`c41271ede0aae294934edfc3e6ff8d49ea31d7dd2d12a0a82a5aa7a07dc8548e`.
The sanitized readiness was
`blocked_supplier_issue_status_unavailable`: launch health accepted, invoice
ineligible. This is a truthful accepted staging variant, not invoice readiness.

Settled90 completed and was accepted with `classificationPending=false`, all129
tables, and outbox head71. The worker proof is
`D:\Yellow\temp\order460-worker-semantic-capture-r9-20260912\settled90.json`,
SHA256 `6336c4b2dfab3a80876fbe398eb2b6345537a8ff876dc33c181b95b6948561b1`.
Owned StopStage then succeeded with `workerProofAccepted=true`; stopped-receipt
SHA256 is
`cf8e01ef5c3343c17d5a43ba0638acfd9aa3af92be7edfdce4eba06ef4c67db5`
at `2026-09-12T18:01:34.9045197Z`. Root independently confirmed child5620 and
supervisor8272 absent, ports3000/3001 absent, and the sole native PostgreSQL listener
still PID2340 on55503. The preserved pre-stop status remains separately hash-bound
at `d6ce92c1d56aa601ee3640edbb89868f1b7657fe645c41dd0d143a14060d92b5`;
the distinct stopped receipt is the authoritative cleanup proof.

This is the successful native staging/convergence/owned-cleanup proof for the saved
candidate and frontier90. It does not promote the candidate, start the main app, or
establish invoice eligibility while the supplier issue-status prerequisite remains
unavailable.

### Receipt-only retry-r8 baseline → retry-r9 baseline accounting

As a separate offline accounting exercise, the reviewer compared only the preserved
r8 and r9 Baseline90 receipts. The executable reviewer-owned check
`offline-r8-r9-baseline-delta-review-20260912.ps1`, SHA256
`95dab2a729bd60cee5e72fbf36c37fe5d1fe40bf2956b1c8ce8a96b7c00da5ef`,
passed1131 assertions without SQL or runtime action. Both receipts retain the same
129-table inventory and source/query pins;124 table closures are unchanged. Exactly
five worker-owned tables changed: three inserted `business_day` rows, three matching
append-only facts, three contiguous outbox events (head68→71), six matching processed
markers (three for each of the two pinned consumers), and two cursor advances68→71.
All three business-day insertions are local-date-bound openings for2026-09-12 with
the exact source actor, fact/event/payload/correlation linkage and receipt-window
timestamps. Property-date and reservation semantic closures remained exact.

The baseline-end gap is exactly793.611737 seconds. Therefore this comparison is only
a receipt-bound classification of the retained r8→r9 worker delta; it explicitly
records `observedConvergence=false` and is not a <=60-second Settled90 proof. The
accepted r9 Settled90 receipt above, rather than this optional accounting comparison,
is the bounded native convergence evidence.

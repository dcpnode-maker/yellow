# Order 184 corrected material-theme review

conclusion: CHANGES_REQUIRED — exact corrected product `3cffb5fa3254d951d6130dff597f5d616b15c2a1` at governance head `f1f767ec6506120621dc680fbb00211f0a911692` closes the recorded static adversarial findings and is independently static-approved, but final Order184 approval remains blocked because the mandatory independent real-browser matrix could not be executed.

evidence: Independent inspection confirms the correction is limited to `index.html`, `operator.css` and `material-theme-skins.test.ts`. Property context now precedes navigation in DOM/focus/visual order; the theme-specific hierarchy clipping and reordering rules are gone; mobile makes the app bar non-sticky and the domain bar `top:0`; the Windows XP title gradient now gives white 5.677:1–8.120:1 and muted 5.100:1–7.295:1 contrast across its endpoints; macOS no longer clips its top-level workbench; Apple remains a horizontal command shelf while Airbnb is a distinct 228px hospitality rail; WebKit material blur, solid unsupported-filter fallbacks, forced-colour coverage and reduced-motion containment are explicit. All sixteen skins retain complete and distinct typography/geometry/border/elevation/control/navigation/layout signatures over one semantic application. Theme switching changes only the root data attribute/select value and does not move DOM, change visibility, requests, property, route, drafts or focus.

files_and_lines: `src/http/operator/index.html:91`; `src/http/operator/operator.js:419`; `src/http/operator/operator.js:5708`; `src/http/operator/operator.css:767`; `src/http/operator/operator.css:822`; `src/http/operator/operator.css:837`; `src/http/operator/operator.css:995`; `src/http/operator/operator.css:1327`; `src/http/operator/operator.css:1354`; `src/http/operator/operator.css:1425`; `src/http/operator/operator.css:1439`; `src/http/operator/operator.css:1447`; `tests/material-theme-skins.test.ts:1`; `tests/operator-material-themes.test.ts:1`; Order184 and D-471/D-472/D-474/D-475 governance.

tests_or_checks: Reviewer-run focused 12 passed, 0 failed, 716 assertions; standing 241 passed, 481 database-gated skipped, 0 failed, 3,512 assertions; `bun run typecheck`; `bun run boundaries` (66 files); `bun run license-check` (23 packages); `bun audit` (no vulnerabilities); clean exact-scope `git diff --check`; independent all-theme text contrast minimum 4.634:1; exact per-file gzip total 97,940/98,304 bytes. A fresh sanctioned target selection for `http://127.0.0.1:3000/` returned `No browser is available`; no screenshot, computed-style, viewport, zoom, focus, fallback, state, overflow or console assertion is claimed.

risks: The corrected CSS has not been independently rendered in a real engine at 375/768/1024/1440 or 200% zoom, so runtime containment, sticky behavior, actual focus visibility, visual material differentiation, forced colours and no-backdrop-filter degradation remain unapproved. The payload has only 364 bytes of reviewer-measured headroom. The builder's 97,763-byte gzip figure did not reproduce under the same per-file method (reviewer: 97,940), although both pass the hard ceiling.

recommended_parent_action: Keep `3cffb5f` as the exact static-approved product but do not mark Order184 approved or promote it to the sole local from this review. Obtain a browser-capable independent non-implementer to execute all sixteen themes at 375 and representative desktop widths plus 200% zoom, including 44px/root-overflow/focus/state/fallback/forced-colour/reduced-motion/console checks and visual inspection of Yellow, Glass, Win95, Neo, Skeuo and Material. If that matrix passes, approval can be additive without another product correction.

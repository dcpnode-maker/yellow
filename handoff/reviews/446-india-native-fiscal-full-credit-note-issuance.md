# Order446 — full credit-note executable evidence

**Status:** implementation and database proof in progress; NOT independently
accepted, merged, deployed or Phase7 complete. All UI/UX work remains paused.

## Provenance and ownership

- `credit_note_sql_builder`: draft SQL, synthetic fixture and genuine database tests.
- `phase7_remaining_backend`: typed service/commands, hostile input tests and additive
  real PostgreSQL signed-session API proof.
- Coordinator: route/method integration, public exports, HTTP boundary tests,
  documentation, target admission and final integration responsibility.
- `native_helper_release_proof`: independent source inspection and personally
  executed financial, security and API proofs; did not implement446. Final
  repaired API acceptance and whole-draft schema equivalence remain outstanding.

## Actual early executions

1. Typed RED: context module absent,0pass/1fail. This was an import failure, not
   proof of failing accounting behavior.
2. Coordinator HTTP RED: both routes absent,0pass/9fail. Genuine app routing
   returned404; no financial endpoint was present.
3. Typed final focused tests:9pass/0fail/91assertions, including proxy/accessor
   rejection, exact input snapshot, identity-bound SQL, receipt validation and
   transaction ownership. Corrected trailing-high-surrogate validation and
   PostgreSQL JSON whitespace/key-order handling; malformed storage is503, not
   ordinary409 conflict. Raw receipt bytes remain unchanged.
4. Coordinator combined typed/API run:18pass/0fail/223assertions; typecheck passes.
   Adjacent invoice/submission/typed/API run:38pass/0fail/3explicit database-gated
   skips/461assertions; boundaries187files and23-package license checks pass.
5. Generated-correlation regression added: HTTP10pass/0fail/3explicit database
   skips/139assertions. One generated identity now reaches both command and response;
   replay's current correlation is a header, never a changed durable body.

The HTTP composition tests above use a stubbed database connection and verified
signed sessions. They are NOT real database financial proof. The separately named
`ACTUAL PostgreSQL + signed-session HTTP proof (not a database mock)` section is
gated on the admitted Order446 database pair and must actually run before acceptance.

## Database execution and retained failures

Order446's written native admission is authoritative. Two exact synthetic clones
were created from pristine77 on retained native55503 and migrated by the canonical
runner to86, nine forward migrations each. Independent-shaped outside/template
hashes match after creation/migration; live app7568/9508 remains unchanged.

First whole draftc1fc8884… failed on reserved-word alias `day`; native psql
--single-transaction rolled back the installation. Correctedf41e7e49… installed
successfully on candidate only. Upgrade remains86 without credit objects.
First ordinary financial case failed42501 before financial writes because its
synthetic fixture lacked existing adjustment permission rows; fixture corrected,
not production authority weakened. Logs are bounded private artifacts under
`.yellow/evidence/order446/builder-logs/`, not evidence of feature completion.

## Independent open findings

- Check durable key reuse before resolving a changed original: any changed payload
  with an existing key conflicts consistently without revealing target existence.
- Prove distinct-key same-original contention and both actual seal/credit race
  orders, not only sequential permission checks.
- Prove actor-status and scoped-property revocation on first call and replay.
- Compare every inverse revenue/tax line to independently derived original lines;
  balanced aggregate totals alone cannot establish correct account allocation.
- Require non-vacuous negative pre-issue and multiple-root transfer scenarios.

These observations were sent to the implementer and are being addressed. Do not
convert this list or a builder's pasted output into independent acceptance. Final
exact-source financial/RLS/numbering/rollback/API proof, whole draft installation,
canonical87 promotion/readiness/schema/CI and release remain separate gates.

## Frozen SQL and independently executed financial proof

The builder addressed all five findings above and froze draft SHA256
c8b4ada5702807a0705a13e888e95730e0dbcc8ac7796e0ad2358208a5f873ba.
The independent reviewer additionally identified a genuine temporary-schema
shadowing attack against unqualified pg_locks. The repair qualifies that catalog
and places pg_temp last on all12 affected functions without widening their ACLs.
The reproduced pre-repair attack and transactional repair fingerprints remain in
private bounded logs. Hostile temporary type names fail closed without effects;
clean-session retry succeeds. Unrelated predecessor functions were not changed.

Independent personal invocation on the final SQL source:
`builder-run.ps1 -Mode Test -Pattern '^(?!.*one hundred)'` passes30/30,
163assertions,94.19s. Log20260907-173112-682-Test.log includes actual invoice
corrections, multiple-root transfers, IGST/UTGST/zero-rounded tax, int64 and366-night
bounds, same/different-key concurrency, production audited day-seal races,
revocations, eight rollback stages and hostile temporary relation/type checks.
It excludes the separately run100-original numbering cohort, not a hidden skip.
Outside DB/role/template fingerprints and live3000 identity remain unchanged.

## Actual API failure and implementation repair

Independent real PostgreSQL signed-session HTTP invocation initially fails0/1:
expected201, received503 in20260907-173816-706-ActualHttp.log. Read-only inspection
confirms all six financial artifact counts are zero for the failed request.
This was not a timeout or SQL accounting failure. Bun1.3.14 SQLResultArray has
own keys0,length,count,command,lastInsertRowid,affectedRows; the typed decoder
incorrectly required only0,length and rejected a genuine successful result.

Coordinator added a matching native-container regression: first0/1 RED, then
10/10 typed GREEN after accepting only the observed own data metadata fields.
Exactly one own data row is still required; unknown fields, proxies and getters
remain rejected without invocation. Receipt fields and raw stored bytes stay
strict. Combined typed/stubbedHTTP20/20,234assertions and typecheck pass.
Independent repaired actual HTTP execution is required next; the initial failure
and zero-partial-artifact result are retained, not overwritten by unit success.

## Runtime release preparation

Q217 adds exact credit table columns, primary/unique/tenant-coherent foreign keys,
nine normalized CHECK constraints, property index, seven trigger shapes and
12function signature/owner/ACL/configuration/mode checks to runtime readiness.
Coordinator personally executes this query on the admitted candidate as runtime:
all20 catalogue flags true and the app-role read-permission probe passes. Before
and after outside/template/live snapshots match. Unit proof rejects false/null/
undefined results; source checks and typecheck pass. Canonical frontier remains86
until whole-draft/independent promotion proof, and no local app is restarted.

## Independent final financial and API acceptance

`native_helper_release_proof` did not implement the Order446 SQL, fixtures, typed
service, HTTP routes or tests. After the builders stopped both admitted targets and
confirmed that no sessions or fault injection remained, this reviewer personally
executed the final proofs below on retained native PostgreSQL16.15 at loopback55503.
Database URLs were constructed from protected local credential files in memory and
passed only in child-process environment variables; credentials were never printed
or placed in command arguments.

The immutable financial inputs used for the review were:

- draft SQL `c8b4ada5702807a0705a13e888e95730e0dbcc8ac7796e0ad2358208a5f873ba`;
- financial integration test `fc08c8ec5c149c99a45f349f573d88db37eb8c59062f2c469fd81b467729b2e4`;
- actual HTTP integration test `aec6fc9747c9cc2ce73e7e12c12c903cac46d31fe35b04d50b84b9afa6b57c63`;
- populated-upgrade test `c9241359b0c4a1280ad0b144130097fbfaefee2847fcf2126301278409b5f2f3`;
- credit fixture `54ef85b711dd4fcb4579a6b905e360a728c2f1a7883bf22ce9fe8377f17cfe43`;
- bounded runner `ff582a964f9657191132515659ba1fadf6d27539bcf65e42e24b41b413fd23cf`.

Exact independent financial commands and results:

1. `& .yellow/evidence/order446/builder-run.ps1 -Mode Test -Pattern '^(?!.*one hundred)'`
   passes30, fails0, filters1, with163 assertions in94.19s. Protected log:
   `20260907-173112-682-Test.log`.
2. `& .yellow/evidence/order446/builder-run.ps1 -Mode Test -Pattern 'one hundred'`
   passes1, fails0, filters30, with302 assertions in247.30s. It creates100
   distinct originals and verifies contiguous C1-100, every recomputed content
   hash, every previous-hash link and the exact series tail. Protected log:
   `20260907-173318-870-Test.log`.
3. `& .yellow/evidence/order446/builder-run.ps1 -Mode UpgradeTest -Pattern 'genuine issued invoice'`
   passes1, fails0, with6 assertions in4.84s. The whole frozen draft reaches the
   intentional PZ446 tail inside a populated86 transaction; schema, function ACLs,
   ledger and every public-table row equal their pre-transaction fingerprints after
   rollback, and the pre-existing invoice still exact-replays. Protected log:
   `20260907-174344-722-UpgradeTest.log`.

The 30-test invocation personally covers independently derived persisted inverse
lines, correction and unrelated multiple-root transfer cases, IGST/CGST-SGST/UTGST
and rounded-zero tax, signed-int64 and366-night limits, same-key8 and distinct-key8
contention, both production `BusinessDaySealService` lock orders with observed lock
waits, actor/property/permission revocation on first call and replay, direct-DML and
private-helper denial, hostile temporary `pg_locks`, hostile built-in type names
failing closed42501 with zero effects followed by clean success, Unicode500, all
eight artifact rollback points, pruning/reversal denial and deferred completeness.

## Repaired real HTTP proof

The retained0/1 HTTP failure above caused the coordinator to repair only the typed
result-container decoder. The reviewer independently observed the native shape in
`20260907-174145-073-RowShape.log`: Bun1.3.14 returns an `SQLResultArray` with own
data keys `0,length,count,command,lastInsertRowid,affectedRows`, while its row is an
ordinary exact record. The repaired decoder reads no metadata values, rejects
metadata accessors, proxies and unknown keys, and still requires length1 plus one
own enumerable data row. Final typed service SHA256 is
`9211f82e605cf3d32a7d0de7275043b1abade8aeee30b7482b7fc83d7f80e7e8` and its test
SHA256 is `50ca351d4c52550a3c1b11cb548c4f659c93b177a29438ac9c2707dfb78afb6d`.

The reviewer then reran, with protected URLs supplied only through the environment:

`C:\Users\astha\.bun\bin\bun.exe test tests/operator-fiscal-credit-note.integration.test.ts --test-name-pattern 'ACTUAL PostgreSQL'`

The genuine signed-session route passes1, fails0, filters10, with16 assertions in
7.89s (`20260907-174951-388-ActualHttp.log`). It proves real issuance, byte-exact
read and replay, changed reason/key/original conflicts, missing and cross-tenant
concealment, exact credit/journal/document/fact/outbox/idempotency census, unchanged
source graph, and denial after current authority is revoked. The earlier failed
request's two newest fixture tenants each retained zero credit bindings, correction
journals, credit documents, credit facts, credit events and credit idempotency rows.

The final pure command
`C:\Users\astha\.bun\bin\bun.exe test tests/build-readiness.test.ts tests/india-native-fiscal-credit-note.test.ts`
passes16, fails0, with222 assertions. This includes the real native result-container
regression and readiness failure on false, null or undefined catalogue evidence.

## Whole-draft equality, readiness drift and containment

The incrementally repaired candidate was not assumed to equal the whole draft.
`bun run .yellow/evidence/order446/independent-catalogue-equality.ts`, helper SHA256
`9855f563eb040f5db654254fa6ab10753a640231389390ba9e7a3cc4bf473922`, compares the
candidate with the complete frozen draft installed inside a rollback-only upgrade
transaction. Namespace, relations, columns, owners and ACLs, functions and configs,
policies, constraints, indexes, noninternal triggers, views and rules produce the
same normalized hash
`e81c5de92313a634268f3b9ecd0ba8c85b7274d3260d673d79eeacf0fad9e485` with no
differences. The upgrade target before and after rollback remains
`d70385614be4b45530738b12c7a7d4ba9c957461d5b470865c70c74f090b3cdb`.
Protected log: `20260907-174843-432-CatalogueEquality.log`.

Under Q217's exact target-local admission, the reviewer next ran
`bun run .yellow/evidence/order446/independent-readiness-drift.ts`, helper SHA256
`7580a86d2744ff7a086757452882ad399e6e3c77d8ef144b001bff89ce244846`.
The real direct `yellow_runtime` readiness path passes the intended baseline, denies
each committed one-at-a-time FORCE-RLS, tenant-policy, app-role INSERT, nonidentity
CHECK, property-index, commit-search-path, runtime-EXECUTE, private-volatility and
deferred-trigger drift, then passes after each exact `finally` restoration. The
normalized public schema plus every row of all129 public tables returns after every
case to baseline
`e09d69a3b755ffa6d58b1942a8407a1231b465ce171315ffe0dec48e3b9ba41c`.
Protected log: `20260907-175603-618-ReadinessDrift.log`.

Correction to the earlier runtime-preparation paragraph: the independent read-only
probe observed **all required catalogue flags true -- 19 named flags in the returned
row -- plus the separate app-role read-permission transaction passing**. It did not
observe a 20-field catalogue row. The exact nine CHECK definitions were also read
from the candidate. Probe SHA256 is
`23ceafb720d37c240a95d31b4d08ab56aa89c215cdbd3b494eeaa79152ddb075`; protected
log is `20260907-180028-734-ReadinessProbe.log`.

After all restores, fault-injection function/trigger count is0. Every bounded runner
before/after check retains outside database/role/membership/settings SHA256
`1232059f6700f7fa7f6364065b0acb450105954c46b661bd37c64f83f8843d15`, pristine
template SHA256 `d5bcf002be8270c2e3ba15920482bd9170bdadb5211831de59ccc158653ff8fe`,
postmaster PID15956, live app PID7568/parent9508 and absent staging port3001.

Final native PG16.15 capture used
`pg_dump.exe --host=127.0.0.1 --port=55503 --username=yellow_deploy --dbname=yellow_order446_credit_candidate_20260907 --no-password --schema-only --no-owner --no-comments`
with `PGPASSWORD` in the child environment only. Raw private dump SHA256 is
`2d50461d73e0421977c3f820cf6c3642d3ceb1f2e325caf1bdde106f7499dbac`
(1,817,897 bytes). Existing `normalizeSchemaDump(raw,true)` produces SHA256
`d7e2b8516a9bf372d3e735ad688e702be0e08c5764667dacf4041627733b1ede`
(1,783,378 bytes).

## Independent decision and remaining gates

The final frozen financial SQL, fixtures, executable financial behavior, populated
upgrade rollback, typed boundary and actual signed-session HTTP implementation are
**independently accepted**. The five earlier financial findings and the temporary
schema-shadowing finding are closed by personally executed final proof, not by
implementer output.

Canonical0087 has since been copied byte-identically from draft SHA256 c8b4ada5...,
the kernel/test frontier has been prepared for87, and generated
`tests/schema/expected.sql` matches the independent normalized dump SHA256
d7e2b851.... The coordinator's current pure release-wiring run passes22/22 with419
assertions. These are preparation facts, not release evidence.

Still open are the clean canonical87 production migration/upgrade execution, its
independent referee run on the separately admitted collision-free target, complete
standing repository/setup gates, current PR CI and ordinary CodeQL, publication,
merge, deployment and founder-visible release verification. Phase7 remains open;
this review does not mark the application released or restart any runtime.

## Canonical test-source path delta

Before the populated upgrade target advanced beyond86, the coordinator renamed the
fixture export from `CREDIT_DRAFT` to `CREDIT_MIGRATION` and changed its URL from the
unpublished handoff draft to `../../migrations/0087_india_native_fiscal_credit_note.sql`.
Only the matching imports and `readFileSync` uses in the financial and upgrade tests
changed; no fixture, assertion or SQL logic changed. The reviewer inspected these
exact three files and proved the canonical migration and reviewed draft are
byte-identical at SHA256
`c8b4ada5702807a0705a13e888e95730e0dbcc8ac7796e0ad2358208a5f873ba`.

Post-delta file hashes are:

- fixture `4800e1464e3898318f8f4acf0d90dbe0de98fc500c93b22398594b0cf5e7fc2d`;
- financial integration test `5032c64445c3b7753246acb26fe8dde0a9aa4f900dfc93e7eec8db9911ad3c9a`;
- populated-upgrade test `e583214d5051b44f4178a5a87c0b0da8b18f723918da071d6af7690d78bc54bf`.

The reviewer personally reran
`& .yellow/evidence/order446/builder-run.ps1 -Mode UpgradeTest -Pattern 'genuine issued invoice'`.
The canonical-path whole-migration rollback passes1, fails0, with6 assertions in
5.11s; protected log `20260907-180745-191-UpgradeTest.log`. The upgrade database
remains at86 with exact schema, ACL, ledger and row restoration after PZ446.

The focused command
`& .yellow/evidence/order446/builder-run.ps1 -Mode Test -Pattern 'all twelve|ordinary:'`
passes2, fails0, filters29, with15 assertions in4.75s; protected log
`20260907-180815-534-Test.log`. It reconfirms all12 trusted-schema configurations
and one genuine ordinary credit with independently derived exact inverse lines and
an unchanged complete source graph. Every runner snapshot again retained outside
SHA256 `1232059f6700f7fa7f6364065b0acb450105954c46b661bd37c64f83f8843d15`,
template SHA256 `d5bcf002be8270c2e3ba15920482bd9170bdadb5211831de59ccc158653ff8fe`,
postmaster15956, live app7568/9508 and absent staging port3001.

The100-original cohort was not repeated: its executed test logic is unchanged and
the only source selection now reads byte-identical canonical SQL. Its earlier
1pass/0fail/302-assertion proof therefore remains applicable. This path correction
is independently accepted; canonical production upgrade/referee, standing gates,
CI, publication, deployment and Phase7 closure remain open.

## Independent functional publication-tree preflight

The reviewer inspected the read-only functional candidate prepared from exact base
`d0f2f86391dfa1529f5436e7d866834bedda3608`. Manifest
`.yellow/evidence/order446/functional-candidate-manifest.json` has SHA256
`cfa9b6b82e6d178700b535edb9502b294a23c860f285181873d3c8974e4bd47f`; its
preparation helper has SHA256
`af0309f84c734cab7526134b507c0b5014bc6a885869bc7abd7ceeb5dfc0114f`.
The immutable publication handle is Git tree
`705e95fb85e93c465a6cf48ad76ed87a60ec7339`, containing2047 regular Git blobs and
exactly33 admitted changed paths. The manifest's binary diff SHA256 is
`4b78ec28db34f17919eb6a482e3a784885f90d3dc24754779092db168c9e0f9a`.

The reviewer independently ran `git cat-file -t 705e95fb85e93c465a6cf48ad76ed87a60ec7339`,
`git ls-tree -r -z 705e95fb85e93c465a6cf48ad76ed87a60ec7339`, and
`git diff --binary d0f2f86391dfa1529f5436e7d866834bedda3608
705e95fb85e93c465a6cf48ad76ed87a60ec7339`. A read-only Bun verifier recomputed the
Git blob identity (`sha1("blob " + byteLength + NUL + bytes)`) of every one of the
2047 extracted files and compared path, mode and object ID with both `ls-tree` and
the manifest: all matched. The independently generated name-status set also equals
all33 manifest entries, and the independently generated binary diff has the stated
SHA256. Git clean-filter comparisons show every admitted file in the pinned tree is
the exact current accepted candidate; the one raw CRLF presentation difference in
`tests/build-readiness.test.ts` normalizes to the same Git blob and is not source
drift.

The accepted publication blobs include:

- canonical0087 `c8b4ada5702807a0705a13e888e95730e0dbcc8ac7796e0ad2358208a5f873ba`;
- command `5f53219941c82d6e36539785e3112a5f3d52f948f189e941d1eb8f942ec1d0f9`;
- typed service `9211f82e605cf3d32a7d0de7275043b1abade8aeee30b7482b7fc83d7f80e7e8`;
- tax-fiscal export `51ba1bf0f60c568fd3cac5f19841d66934874d7634e604dbf6999f41d8e04318`;
- selected `src/app.ts` `161d3e7089d5fd739741248f699d70d16bac1eefda8e786a784ce869b82acc00`;
- selected `src/http/operator.ts` `d07e509818b2b7cc750ce9ee1584dd9fe5a8da20553e4f2dff4431bf65ec1876`;
- typed test `50ca351d4c52550a3c1b11cb548c4f659c93b177a29438ac9c2707dfb78afb6d`;
- financial integration `5032c64445c3b7753246acb26fe8dde0a9aa4f900dfc93e7eec8db9911ad3c9a`;
- populated-upgrade test `e583214d5051b44f4178a5a87c0b0da8b18f723918da071d6af7690d78bc54bf`;
- actual signed-HTTP integration `aec6fc9747c9cc2ce73e7e12c12c903cac46d31fe35b04d50b84b9afa6b57c63`.

Release metadata is coherent in the pinned tree: there are exactly87 migration
files ending in canonical0087; `CURRENT_MIGRATION_FRONTIER`, pure readiness tests,
local review and release metadata all require87; setup expects129 tables after
migrations1-87. `src/kernel/build-info.ts` SHA256 is
`74fafa0036dbbc90fe36b2da28117c959816b4b13627f7e6de0a6ea326c3d811`.
Its runtime query retains all19 named catalogue flags and additionally performs the
separate read-only app-role permission proof; the new three credit flags must be
literal `true`, as must every earlier flag. Generated `tests/schema/expected.sql`
is the independently accepted normalized PG16.15 schema at SHA256
`d7e2b8516a9bf372d3e735ad688e702be0e08c5764667dacf4041627733b1ede`.
CI runs the full financial suite and actual HTTP suite on an isolated fresh87
database, the populated86 whole-migration rollback test, and current readiness on
an isolated87 database. Release metadata publishes frontier87.

The exact candidate diff has no deletion. It contains no guest-picker source or
tests, `partyProfilePickerJs`, `operator-party-profile-picker.js`, Urbanist font,
Phosphor icon, design-prototype, governance/history/status/review file, `.yellow`,
or private-evidence change. The only `src/app.ts` hunks are the two credit routes;
the only selected `src/http/operator.ts` hunks are credit imports, scopes,
validation/response helpers, error mapping and issue/read methods. The full pinned
tree naturally retains pre-existing application UI files from the base, but it
introduces no UI/UX delta. The real repository index remained byte-identical across
preparation and after this inspection at SHA256
`5cbf7592939c2a02cf6b93ad4401b58ddae2ba5671f4df07393bf1293ad7fc42`;
its staged-entry semantic SHA256 likewise remained
`a9d1ae3e7d46f9ac5ff06294daa95aa3f4f761fe7172ddd364c1a70248670d8f`.

The manifest records archive SHA256
`4242efddec25ca3588b2183d61e3c28cf3af40206e0c47845ed8fa0456f159f4`.
The preparation helper verified that archive's extracted path set and every blob
before deliberately deleting its temporary archive. That historical tar cannot be
rehash-checked now, and a later `git archive` tar is timestamp-sensitive; publication
must therefore use the independently verified Git tree handle rather than treating
a newly generated tar byte hash as identity. Four standing-gate log files and a
permitted `node_modules` junction were subsequently added to the validation
directory for execution; neither is in the pinned tree, and the directory must not
be copied wholesale as the publication source.

### Read-only inspection of the coordinator's canonical execution

The reviewer did not rerun seed, reset or migration writes. The root-used bounded
PowerShell wrapper SHA256 is
`6b9e73cdb29d5ae21d1fa547277622354a09a6432500425c4eec87a291d4c74a`;
its TypeScript verifier SHA256 is
`97ce543628c1230a2597ec86cac2b98864d85d905cde1d2bd7cb963bf33f09f4`.
Source inspection found the admitted loopback55503/database/role checks, environment-
only protected credentials, explicit write-mode switches, 420-second/5MiB child
bounds, collision refusal, no reset/reseed path, exact before/finally outside,
template and live-process snapshots, row/ledger/ACL preservation, no-op reruns, and
normalized schema equality. The verifier pins canonical0087 plus the seed, invariant
referee and migrator at SHA256 respectively `c8b4ada5...`,
`f8e8147800bc3ee24ba5020b70f95ad77a987c698d3c63dd664ed8d4cba1a409`,
`2afa95bb7c02cd9637ffc9c3df00d1ddf7cfc5d8d31c4fd8fad29b950c1a418d`
and `1c744395992ad99cb7eb44c5db811c4edddf2fb1169720aac96445d1042c6354`.
No source-level blocker was found.

The actual upgrade log SHA256
`aac31565f1f6c63efba622ce688d60c05e0dbf18bc772c9fe7504cdb259612d7`
shows only0087 applied to populated86, then an applied0 no-op,129 tables and exact
old-row/ledger/ACL preservation. Its schema-equality log SHA256
`be20089c77879faaac1a825ae6e31b4732224f3f4b22bf8b05bb9c7709bf852e`
binds normalized schema `d7e2b851...`. The clean referee migration log SHA256
`7f6a5ca96e5942b9db14567a33a3a7cd9beb52190ead8e6a2f4cdab751026903`
shows exactly0078-0087 applied, then applied0,129 tables and preservation. Its
referee log SHA256
`6c72cf0fbff488271cc3c1bce44ebd9841829bc3c61f724f6223ad876bcadd2e`
reports11pass/0fail including concurrency, invariants, sealed-day denial, gapless100
and RLS isolation. The exact clean post-migration snapshot and pre-referee snapshot
are byte-identical SHA256
`69f69c748681e7507c0fb644fa87ee0c03596f7417fdb4a97b420a18e35a614c`;
the post-referee verifier reports all pre-existing rows, ledger and functions
preserved. This is a source-and-log audit of the coordinator's run, not a substitute
for the independent financial execution recorded earlier.

### Publication decision at this boundary

No functional-source finding blocks publication of exact tree `705e95fb...`. The
financial SQL/API implementation remains independently accepted, and the pinned
tree faithfully packages that acceptance with coherent frontier87 and schema
metadata. Publication is eligible only after the complete standing suite succeeds
against this exact Git tree in a valid Git context, dependency licensing is proven
against the real23-package installation, and new exact-source PR CI plus ordinary
CodeQL are green. At the time of this preflight, the first extracted-tree test run's
two failures were the known `.git`-history-dependent archive tests and its
zero-package license result was not sufficient; the authorized Git-context rerun
and real-package license proof were still open. This review does not authorize
publication from a different tree, merge, deployment, runtime restart, live3000
change, Phase7 closure or a released claim.

## Published functional checkpoint and attributed standing evidence

This section distinguishes the reviewer's personally executed high-risk proof from
later standing and publication operations. The independent SQL, concurrency,
rollback, readiness-drift and actual signed-HTTP results above remain the D-84
review evidence. The complete source-only standing suite below was executed by the
coordinator's standing-gate worker and inspected by this reviewer; canonical
upgrade/referee and publication were executed by the coordinator and independently
audited here. Neither is mislabeled as a second reviewer-run database proof.

Against the unchanged2047-blob artifact for tree
`705e95fb85e93c465a6cf48ad76ed87a60ec7339`, the worker's valid-Git-context run
passes1899, fails0 and explicitly skips1375 database/Unix-gated cases, with33962
assertions across532 files in218.48s. Log SHA256 is
`4864d31238c7e4a38d30cbe75f55ae3783bd75f8f095182a41ff7607c63ea10c`.
The two archive-history tests that had failed only in the Git-less extraction pass4,
fail0, skip4 in their focused Git-context run, log SHA256
`ee527e948c8be30e808be755a2b41a30158b4310c86a0c56ef8a8e6cd908192c`.
Typecheck passes (log SHA256
`5a3973f79ed9becd5f23c4feff467513814de7ea381d75a4d2e339aa8b8edca9`)
and import boundaries pass187 files (log SHA256
`a641f33dd34df1b5bc3cf108bb830c3add480235402c990fd613a3a0ef0a821c`).
The real installed dependency proof passes all23 packages, log SHA256
`28463f6c8174daae303f5a35c001561701951371a121c10915ceb3a27d4b42a2`.
The reviewer confirmed the artifact and installed-context `package.json`, `bun.lock`
and scanner are byte-identical at SHA256 respectively
`7db3c6a252fa817f6a89ab9c1d321d0ebcef266a072838a7e54d6a96e112ed24`,
`16b1eb40baf0797fca6d8b57f4d0eba021525413f860f73e90838135217ffd1b`
and `61d24ca6351afa7981d7bf75a4987c97e9a24da690e4187da5fa5e9458ec6869`.
The earlier zero-package extraction result remains documented as
insufficient and is not counted.

The reviewer fully inspected frozen publication helper SHA256
`e2532844c5a0819a48762767f5e839a84e14e2eedd762c212f01a1096e23bc65`
and independently validated prepared manifest SHA256
`b29c4f62d1ca02d8d0541316e10ccf4f46076ab1221fd456d1adb08ea89f8952`.
Its fresh original index backup has raw SHA256
`6c0427a89780a5c70bbf07a360b37731f6c136ea3e70416ddc5617103e03f7b3`,2048
stage-zero entries and semantic SHA256
`a9d1ae3e7d46f9ac5ff06294daa95aa3f4f761fe7172ddd364c1a70248670d8f`,
exactly equal to the original functional manifest's entries. The earlier raw
`5cbf7592939c2a02cf6b93ad4401b58ddae2ba5671f4df07393bf1293ad7fc42`
to `6c0427a89780a5c70bbf07a360b37731f6c136ea3e70416ddc5617103e03f7b3`
change is recorded
honestly as raw-only index drift of unproven cause; no old bytes were restored.
Projected index SHA256
`32713ba01c4f2bf864dee364f10949f7ab6b997db8a090f7cf9d83b4fccd551b`
has2056 entries exactly equal projected tree
`c61ab39ed37ca8781b2e8b5aa225f1a5707f436d`: every path outside the33 functional
paths retains its original entry, every nonmixed functional path equals tree705,
the existing staged Urbanist/Phosphor additions remain staged, and only the exact
party-picker additions remain unstaged.

The coordinator then ran the root-only publisher successfully. Execution-started,
new-commit and completion records have SHA256 respectively
`61aa0c53458c3e2342357666bf2e373f1f48defc3438c09c3668520f6faa5474`,
`26491e127df90ead05dfca8089689dc1ab4dc032b5aef41621348435eb902901`
and `9880ebd00be3d27fc846d201c4893ca5104a32dbb43834e358a4d1f2c9b6a163`.
Commit
`7f416a0e0a28a6944dbf95fa1134d42085fa8ad4` has exact tree705, sole parent
`d0f2f86391dfa1529f5436e7d866834bedda3608` and the scoped Order446 message. The
completion record binds those exact original and projected index hashes. GitHub
draft PR92 reports that exact head commit. The reviewer did
not execute the publisher, commit, push, merge or alter the index.

### First exact-head CI observation

Normal CodeQL run34152533398 for head `7f416a0e...` passes all three analyses:
Python, Actions and JavaScript/TypeScript. Each SARIF upload completed; the JS/TS
job scanned737 TypeScript and9 JavaScript files. Optional AI-review run34152536646
is separate and fails HTTP403 because the actor is not licensed for Copilot; it is
not normal CodeQL and is not counted as a product or security finding.

CI run34152535210 passes local-review, quality, Windows-state, native ARM64 and
container-smoke, but its database job fails before reaching any Order446 step.
`tests/financial-postings.integration.test.ts:194` expects128 public base tables
after a fresh current migration; canonical migrations1-87 correctly produce129.
The log reports Expected128/Received129 in P1, then the Phase3 gate aborts and skips
migration, seed, Order434, all actual Order446 SQL/HTTP, readiness/containment,
compatibility, acceptance, referee and runtime steps. This is a stale current-
frontier assertion outside the33-path functional checkpoint, not a credit-note SQL
semantic failure. It is nevertheless a genuine red required gate: no all-six-CI or
CI-native credit claim is made until an exact-head correction and complete rerun
pass.

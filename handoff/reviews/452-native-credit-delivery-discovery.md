# Order452 — Independent backend review and proof record

**Status:** functional native and canonical89 upgrade/referee/readiness proofs pass;
full standing and publication remain pending. UI/UX is paused; no live app or
external provider action.
**Reviewer:** root Codex, not SQL, service, command, HTTP or test implementer.

Root read the complete SQL draft, service/command/export/GET changes, four test
files and fixture; reviewed canonical0078/0079 and Q187 policy rather than inventing
legacy or multi-provider credit states. Authority precedes absence, both current
permissions are required, immutable credit/original/content hashes are checked,
head lookup is limited2, and unchanged0086 projects the durable signed receipt.
No broad fiscal-submission SELECT grant or new table is added.

Root's first focused run5/0(69) passed but did not satisfy the complete contract.
Root required meaningful paused-query input mutation, broader receipt/hostile
coverage and an actual gated signed-session native case. Root found that the first
native HTTP token used dummy identities instead of generated tenant/actor; the
implementer corrected it before execution. No failed native call is fabricated.
The final source-only combined run is10 pass/0 fail/17 explicit gated skips with
176 assertions across four files; fresh typecheck passes. Skips are NOT native proof.

A compiler invocation briefly reported TS2540 in the unchanged Order450 test,
whose BEFE9285 file hash matches its published bytes. Subsequent fresh compiler
invocations pass. Its cause is not established; no old file or suppression was
changed and the initial diagnostic remains in the execution transcript.

Q241 StageA was personally executed through fully read frozen helper8a4803f5.
Complete canonical state remains byte-identical1d79d66d; both proposed new names
are absent, existing server and live app identities unchanged. Receiptd6f63068
records read-only proof, not permission to claim draft/native success.

Native fault injection must remain isolated, no-argument and bound to exact newly
created fixture UUIDs, with whole-transaction rollback and exact helper removal.
Real legacy/multi-head exclusions are tested without weakening historical guards.
Frozen draft SQL26aac42e and inferred body702a035e are source hashes only until
actual database execution independently verifies them.

Before native execution, root required permission restoration in finally and exact
42501 denial checks at the signed-HTTP fixture. The next actual typecheck caught
TS18048 (possibly absent grant); the implementer added an explicit fail-closed
guard, yielding HTTP testb7814919 and a fresh passing root compiler invocation.
No database execution preceded this repair. Root fully reviewed frozen native
runner1160956f/proofbd3f6fdc and personally generated Review manifestdf391e94;
Q241 now admits only its exact isolated StageB sequence. Native results pending.

First StageB execution092227 created exactly the candidate database and verified
complete clone equality (receipt8bb2d48f). It then failed before rollback tests or
draft installation: the helper compared PostgreSQL C-ordered database rows with
JavaScript localeCompare ordering. AfterClone and final classification both retain
the actual error. Root independently read the captured after-state and verified
the exact filtered prior database rows, all four companion snapshots and global
roles/memberships/settings are unchanged. No functional native case has run.
The same clone is retained; a narrow explicitly pinned resume path is being
prepared. No automatic delete or second clone is authorized.

## Actual native execution and test-oracle recovery

Root executed admitted ResumeAfterClone093224. Rollback3/0 (19 assertions) proved
canonical predecessor constraints and real late-fault production-runner rollback.
Exact draft26aac42e installed with actual normalized prosrc702a035e and owner/app
only EXECUTE; installed receiptcbe7eb698ad9a02a3a3c958a8f61c263925820de9a4f8fd9284979ff7d1b6861.
SQL returned9/2 (153 assertions), so HTTP did not run. First failure checked lookup
before its natural15-second due time; second counted adapter construction as a
provider request. Real permissions/revocations, terminal/retry paths, second-provider
denial, owned outbox pruning and five rollback-only corruption probes executed.
Finald3ada4ae preserved all original rows, complete installed catalogue and all
companion/role state; candidate3080 rows included9 genuine new cohorts/27 codes.

Implementer changed ONLY SQL-test oracles (hash914825b1), retaining genuine natural
database-clock eligibility, explicit busy response and exact provider-request/wire
counters. Root read the complete changes and runtime-only helper extensions.
Root generated Reviewd8b7022d, then read-only full3080-row baseline301f76e4; see
Q241 for exact frozen manifests, commands and separate runtime-only admission.

Root personally executed ResumeRuntime095023 on the SAME candidate, without
cloning, reinstalling or rerunning rollback:

- SQL: **11 passed / 0 failed**,164 assertions,48.41s.
- Signed-session HTTP: **3 passed / 0 failed**,83 assertions,5.27s.
- Final phase tested;4122 rows, all3080 immediately preceding rows and all2054
  original clone rows preserved;9 new cohorts/27 codes, cumulative18/54.
- Exactly one additional new owned outbox-prune cohort; all five temporary fault
  helpers absent; complete installed structure and every companion/role unchanged.
- Postmaster15956 and live7568/9508 unchanged;3001 remains absent.

Actual logs: `.yellow/evidence/order452/native-logs/20260908-095023-573-8b774a62-sql-runtime.log`
and matching `-http-runtime.log`. Full final receipt `-final-runtime.json` SHA256
8980ea7f743c5423a02969eee2f232a4549b4d7f1afa103ddd085b64868e1f41.
Companion snapshot stays011a7217e2ba232fdc37e356e42403a478048932e97aa9a4a2763cdc34a1b5bd.
Earlier failures remain; no skip or implementer-pasted result substitutes for this
root-executed proof.

After native success, root applied exact canonical0089 (verified26aac42e), reviewed
readiness8202628c and release-wiringd0d0dd5e. Initial focused release tests returned
24/1 (432 assertions) and compiler TS2448/TS2454: the newly extended workflow test
referenced q208End before declaration. Only declaration ordering is being repaired;
canonical execution and release completeness are not claimed yet.

The implementer moved only q208End's declaration before its first assertion.
Root independently read that change and reran all five focused files:25/0 with571
assertions. Fresh compiler passes and191 import-boundary files pass. One attempted
boundary-script spelling was invalid; the actual package script `boundaries` was
then executed successfully, without changing scripts or suppressing checks.

## Executed canonical89 release proof

Root fully read StageC source, required complete existing-database metadata,
the older446 candidate's34,430-row snapshot, pinned functional4122-row final proof,
atomic mutation apply/conditional cleanup and a non-superuser grant-option probe.
An initial PowerShell merged-return token failed before DB work; the repaired
read-only preflight passed state checks but hit a pretty-JSON evidence limit.
Compact full-content JSON retained every field; measured existing state4,876,526B
plus clean-target0.43MB justified a fixed6MiB evidence cap before writes, while
child logs stayed5MiB. All earlier failures are retained.

Frozen runnerb675aee6/proofb281b759, source reviewd598daf0 and preflight157f58a5
were personally executed as separately admitted in Q241:

1. Populated canonical88 target2054 rows upgraded via the production migrator,
   applying ONLY0089. All prior rows, first88 ledger entries/timestamps, functions,
   catalogue and sequences preserved. A second migrator run was an exact no-op.
   Receipt043b6bc6f97bf78af3f66e2bb1cf12084735af232452cfa4ed054a9f7bf711c3.
2. New exact clean89 target cloned pristine77, migrated78–89, preserved template
   rows and first77 ledger entries; exact no-op and clean/upgraded normalized
   schema equality pass. Unchanged seed and referee: **11 passed / 0 failed**,
   including100-way gapless numbering and actual tenant/table/view isolation.
3. Genuine runtime readiness rejects retained canonical88, accepts clean89, and
   rejects **30/30** committed reader/projector faults; all30 exact restorations
   immediately pass. Complete final state is BYTE-IDENTICAL to the immediately
   preceding readiness snapshotb6d1d524e51ab381ede80b85b04f370f936e155ad8af7c54ba8130149a965acb.
   Receipt9b2ba954cc9fa306e85e32d076b6fe760573bf47e045a30436425905c927257c.

Actual canonical logs are under `.yellow/evidence/order452/canonical-logs/`:
101934-1797522f upgrade,102140-7dcf4398 clean/referee and102600-871b5c48 readiness.
Root separately compared every protected companion/legacy/template row snapshot
and all outside metadata/roles before each next action. No unrelated data, server,
live app, provider, main branch or UI changes occurred.

The schema snapshot was generated by real pg_dump/normalization from both executed
databases. Root reviewed its two additive function/ACL hunks, applied them, and
verified `tests/schema/expected.sql` equals BOTH actual schema files at SHA256
78e76f927b299f43c76d3050ace81007aea84cae8bf0830c48451daee96b9703.
Final whole-candidate standing, CI and publication are still separate.

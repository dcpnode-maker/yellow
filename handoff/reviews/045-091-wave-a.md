# REVIEW 045–091 — Wave A protected floor and inherited findings

**Reviewed by:** OpenAI Codex independent non-implementing reviewer
(`/root/review_order_092`) · **Date:** 2026-08-23

**Reviewed commit:** `4874f5cd8052435d5c5d2f67698f6088cff502fe`

**Verdict:** **APPROVED**

## Exclusive discharge scope

This review approves only Wave A's exclusive manifest owners: Orders **048, 050–052,
057, 064–066, 069–070, 074, 078–079 and 083**. It does not approve any Wave B, C or D
owner and does not treat builder output as review evidence.

## Environment and exact-tip confirmation

The reviewer executed the proof in the clean native-Linux clone
`/home/astha/projects/yellow-order-091-final-5d3f137` with Bun 1.3.14.

```text
git rev-parse HEAD
4874f5cd8052435d5c5d2f67698f6088cff502fe

git status --porcelain=v1
<empty>
```

The first fresh Compose attempt used `yellow_wave_a_referee` on the repository's default
ports and stopped before assertions because host port 6389 was already allocated by the
clone's existing Valkey container. The reviewer did not stop or delete that existing
project. The complete database proof restarted in a new disposable project with
`COMPOSE_PROJECT_NAME=yellow_wave_a_referee2`, `YELLOW_APP_PORT=3100`,
`YELLOW_POSTGRES_PORT=5542` and `YELLOW_VALKEY_PORT=6489`. Post-proof `docker compose ps
-a` showed only healthy PostgreSQL and Valkey services; the app was never created or
started.

## Reviewer-executed commands and results

### Protected floor

```bash
git log --oneline --follow -- migrations/0001_init.sql
sha256sum migrations/0001_init.sql tests/run_invariants.py
git rev-parse 61b0fd3:tests/run_invariants.py HEAD:tests/run_invariants.py
git rev-parse 61b0fd3:migrations/0001_init.sql HEAD:migrations/0001_init.sql
```

Results:

- migration history contains only `bc0e492`;
- `migrations/0001_init.sql` SHA-256 is
  `fe2a9fc949c6bacded3f8d3fc4d14fc596a83ebde9aeb043eb10845f07b30923`;
- `tests/run_invariants.py` SHA-256 is
  `3228279bd99a8f9b6af99748f31d4d4b482a8e627e16d92644d9d859ad8befa1`;
- referee base/tip blob is identically
  `0d2b9d531a7a75797a2a6abeabf740826d19007d`;
- migration base/tip blob is identically
  `dce210b2c911efb63a49a4e58778d88084073494`.

### Static and supply-chain gates

```bash
export PATH=/home/astha/.bun/bin:$PATH
bun install --frozen-lockfile
bun run typecheck
bun run boundaries
bun run license-check
bun audit
YELLOW_SCHEMA_DATABASE=yellow_test bun run schema:check
git status --porcelain=v1
```

Results: frozen install checked 23 installs across 43 packages with no changes;
typecheck passed; import boundaries passed for 58 TypeScript files; 23 installed
packages passed licence policy; no vulnerabilities were found; schema matched
`tests/schema/expected.sql`; the reviewed clone remained clean.

### Fresh app-never-started referee

```bash
export COMPOSE_PROJECT_NAME=yellow_wave_a_referee2
export YELLOW_APP_PORT=3100 YELLOW_POSTGRES_PORT=5542 YELLOW_VALKEY_PORT=6489
./setup.sh --db-only
docker compose ps -a
```

Results: migrations 0001–0006 applied, the fresh database contained 84 public tables,
and the referee returned **`11 passed, 0 failed of 11`**. TC-12.4 personally observed
direct occupancy INSERT denied with SQLSTATE 42501; concurrency, balanced journal,
sealed-day, gapless numbering and table/view tenant-isolation checks all passed.

### F11/F12 and Order 083 gate

```bash
bun test tests/operator-assets-security.test.ts
YELLOW_PHASE3_GATE_ADMIN_URL=postgres://yellow:yellow@127.0.0.1:5542/postgres \
YELLOW_PHASE3_GATE_PASSWORD=YellowReviewOnly2026! \
  bun run test:phase3-gate
git status --porcelain=v1
```

Results:

- asset-security proof: **3 pass, 0 fail, 41 assertions**;
- Phase-3 gate: **13/13 suites passed with isolated databases**;
- owned suite evidence included Order 065 6/6, Order 066 6/6, Order 069 plus
  approval evidence 11/11, Order 070 8/8, review seed 11/11, founder status 4/4,
  Order 048 6/6, Order 050 7/7, Order 051 6/6, Order 052 7/7 and Order 057 6/6;
- no named proof was skipped and the clone remained clean.

## Assertion-fidelity inspection

### F11 — browser SQL false positive

`tests/operator-assets-security.test.ts` retains the exact ordinary copy
`Save or select a draft before previewing.` and proves it matches the rejected legacy
word guard but not `BROWSER_SQL_SYNTAX`. Six positive SELECT/INSERT/UPDATE/DELETE syntax
canaries match the bounded guard, while the complete operator script does not. This
tests the original failure mechanism rather than evading it by changing UI copy.

### F12 — independent-review coverage

`scripts/derive-review-coverage.ts` accepts only verdicts beginning `APPROVED` via
`/^APPROVED\b/i`. `tests/founder-status.integration.test.ts` derives coverage from the
review files, asserts exact coverage through Order 044, explicitly excludes
`045-073-gate-3.md`, byte-compares the generated approved-file list, and checks the
runtime snapshot uses that derived value. All four founder-status assertions passed.

### Order 083 — isolated inherited proof repair

`scripts/run-phase-3-gate.ts` declares 13 fixed database proofs, recreates and migrates
one database per file, forces each file's `YELLOW_REQUIRE_*` flag, aborts on non-zero
exit, and drops each proof database. The explicit `identity_inventory` review-seed mode
returns `rate: null`; only the Order-050 proof selects it. Order 050's original empty
configuration, FLEX, replay, authorization and rollback assertions passed unchanged
7/7, while the normal review-seed path retained published-rate evidence.

## Invariant and scope assessment

- Protected migration and referee artifacts are byte-identical to the reviewed base.
- Occupancy denial, journal balance, sealed-day, numbering and tenant table/view
  isolation were personally executed by the referee.
- Exact-money, immutable price/release history, four-eyes publication, concurrency,
  rollback and cross-tenant negatives in the owned suites executed without skips.
- This review made no implementation, migration, manifest, decision or order change.

## Findings

None. Wave A's exclusive orders are **APPROVED** at the exact reviewed commit. Remaining
manifest rows stay UNVERIFIED until their owning waves complete reviewer-executed proof.
